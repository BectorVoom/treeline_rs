---
phase: 08-pyo3-python-binding
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - crates/treelite-py/Cargo.toml
  - crates/treelite-py/pyproject.toml
  - crates/treelite-py/src/lib.rs
  - crates/treelite-py/python/treelite_rs/__init__.py
  - crates/treelite-py/python/treelite_rs/py.typed
  - crates/treelite-py/tests/python/conftest.py
  - crates/treelite-py/tests/python/test_frontend.py
  - crates/treelite-py/tests/python/test_predict_ab.py
  - crates/treelite-py/tests/python/test_zero_copy.py
  - crates/treelite-py/tests/python/test_serialize.py
  - crates/treelite-py/tests/python/test_sklearn_ab.py
  - crates/treelite-py/tests/python/test_errors.py
  - crates/treelite-py/tests/python/test_backend.py
autonomous: true
requirements: [PY-06]
user_setup:
  - service: maturin
    why: "Build the abi3 wheel into the existing uv venv (main tree)"
    env_vars: []
    dashboard_config:
      - task: "Install maturin into the uv venv if absent"
        location: "main tree: `uv pip install maturin` (research confirms 1.13.3 on PyPI)"

must_haves:
  truths:
    - "`crates/treelite-py` is a registered workspace member and `cargo build -p treelite-py` succeeds"
    - "`maturin develop` builds an abi3 cdylib named `_treelite_rs` into the uv venv"
    - "`uv run python -c \"import treelite_rs\"` succeeds (empty package imports)"
    - "`uv run pytest crates/treelite-py/tests/python` collects all 7 test files (RED/skip stubs, no errors)"
  artifacts:
    - path: "crates/treelite-py/Cargo.toml"
      provides: "cdylib _treelite_rs + abi3-py310 + per-backend features"
      contains: "crate-type"
    - path: "crates/treelite-py/pyproject.toml"
      provides: "maturin abi3 build config, python-source layout"
      contains: "[tool.maturin]"
    - path: "crates/treelite-py/src/lib.rs"
      provides: "#[pymodule] _treelite_rs skeleton with frontend/gtil/sklearn submodules"
      contains: "pymodule"
    - path: "crates/treelite-py/tests/python/conftest.py"
      provides: "shared fixtures + treelite/treelite_rs import + skip-if-no-upstream"
      contains: "treelite_rs"
  key_links:
    - from: "Cargo.toml (root)"
      to: "crates/treelite-py"
      via: "workspace members entry"
      pattern: "treelite-py"
    - from: "crates/treelite-py/pyproject.toml"
      to: "crates/treelite-py/Cargo.toml"
      via: "manifest-path + module-name = _treelite_rs"
      pattern: "module-name|manifest-path"
---

<objective>
Stand up the `treelite-py` workspace crate and its maturin abi3 packaging so an (empty) `treelite_rs` package builds, installs into the uv venv, and imports — and create the full Nyquist pytest scaffold (conftest + 7 RED/skip test files) that every downstream slice in this phase verifies against. This is the Walking-Skeleton-of-the-binding wave: it proves the build/import/test plumbing end-to-end before any capability is wired (PY-06).

Purpose: Everything downstream (`Model`, `frontend`, `gtil`, `sklearn`, error mapping, backend kwarg) depends on the crate existing, the wheel building as abi3, and the pytest harness being collectible. Wave 0 must land the test infrastructure (Nyquist Dimension 8) before any slice relies on it.
Output: a registered `crates/treelite-py` member; `Cargo.toml`/`pyproject.toml`; a minimal `#[pymodule]` cdylib `_treelite_rs`; the `treelite_rs` python-source package (`__init__.py` + `py.typed`); `conftest.py` + 7 pytest files as collectible RED/skip stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pyo3-python-binding/08-CONTEXT.md
@.planning/phases/08-pyo3-python-binding/08-RESEARCH.md
@.planning/phases/08-pyo3-python-binding/08-PATTERNS.md
@.planning/phases/08-pyo3-python-binding/08-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This phase (across all 5 plans) creates the following NEW symbols/files — exclude them from any drift/convergence source-grounding pass:
- The `treelite-py` workspace crate; the compiled cdylib `_treelite_rs`.
- `#[pyclass] Model` and its methods (`serialize_bytes`/`deserialize_bytes`/`serialize`/`deserialize`/`dump_as_json`/`num_tree`/`num_feature`/`input_type`/`output_type`/`concatenate`).
- `#[pyfunction]`s under submodule `frontend`: `load_xgboost_json_str`, `load_xgboost_ubjson_bytes`, `load_xgboost_legacy_bytes`, `load_lightgbm_str`, `detect_xgboost_format_bytes`.
- `#[pyfunction]`s under submodule `gtil`: `predict_f32`, `predict_f64`, `output_shape`.
- `#[pyfunction]`s under submodule `sklearn`: `load_random_forest_regressor`/`_classifier`, `load_extra_trees_regressor`/`_classifier`, `load_gradient_boosting_regressor`/`_classifier`, `load_isolation_forest`, `load_hist_gradient_boosting_regressor`/`_classifier`.
- The exception class `TreeliteError`.
- The `backend=` kwarg on `gtil.predict*`.
- New files: `crates/treelite-py/pyproject.toml`, `.pyi` stubs, `py.typed`, the `treelite_rs/` python package, all `tests/python/*.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register treelite-py crate + maturin abi3 packaging + minimal #[pymodule] that imports</name>
  <files>Cargo.toml, crates/treelite-py/Cargo.toml, crates/treelite-py/pyproject.toml, crates/treelite-py/src/lib.rs, crates/treelite-py/python/treelite_rs/__init__.py, crates/treelite-py/python/treelite_rs/py.typed</files>
  <read_first>
    - Cargo.toml (root `members` list — append `crates/treelite-py`; `[workspace.package]` for `*.workspace = true`; `[workspace.dependencies]` for `thiserror`)
    - crates/treelite-cubecl/Cargo.toml (the per-backend feature forwarding shape: `rocm = ["cubecl/rocm"]` etc. — mirror as `rocm = ["treelite-cubecl/rocm"]`)
    - crates/treelite-harness/Cargo.toml (path-dep `{ path = "../<crate>" }` discipline + feature gating)
    - 08-RESEARCH.md §"Installation" (lines 106-129: the `[lib] name="_treelite_rs" crate-type=["cdylib"]`, `pyo3 = { version="0.28.3", features=["abi3-py310","extension-module"] }`, `numpy = "0.28.0"`, the `[features] default=["cpu"]` block — copy verbatim) and §"Recommended Project Structure" (lines 190-214)
    - 08-RESEARCH.md §"Code Examples" "#[pymodule] with submodules" (lines 426-453) — the skeleton `#[pymodule] fn _treelite_rs(...)` registering `frontend`/`gtil`/`sklearn` submodules
    - treelite-mainline/python/pyproject.toml (shape reference only) + treelite-mainline/python/treelite/__init__.py (the re-export shape to mirror, D-01) + treelite-mainline/python/treelite/py.typed (empty marker — copy)
    - 08-PATTERNS.md §"crates/treelite-py/Cargo.toml" and "No Analog Found" table (pyproject/cdylib are pyo3 idiom — use RESEARCH)
  </read_first>
  <action>
    Append `"crates/treelite-py"` to root `Cargo.toml` `members`. Create `crates/treelite-py/Cargo.toml`: package header uses `version.workspace`/`edition.workspace`/`license.workspace`; `[lib] name = "_treelite_rs"`, `crate-type = ["cdylib"]`; deps `pyo3 = { version = "0.28.3", features = ["abi3-py310", "extension-module"] }`, `numpy = "0.28.0"`, `thiserror = { workspace = true }`, and path deps on `treelite-core`/`treelite-xgboost`/`treelite-lightgbm`/`treelite-sklearn`/`treelite-gtil`/`treelite-builder`/`treelite-cubecl` (default features = cpu); `[features] default = ["cpu"]`, `cpu = []`, `rocm = ["treelite-cubecl/rocm"]`, `cuda = ["treelite-cubecl/cuda"]`, `wgpu = ["treelite-cubecl/wgpu"]` (forwarding shape from treelite-cubecl). Create `crates/treelite-py/pyproject.toml` with `[build-system] requires=["maturin>=1.13,<2"]`, `[tool.maturin]` keys: `module-name = "treelite_rs._treelite_rs"`, `python-source = "python"`, `features = ["pyo3/abi3-py310"]`, `bindings = "pyo3"`, and `manifest-path` if needed. Create `crates/treelite-py/src/lib.rs` with a minimal `#[pymodule] fn _treelite_rs(m: &Bound<'_, PyModule>) -> PyResult<()>` that creates empty `frontend`/`gtil`/`sklearn` submodules via `PyModule::new` + `m.add_submodule(...)` and returns Ok — no functions yet (those land in later plans). Create `crates/treelite-py/python/treelite_rs/__init__.py` (empty stub re-exporting `from . import _treelite_rs` guarded so import succeeds even before symbols exist) and an empty `crates/treelite-py/python/treelite_rs/py.typed` (PEP 561 marker, D-10). Use abi3-py310 floor (D-09). Do NOT overload the root `pyproject.toml` — the maturin config is a separate file (RESEARCH Runtime State Inventory).
  </action>
  <verify>
    <automated>cargo build -p treelite-py 2>&1 | tail -5; cd crates/treelite-py && uv run maturin develop 2>&1 | tail -5 && uv run python -c "import treelite_rs; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "crates/treelite-py" Cargo.toml` (member registered)
    - `grep -q 'crate-type = \["cdylib"\]' crates/treelite-py/Cargo.toml` and `grep -q 'abi3-py310' crates/treelite-py/Cargo.toml`
    - `grep -q '\[tool.maturin\]' crates/treelite-py/pyproject.toml` and `grep -q 'python-source' crates/treelite-py/pyproject.toml`
    - `grep -q 'pymodule' crates/treelite-py/src/lib.rs`
    - `cargo build -p treelite-py` exits 0
    - `cd crates/treelite-py && uv run maturin develop` exits 0 and `uv run python -c "import treelite_rs"` exits 0
    - `cargo test --workspace` still green (no regression to existing crates)
  </acceptance_criteria>
  <done>The crate builds, the abi3 wheel installs into the uv venv via maturin develop, and `import treelite_rs` succeeds (PY-06 build/import proven for an empty surface).</done>
</task>

<task type="auto">
  <name>Task 2: pytest harness — conftest + 7 collectible RED/skip test stubs</name>
  <files>crates/treelite-py/tests/python/conftest.py, crates/treelite-py/tests/python/test_frontend.py, crates/treelite-py/tests/python/test_predict_ab.py, crates/treelite-py/tests/python/test_zero_copy.py, crates/treelite-py/tests/python/test_serialize.py, crates/treelite-py/tests/python/test_sklearn_ab.py, crates/treelite-py/tests/python/test_errors.py, crates/treelite-py/tests/python/test_backend.py</files>
  <read_first>
    - 08-VALIDATION.md "Per-Task Verification Map" (the 8 rows: PY-01..PY-06, MEM-04, D-05/D-08 → which test file + command) and "Wave 0 Requirements" (lines 59-65)
    - 08-RESEARCH.md §"Validation Architecture" (lines 559-606: the A/B headline test, zero-copy `__array_interface__['data'][0]` proof, the build-into-venv + run commands)
    - 08-PATTERNS.md §"tests/python/test_predict_ab.py" (lines 146-164: the A/B headline pattern, the fixtures list, the conftest pattern, zero-copy proof mirroring `crates/treelite-core/tests/serialize_pybuffer.rs:80-90`)
    - xgboost-master/tests/python/conftest.py (pytest-fixture idiom reference)
    - fixtures/ listing (binary_logistic.model.json, xgb_3format.{json,ubj,model}, lightgbm_{numerical,categorical}.txt, sklearn_*.golden.json) — the A/B model classes to iterate
  </read_first>
  <action>
    Create `conftest.py` with: a `FIXTURES` path constant pointing at the repo `fixtures/` dir; a seeded `rng` fixture (`np.random.default_rng(0)`); a session fixture importing both `treelite` (upstream) and `treelite_rs` with `pytest.importorskip("treelite")` skip-if-no-upstream (D-11 witness); and a `scipy` availability marker (scipy 1.17.1 confirmed present — gate sparse cells with `pytest.importorskip("scipy")`). Create the 7 test files as COLLECTIBLE RED scaffolds: each defines its target test functions with a real assertion body but marked `@pytest.mark.skip(reason="MISSING — implemented in 08-0X")` (Nyquist MISSING markers) so `pytest` collects them without error and they flip live as each slice lands. `test_frontend.py` (PY-01: load each fixture → assert `num_tree`/`num_feature`), `test_predict_ab.py` (PY-02 D-11: `np.testing.assert_allclose(treelite.gtil.predict(...), treelite_rs.gtil.predict(...), atol=1e-5, rtol=0)` across model classes/presets/kinds), `test_zero_copy.py` (MEM-04/D-03: `__array_interface__['data'][0]` unchanged + non-contiguous/wrong-dtype raises `TreeliteError`), `test_serialize.py` (PY-03: serialize_bytes→deserialize_bytes round-trip + dump_as_json parses), `test_sklearn_ab.py` (PY-04: fit estimator → import_model → A/B 1e-5), `test_errors.py` (PY-05: bad dtype/malformed bytes raise `TreeliteError`; forced panic surfaces as `TreeliteError` not abort), `test_backend.py` (D-05/D-08: `predict(backend='cpu')` works; un-built backend raises `TreeliteError`; `rocm` cell hardware-gated `@pytest.mark.skipif`). Reference fixtures via the `FIXTURES` constant; do NOT hardcode absolute paths.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && uv run pytest crates/treelite-py/tests/python --collect-only -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 7 test files exist under `crates/treelite-py/tests/python/`
    - `uv run pytest crates/treelite-py/tests/python --collect-only -q` exits 0 and lists tests from all 7 files (no collection errors)
    - `uv run pytest crates/treelite-py/tests/python -q` exits 0 with all tests SKIPPED (RED markers), zero failures/errors
    - `grep -q 'importorskip' crates/treelite-py/tests/python/conftest.py` (upstream-skip guard present)
    - `grep -q 'assert_allclose' crates/treelite-py/tests/python/test_predict_ab.py` and `grep -q '1e-5\|atol=1e-5' crates/treelite-py/tests/python/test_predict_ab.py`
    - `grep -q '__array_interface__' crates/treelite-py/tests/python/test_zero_copy.py`
  </acceptance_criteria>
  <done>The full pytest harness collects cleanly with all tests skipped as Nyquist MISSING markers; downstream slices flip each file to GREEN as they land. Wave 0 test infrastructure is in place before any slice depends on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → wheel | maturin reads the host interpreter from the active uv venv; wrong interpreter (worktree) builds against system Python |
| package install → venv | `_treelite_rs` cdylib installed alongside upstream `treelite` in one venv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | maturin builds against the wrong interpreter in a worktree | mitigate | Build + test on the MAIN tree under `uv run` only (RESEARCH Pitfall 5 / MEMORY.md worktree-isolation-unsafe-here); the verify commands use `uv run` |
| T-08-02 | Denial of Service | abi3 + GPU-feature native links coexistence | accept | Wave-0 ships cpu-only default; GPU-feature wheels are exercised in 08-05 (RESEARCH A2, low risk — native libs orthogonal to Python ABI) |
| T-08-SC | Tampering | cargo installs `pyo3`/`numpy` + pip `maturin` | mitigate | RESEARCH Package Legitimacy Audit ran slopcheck → 3 OK (all PyO3-org canonical sources); no [ASSUMED]/[SUS] packages → no human checkpoint required |
</threat_model>

<verification>
- `cargo build -p treelite-py` and `cargo test --workspace` green (no regression).
- `cd crates/treelite-py && uv run maturin develop && uv run python -c "import treelite_rs"` succeeds.
- `uv run pytest crates/treelite-py/tests/python --collect-only` collects all 7 files with zero errors; full run all-skipped.
</verification>

<success_criteria>
- `treelite-py` is a registered workspace member building a cdylib `_treelite_rs` abi3-py310 wheel (PY-06).
- `import treelite_rs` succeeds in the uv venv.
- The pytest harness (conftest + 7 RED test files) collects cleanly — the Nyquist instrument for the whole phase is in place.
</success_criteria>

<output>
Create `.planning/phases/08-pyo3-python-binding/08-01-SUMMARY.md` when done.
</output>
