---
phase: 08-pyo3-python-binding
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - crates/treelite-py/src/sklearn.rs
  - crates/treelite-py/src/lib.rs
  - crates/treelite-py/python/treelite_rs/__init__.py
  - crates/treelite-py/python/treelite_rs/sklearn/__init__.py
  - crates/treelite-py/python/treelite_rs/sklearn/__init__.pyi
  - crates/treelite-py/tests/python/test_sklearn_ab.py
autonomous: true
requirements: [PY-04]
user_setup: []

must_haves:
  truths:
    - "`treelite_rs.sklearn.import_model(fitted_estimator)` returns a Model that predicts within 1e-5 of upstream `treelite.sklearn.import_model` for RF/ET/GB/IsolationForest/HistGB"
    - "The estimator object stays in Python; only numpy arrays cross into Rust (zero-copy via the array-signature loaders)"
  artifacts:
    - path: "crates/treelite-py/src/sklearn.rs"
      provides: "thin pyo3 wrappers over treelite_sklearn array-signature loaders"
      contains: "pyfunction"
    - path: "crates/treelite-py/python/treelite_rs/sklearn/__init__.py"
      provides: "estimator→arrays extraction (port of importer.py) + dispatch"
      contains: "import_model"
  key_links:
    - from: "crates/treelite-py/python/treelite_rs/sklearn/__init__.py"
      to: "_treelite_rs.sklearn.load_*"
      via: "extract tree_.* arrays Python-side → call array loaders"
      pattern: "load_random_forest|load_gradient_boosting|load_hist"
    - from: "crates/treelite-py/src/sklearn.rs"
      to: "treelite_sklearn::load_*"
      via: "borrow numpy array-of-arrays → pass slices through"
      pattern: "treelite_sklearn::load"
---

<objective>
The sklearn marshalling slice: a `sklearn.import_model(estimator)` entry point that extracts a fitted scikit-learn estimator's tree arrays Python-side (port of upstream `importer.py`) and hands them to the existing `treelite-sklearn` array-signature loaders, producing a Model that predicts within 1e-5 of upstream `treelite.sklearn.import_model` (PY-04). The estimator object never crosses the FFI boundary — only numpy arrays do (D-01 discretion).

Purpose: PY-04 (`sklearn.import_model` marshals fitted estimators). The Rust side is thin `#[pyfunction]` wrappers over `treelite_sklearn::load_random_forest_*`/`load_extra_trees_*`/`load_gradient_boosting_*`/`load_isolation_forest`/`load_hist_gradient_boosting_*` (Phase-4 D-01 array signatures); the heavy estimator→arrays logic is the Python-side port of `importer.py`.
Output: `src/sklearn.rs` (array-loader pyfunctions), the `sklearn/__init__.py` extraction shim, `.pyi` stub; `test_sklearn_ab.py` flipped GREEN across the sklearn estimator set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pyo3-python-binding/08-CONTEXT.md
@.planning/phases/08-pyo3-python-binding/08-RESEARCH.md
@.planning/phases/08-pyo3-python-binding/08-PATTERNS.md
@.planning/phases/08-pyo3-python-binding/08-02-SUMMARY.md
@.planning/phases/08-pyo3-python-binding/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sklearn array-loader pyfunctions (src/sklearn.rs) + register into _treelite_rs.sklearn</name>
  <files>crates/treelite-py/src/sklearn.rs, crates/treelite-py/src/lib.rs</files>
  <read_first>
    - crates/treelite-sklearn/src/lib.rs (re-exports lines 42-66: `SklError`, `load_random_forest_classifier`/`_regressor` (bulk.rs :230/:294), `load_extra_trees_regressor` :65/`_classifier` :100, `load_gradient_boosting_regressor` (mixin.rs :336)/`_classifier` :397, `load_isolation_forest` (mixin.rs :268), `load_hist_gradient_boosting_regressor` (histgb.rs :526)/`_classifier` :576) — the exact array signatures (`&[&[i64]]`/`&[&[f64]]`, Phase-4 D-01) the pyfunctions wrap; the lib.rs doc lines 5-12 confirm "so the Phase-8 PyO3 layer can hand zero-copy numpy buffers straight through"
    - 08-RESEARCH.md §"sklearn import_model shim" (lines 483-500) and Architectural Responsibility Map (line 69: estimator→arrays is Python-side; only arrays cross)
    - 08-RESEARCH.md Pattern 1 (zero-copy borrow — the inner arrays cross as `PyReadonlyArray1/2` borrowed slices)
    - crates/treelite-py/src/lib.rs (the 08-03 #[pymodule] with the empty `sklearn` submodule to register into)
    - 08-PATTERNS.md §"src/sklearn.rs" (exact thin-wrap match)
  </read_first>
  <action>
    In `src/sklearn.rs`: one `#[pyfunction]` per `treelite_sklearn::load_*` entry point — `load_random_forest_regressor`/`_classifier`, `load_extra_trees_regressor`/`_classifier`, `load_gradient_boosting_regressor`/`_classifier`, `load_isolation_forest`, `load_hist_gradient_boosting_regressor`/`_classifier`. Each accepts the scalar header params (n_estimators, n_features, n_outputs/n_classes, etc.) plus the per-tree array-of-arrays as borrowed numpy buffers (the shape the existing loader signature expects), collects them into `&[&[..]]` zero-copy via `.as_slice()`, dispatches to the matching `treelite_sklearn::load_*`, and maps `SklError → TreeliteError`, wrapping the result `Model{inner}`. Match the existing loader signatures exactly — do NOT re-derive tree internals in Rust (that stays Python-side). Register all of them into the `sklearn` submodule in `src/lib.rs`.
  </action>
  <verify>
    <automated>cargo build -p treelite-py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' crates/treelite-py/src/sklearn.rs | grep -c 'fn load_'` ≥ 9 (one wrapper per sklearn loader)
    - `grep -q 'treelite_sklearn::load' crates/treelite-py/src/sklearn.rs`
    - `grep -q 'PyReadonlyArray' crates/treelite-py/src/sklearn.rs` (zero-copy array borrow)
    - `cargo build -p treelite-py` exits 0
  </acceptance_criteria>
  <done>The Rust side exposes a thin pyo3 wrapper per sklearn array-signature loader, registered under `_treelite_rs.sklearn`, consuming numpy array buffers zero-copy.</done>
</task>

<task type="auto">
  <name>Task 2: import_model estimator→arrays extraction shim (port importer.py) → live A/B 1e-5</name>
  <files>crates/treelite-py/python/treelite_rs/sklearn/__init__.py, crates/treelite-py/python/treelite_rs/sklearn/__init__.pyi, crates/treelite-py/python/treelite_rs/__init__.py, crates/treelite-py/tests/python/test_sklearn_ab.py</files>
  <read_first>
    - treelite-mainline/python/treelite/sklearn/importer.py (the EXACT port source: `import_model` :62, `ArrayOfArrays` :15 with `add`/`as_c_array`, the per-estimator branches — RF/ET :175-198, classifiers, GB with learning-rate leaf shrink, IsolationForest with depths + `ratio_c`, `_import_hist_gradient_boosting` :355 with packed nodes + features_map/categories_map). Port branch-for-branch; estimator stays Python (`tree_.children_left/threshold/value/...`), only numpy arrays cross.
    - 08-RESEARCH.md §"sklearn import_model shim" (lines 483-500: the `_extract_forest` ArrayOfArrays skeleton + dispatch on `sklearn_model.__class__.__name__`)
    - 08-PATTERNS.md §"python/treelite_rs/sklearn/__init__.py" (exact-port analog, lines 134-136)
    - crates/treelite-py/python/treelite_rs/sklearn/__init__.py if created as a stub by 08-01 (extend it)
    - fixtures/ sklearn goldens (sklearn_rf/gb/iforest/histgb_*.golden.json) + the capture script (fixtures/capture_sklearn.py) for the estimator construction the A/B test reproduces
  </read_first>
  <action>
    In `sklearn/__init__.py`: port upstream `importer.py`'s `import_model(sklearn_model)` — dispatch on `sklearn_model.__class__.__name__` (RandomForest/ExtraTrees Regressor/Classifier, GradientBoosting Regressor/Classifier, IsolationForest, HistGradientBoosting Regressor/Classifier), extract the per-tree arrays via the `ArrayOfArrays` pattern (children_left/right, feature, threshold, value, n_node_samples, weighted_n_node_samples, impurity; GB leaf-shrink by learning_rate; IsolationForest isolation depths + ratio_c; HistGB packed nodes + features_map/categories_map), and call the matching `_treelite_rs.sklearn.load_*` with numpy arrays. Return `Model`. Re-export `import_model` from `treelite_rs.__init__` so `treelite_rs.sklearn.import_model` resolves (D-01). Hand-write `sklearn/__init__.pyi`. Flip `test_sklearn_ab.py` GREEN: for each estimator class, fit a small estimator (mirroring fixtures/capture_sklearn.py), run `treelite_rs.sklearn.import_model(est)` and `treelite.sklearn.import_model(est)`, predict the same X, assert `np.testing.assert_allclose(..., atol=1e-5, rtol=0)` (D-11 live A/B).
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs/crates/treelite-py && uv run maturin develop 2>&1 | tail -3 && cd /home/user/Documents/workspace/treeline_rs && uv run pytest crates/treelite-py/tests/python/test_sklearn_ab.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def import_model' crates/treelite-py/python/treelite_rs/sklearn/__init__.py`
    - `grep -q '__class__.__name__\|RandomForest\|HistGradientBoosting' crates/treelite-py/python/treelite_rs/sklearn/__init__.py` (per-estimator dispatch ported)
    - `uv run pytest crates/treelite-py/tests/python/test_sklearn_ab.py -x` exits 0 (PY-04 GREEN — import_model A/B within 1e-5 across RF/ET/GB/IsolationForest/HistGB)
  </acceptance_criteria>
  <done>A Python user can `sklearn.import_model(fitted_estimator)` for the full sklearn estimator set and get a model that predicts within 1e-5 of upstream's import_model, with only numpy arrays crossing the FFI boundary (PY-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estimator arrays → loader | sklearn tree arrays (possibly ill-shaped) cross into the array-signature loaders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering / DoS | malformed/ill-shaped sklearn arrays → OOB in the loader | mitigate | `treelite_sklearn::load_*` already validate dimensions/topology → typed `SklError` (Phase 4, no transmute; HistGB packed-node bounds-checks T-04-18..21) → `TreeliteError` |
| T-08-10 | Tampering | wrong-dtype numpy array passed to an array loader | mitigate | typed `PyReadonlyArray1<i64/f64>` rejects wrong dtype before the body; estimator extraction casts to the expected dtype Python-side (port of importer.py) |
</threat_model>

<verification>
- `cargo build -p treelite-py` green; `cargo test --workspace` green.
- `uv run pytest crates/treelite-py/tests/python/test_sklearn_ab.py -x` GREEN (A/B 1e-5 across the sklearn estimator set).
</verification>

<success_criteria>
- PY-04: `sklearn.import_model(estimator)` marshals fitted RF/ET/GB/IsolationForest/HistGB estimators and predicts within 1e-5 of upstream, with only numpy arrays crossing into Rust.
</success_criteria>

<output>
Create `.planning/phases/08-pyo3-python-binding/08-04-SUMMARY.md` when done.
</output>
