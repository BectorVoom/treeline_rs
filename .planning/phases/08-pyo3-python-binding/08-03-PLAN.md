---
phase: 08-pyo3-python-binding
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - crates/treelite-py/src/model.rs
  - crates/treelite-py/src/lib.rs
  - crates/treelite-py/python/treelite_rs/__init__.py
  - crates/treelite-py/python/treelite_rs/model.pyi
  - crates/treelite-py/tests/python/test_serialize.py
autonomous: true
requirements: [PY-03]
user_setup: []

must_haves:
  truths:
    - "`Model.serialize_bytes()` → `Model.deserialize_bytes(buf)` round-trips to an equivalent model (predicts identically within 1e-5)"
    - "`Model.dump_as_json()` returns a JSON string that parses and is structurally equivalent to upstream `dump_as_json`"
    - "`Model.concatenate([m1, m2])` merges models via the builder; empty input raises TreeliteError"
  artifacts:
    - path: "crates/treelite-py/src/model.rs"
      provides: "serialize_bytes/deserialize_bytes/dump_as_json/concatenate #[pymethods]"
      contains: "serialize_bytes"
  key_links:
    - from: "crates/treelite-py/src/model.rs"
      to: "treelite_core::serialize_to_buffer / deserialize / dump_as_json_string"
      via: "Vec<u8> ↔ PyBytes binary round-trip; String dump"
      pattern: "serialize_to_buffer|deserialize|dump_as_json_string"
    - from: "crates/treelite-py/src/model.rs"
      to: "treelite_builder::concatenate"
      via: "concatenate(&[&Model]) -> Option<Model>; None → TreeliteError"
      pattern: "concatenate"
---

<objective>
Widen the `Model` pyclass with the persistence + inspection surface: `serialize_bytes`/`deserialize_bytes` (binary v5 round-trip), `serialize`/`deserialize` (file paths via the Python shim), `dump_as_json`, and `concatenate` — so a Python user can persist, reload, JSON-dump, and merge models, matching upstream's `Model` API 1:1 (D-01). This is a self-contained vertical: each method is a Python call into an already-1e-5-green core serialize seam, proven by a round-trip A/B test.

Purpose: PY-03 (serialize/deserialize/JSON-dump from Python). Uses `treelite_core::serialize_to_buffer`/`deserialize`/`dump_as_json_string` (all `&mut Model`) and `treelite_builder::concatenate` (D-01 `concatenate`).
Output: serialize/deserialize/dump/concatenate `#[pymethods]` on `Model`; the `model.pyi` stub completed; `test_serialize.py` flipped GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pyo3-python-binding/08-CONTEXT.md
@.planning/phases/08-pyo3-python-binding/08-RESEARCH.md
@.planning/phases/08-pyo3-python-binding/08-PATTERNS.md
@.planning/phases/08-pyo3-python-binding/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: serialize_bytes/deserialize_bytes + dump_as_json + concatenate #[pymethods]</name>
  <files>crates/treelite-py/src/model.rs, crates/treelite-py/src/lib.rs, crates/treelite-py/python/treelite_rs/__init__.py, crates/treelite-py/python/treelite_rs/model.pyi, crates/treelite-py/tests/python/test_serialize.py</files>
  <read_first>
    - crates/treelite-py/src/model.rs (the 08-02 Model pyclass + getters to extend with persistence methods)
    - 08-RESEARCH.md §"Code Examples" "#[pyclass] Model serialize/deserialize/dump" (lines 455-481: `serialize_bytes` → `serialize_to_buffer` → `PyBytes::new`; `deserialize_bytes` → `deserialize`; `dump_as_json` → `dump_as_json_string`) and Pattern 6 (lines 335-352: serialize_bytes/deserialize_bytes ride the BINARY serializer — they do NOT need the Frame layout; the Frame/get_field accessors are a Phase-9 refinement per A4)
    - crates/treelite-core/src/serialize/binary.rs (`serialize_to_buffer(m: &mut Model) -> Vec<u8>` :84); crates/treelite-core/src/serialize/mod.rs (`deserialize(buf: &[u8]) -> Result<Model, SerializeError>` :264); crates/treelite-core/src/serialize/json.rs (`dump_as_json_string(m: &mut Model) -> String` :95) — note all serialize fns take `&mut Model`, so the pymethods need `&mut self`
    - crates/treelite-builder/src/concat.rs (`concatenate(&[&Model]) -> Result<Option<Model>, BuilderError>` :64 — map `Ok(None)` empty-input → TreeliteError per RESEARCH Open Q1 line 534)
    - treelite-mainline/python/treelite/model.py (`serialize`/`serialize_bytes` :250, `deserialize`/`deserialize_bytes` :270/299, `dump_as_json` :171, `concatenate` :76 — the D-01 method names/signatures; `dump_as_json(pretty_print=True)` — A3: plumb a `pretty_print` param or post-format)
    - 08-PATTERNS.md §"src/model.rs"
  </read_first>
  <action>
    Extend `#[pymethods] impl Model`: `serialize_bytes(&mut self, py) -> PyResult<Bound<PyBytes>>` = `PyBytes::new(py, &treelite_core::serialize_to_buffer(&mut self.inner))` (small copy out acceptable, RESEARCH Pattern 6); `#[staticmethod] deserialize_bytes(buf: &[u8]) -> PyResult<Model>` = `treelite_core::deserialize(buf).map(|inner| Model{inner}).map_err(PyErr::from)`; `dump_as_json(&mut self, pretty_print: bool) -> PyResult<String>` = `treelite_core::serialize::json::dump_as_json_string(&mut self.inner)` (honor `pretty_print` per A3 — add a pretty toggle or re-serialize the parsed Value with indentation); `#[staticmethod] concatenate(models: Vec<PyRef<Model>>) -> PyResult<Model>` collecting `&[&Model.inner]` and calling `treelite_builder::concatenate(...)`, mapping `Ok(None)` → `TreeliteError::new_err("concatenate requires at least one model")` and `Ok(Some(m))` → `Model{inner:m}`. Add the Python-side `serialize(path)`/`deserialize(path)` file shims in `__init__.py` or `model` shim (open file → bytes → call the `_bytes` methods, mirroring upstream `model.py`). Complete `model.pyi` with the new method signatures (D-10). Flip `test_serialize.py` GREEN: serialize_bytes→deserialize_bytes round-trip predicts identically within 1e-5 to the original (and optionally A/B vs upstream's serialize); dump_as_json output parses as JSON and is structurally equivalent to upstream `dump_as_json` (parsed-value level, not byte-compare — matches Phase-2 D-04 discipline).
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs/crates/treelite-py && uv run maturin develop 2>&1 | tail -3 && cd /home/user/Documents/workspace/treeline_rs && uv run pytest crates/treelite-py/tests/python/test_serialize.py -x -q 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'serialize_bytes' crates/treelite-py/src/model.rs` and `grep -q 'deserialize_bytes' crates/treelite-py/src/model.rs` and `grep -q 'dump_as_json' crates/treelite-py/src/model.rs` and `grep -q 'concatenate' crates/treelite-py/src/model.rs`
    - `grep -q 'serialize_to_buffer' crates/treelite-py/src/model.rs` (binary path, not Frame) and `! grep -q 'Frame' crates/treelite-py/src/model.rs` (Frame/get_field deferred per A4)
    - `cargo build -p treelite-py` exits 0
    - `uv run pytest crates/treelite-py/tests/python/test_serialize.py -x` exits 0 (PY-03 GREEN — round-trip predicts within 1e-5; dump_as_json parses; concatenate merges; empty input raises TreeliteError)
  </acceptance_criteria>
  <done>A Python user can serialize a model to bytes, deserialize it back to an equivalent model (1e-5 round-trip), JSON-dump it, and concatenate models — matching upstream `Model`'s persistence API (PY-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serialized bytes → deserialize | untrusted/adversarial model bytes parsed by `treelite_core::deserialize` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering / DoS | malformed model bytes → OOB read in deserialize | mitigate | `treelite_core::deserialize` is bounds-checked → `SerializeError` → `TreeliteError` (Phase 2 02-03 "bounds-checked deserialize"); no `transmute` |
| T-08-08 | Denial of Service | empty/None concatenate input → null deref | mitigate | `Ok(None)` from `treelite_builder::concatenate` mapped to a typed `TreeliteError`, never unwrapped (RESEARCH Open Q1) |
</threat_model>

<verification>
- `cargo build -p treelite-py` green; `cargo test --workspace` green.
- `uv run pytest crates/treelite-py/tests/python/test_serialize.py -x` GREEN (round-trip 1e-5 + dump parses + concatenate).
</verification>

<success_criteria>
- PY-03: serialize_bytes/deserialize_bytes round-trip (1e-5), dump_as_json, and concatenate all callable from Python, matching upstream `Model` API.
</success_criteria>

<output>
Create `.planning/phases/08-pyo3-python-binding/08-03-SUMMARY.md` when done.
</output>
