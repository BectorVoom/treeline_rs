---
phase: 08-pyo3-python-binding
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - crates/treelite-py/src/lib.rs
  - crates/treelite-py/src/error.rs
  - crates/treelite-py/src/model.rs
  - crates/treelite-py/src/frontend.rs
  - crates/treelite-py/src/gtil.rs
  - crates/treelite-py/python/treelite_rs/__init__.py
  - crates/treelite-py/python/treelite_rs/frontend.py
  - crates/treelite-py/python/treelite_rs/frontend.pyi
  - crates/treelite-py/python/treelite_rs/model.pyi
  - crates/treelite-py/python/treelite_rs/gtil/__init__.py
  - crates/treelite-py/python/treelite_rs/gtil/__init__.pyi
  - crates/treelite-py/tests/python/test_frontend.py
  - crates/treelite-py/tests/python/test_predict_ab.py
  - crates/treelite-py/tests/python/test_zero_copy.py
autonomous: true
requirements: [PY-01, PY-02, MEM-04]
user_setup: []

must_haves:
  truths:
    - "From Python, `treelite_rs.frontend.load_xgboost_model(path)` returns a Model with the same num_tree/num_feature as upstream treelite"
    - "`treelite_rs.gtil.predict(model, X)` matches `treelite.gtil.predict(model, X)` within 1e-5 (dense, f32 and f64)"
    - "The numpy input buffer is consumed zero-copy — its data pointer is unchanged across predict, and a non-contiguous/wrong-dtype array raises TreeliteError (no silent cast)"
    - "input_type reflects the model variant (float32/float64), not the stale kInvalid staged DType"
  artifacts:
    - path: "crates/treelite-py/src/gtil.rs"
      provides: "predict_f32/predict_f64 zero-copy entry points + output_shape"
      contains: "PyReadonlyArray2"
    - path: "crates/treelite-py/src/frontend.rs"
      provides: "thin pyo3 wrappers over treelite_xgboost/lightgbm loaders"
      contains: "pyfunction"
    - path: "crates/treelite-py/src/model.rs"
      provides: "#[pyclass] Model + num_tree/num_feature/input_type/output_type getters"
      contains: "pyclass"
    - path: "crates/treelite-py/src/error.rs"
      provides: "TreeliteError + From<E> for PyErr per crate enum"
      contains: "create_exception"
    - path: "crates/treelite-py/python/treelite_rs/gtil/__init__.py"
      provides: "dtype dispatch + reshape flat→N-D shim over predict_f32/_f64"
      contains: "predict"
  key_links:
    - from: "crates/treelite-py/src/gtil.rs"
      to: "treelite_gtil::predict::<O>"
      via: "py.detach(|| predict::<O>(&model.inner, slice, num_row, &cfg))"
      pattern: "detach|predict::"
    - from: "crates/treelite-py/python/treelite_rs/gtil/__init__.py"
      to: "_treelite_rs.gtil.predict_f32/_f64"
      via: "dtype-dispatched call + reshape via output_shape"
      pattern: "predict_f32|predict_f64|reshape"
    - from: "crates/treelite-py/src/error.rs"
      to: "TreeliteError"
      via: "impl From<XgbError/LgbError/GtilError/...> for PyErr"
      pattern: "From<.*> for PyErr"
---

<objective>
The walking-skeleton capability slice: wire `Model` + the loaders + zero-copy dense predict end-to-end so a Python user can `load_xgboost_model(path)` then `predict(model, X)` and get a result that matches upstream `treelite` within 1e-5 — the headline live A/B proof (D-11). This is the thinnest complete vertical (Python call → Rust seam → numeric parity) and proves the binding faithful before serialize/sklearn/backend slices widen it.

Purpose: PY-01 (load XGB/LGB from Python), PY-02 (predict over numpy, zero-copy), MEM-04 (buffer-protocol borrow consumed zero-copy). Also lands `src/error.rs` (the single `TreeliteError` + From impls, D-06) because both loaders and predict need error translation — the panic `guard` and backend kwarg are deferred to 08-05.
Output: `src/error.rs`, `src/model.rs` (Model pyclass + getters), `src/frontend.rs`, `src/gtil.rs` (predict_f32/_f64 + output_shape), the `frontend.py` path shim + `gtil/__init__.py` dtype-dispatch+reshape shim, `.pyi` stubs; three pytest files (frontend, predict_ab, zero_copy) flipped GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pyo3-python-binding/08-CONTEXT.md
@.planning/phases/08-pyo3-python-binding/08-RESEARCH.md
@.planning/phases/08-pyo3-python-binding/08-PATTERNS.md
@.planning/phases/08-pyo3-python-binding/08-VALIDATION.md
@.planning/phases/08-pyo3-python-binding/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: error.rs (single TreeliteError + From impls) + Model pyclass + frontend loaders</name>
  <files>crates/treelite-py/src/error.rs, crates/treelite-py/src/model.rs, crates/treelite-py/src/frontend.rs, crates/treelite-py/src/lib.rs, crates/treelite-py/python/treelite_rs/__init__.py, crates/treelite-py/python/treelite_rs/frontend.py, crates/treelite-py/python/treelite_rs/frontend.pyi, crates/treelite-py/python/treelite_rs/model.pyi, crates/treelite-py/tests/python/test_frontend.py</files>
  <read_first>
    - crates/treelite-py/src/lib.rs (the 08-01 skeleton #[pymodule] to register Model + frontend functions into)
    - crates/treelite-gtil/src/error.rs (thiserror discipline — every variant carries a rich `#[error("...")]` message; D-06 preserves these as the exception text)
    - 08-RESEARCH.md §"Code Examples" "#[pyclass] Model" (lines 456-481) and Pattern 4 (lines 274-296: `create_exception!(_treelite_rs, TreeliteError, PyException)` + the `err_to_treelite!` macro generating `impl From<$t> for PyErr` per crate enum). DO NOT add the `guard()` panic helper here (08-05).
    - 08-RESEARCH.md Pitfall 2 (lines 397-401): compute `input_type`/`output_type` from `ModelVariant::F32 => "float32" / F64 => "float64"` (variant is source of truth), NOT the staged DType (reads kInvalid pre-serialization)
    - crates/treelite-core/src/model.rs (`pub enum ModelVariant` line 44, `pub num_feature: i32` line 60, `pub fn num_tree(&self)` line 186) — the fields the getters read
    - crates/treelite-xgboost/src/lib.rs (`load_xgboost_json` :285, `load_xgboost_ubjson` :308, `load_xgboost_legacy` re-export :25, `detect_xgboost_format` :24, `XgbError` :36); crates/treelite-lightgbm/src/lib.rs (`load_lightgbm` :319, `LgbError` :27)
    - treelite-mainline/python/treelite/frontend.py (`load_xgboost_model` :60, `load_xgboost_model_legacy_binary` :25, `load_lightgbm_model` :147, `_normalize_path` :17 — file I/O stays Python-side; Rust takes str/bytes) and model.py (`num_tree`/`num_feature`/`input_type`/`output_type` API shape)
    - 08-PATTERNS.md §"src/error.rs", §"src/model.rs", §"src/frontend.rs"
  </read_first>
  <action>
    In `src/error.rs`: `create_exception!(_treelite_rs, TreeliteError, PyException)`; write the `err_to_treelite!` macro producing `impl From<$t> for PyErr { TreeliteError::new_err(e.to_string()) }` for `treelite_core::CoreError`, `treelite_xgboost::XgbError`, `treelite_lightgbm::LgbError`, `treelite_sklearn::SklError`, `treelite_gtil::GtilError`, `treelite_cubecl::CubeclError`, `treelite_builder::BuilderError` (D-06 — one exception, message carries detail). In `src/model.rs`: `#[pyclass] pub struct Model { pub inner: treelite_core::Model }` with `#[pymethods]` getters `num_tree` (-> u64, from `inner.num_tree()`), `num_feature` (-> i32), `input_type`/`output_type` (-> &'static str, matched on `inner.variant` ModelVariant — NOT staged DType, Pitfall 2). In `src/frontend.rs`: thin `#[pyfunction]`s `load_xgboost_json_str(json: &str)`, `load_xgboost_ubjson_bytes(bytes: &[u8])`, `load_xgboost_legacy_bytes(bytes: &[u8])`, `detect_xgboost_format_bytes(bytes: &[u8]) -> String`, `load_lightgbm_str(s: &str)` — each `treelite_xgboost::load_*(...).map(|m| Model{inner:m}).map_err(PyErr::from)`. Register `Model`, `TreeliteError`, and the frontend functions (into the `frontend` submodule) in `src/lib.rs`. Python side: `frontend.py` ports upstream `_normalize_path` + file-read (open file, read bytes/str, sniff format via `detect_xgboost_format_bytes`) then calls the compiled loaders, exposing `load_xgboost_model(path, *, format_choice=...)`, `load_xgboost_model_legacy_binary(path)`, `load_lightgbm_model(path)` (D-01 signatures). Update `__init__.py` to re-export `Model`, `TreeliteError`, `frontend`. Hand-write `frontend.pyi` + `model.pyi` stubs (D-10). Flip `test_frontend.py` GREEN (load each fixture, assert `num_tree`/`num_feature` match upstream).
  </action>
  <verify>
    <automated>cargo build -p treelite-py 2>&1 | tail -3 && cd /home/user/Documents/workspace/treeline_rs/crates/treelite-py && uv run maturin develop 2>&1 | tail -3 && cd /home/user/Documents/workspace/treeline_rs && uv run pytest crates/treelite-py/tests/python/test_frontend.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'create_exception' crates/treelite-py/src/error.rs` and `grep -c 'From<.*> for PyErr' crates/treelite-py/src/error.rs` ≥ 5 (one per crate enum, comments excluded: `grep -v '^//' | grep -c`)
    - `grep -q 'pyclass' crates/treelite-py/src/model.rs` and `grep -q 'ModelVariant::F32 =>' crates/treelite-py/src/model.rs` (variant-derived input_type, Pitfall 2)
    - `grep -q 'load_xgboost_json_str\|load_lightgbm_str' crates/treelite-py/src/frontend.rs`
    - `cargo build -p treelite-py` exits 0
    - `uv run pytest crates/treelite-py/tests/python/test_frontend.py -x` exits 0 (PY-01 GREEN — loads XGB JSON/UBJSON/legacy + LightGBM, num_tree/num_feature == upstream)
  </acceptance_criteria>
  <done>A Python user can load XGBoost (JSON/UBJSON/legacy) and LightGBM models; `num_tree`/`num_feature`/`input_type` match upstream; every Rust error surfaces as one `TreeliteError` (PY-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zero-copy dense predict (predict_f32/_f64 + GIL release) + reshape shim → live A/B 1e-5</name>
  <files>crates/treelite-py/src/gtil.rs, crates/treelite-py/src/lib.rs, crates/treelite-py/python/treelite_rs/gtil/__init__.py, crates/treelite-py/python/treelite_rs/gtil/__init__.pyi, crates/treelite-py/tests/python/test_predict_ab.py, crates/treelite-py/tests/python/test_zero_copy.py</files>
  <behavior>
    - test_predict_ab (PY-02/D-11): for each fixture model (XGB json/ubjson/legacy, LightGBM numerical/categorical), `np.testing.assert_allclose(treelite.gtil.predict(m_up, X), treelite_rs.gtil.predict(m_rs, X), atol=1e-5, rtol=0)` passes for f32 and f64 inputs, and the returned shape equals upstream's `(num_row, num_target, max_num_class)`.
    - test_zero_copy (MEM-04/D-03): the input array's `__array_interface__['data'][0]` is identical before and after predict (no copy); a non-contiguous slice `X[:, ::2]` raises `TreeliteError`; an f64 array against an f32 model raises (strict, no auto-cast).
  </behavior>
  <read_first>
    - 08-RESEARCH.md Pattern 1 (lines 216-250: `PyReadonlyArray2<'py, O>` typed param → `.as_slice()` zero-copy with `AsSliceError → TreeliteError("...call np.ascontiguousarray...")`; the recommendation to use TWO typed entry points predict_f32/predict_f64 — no f32→f64 pre-cast), Pattern 2 (lines 252-261: `out.into_pyarray(py)` moves the Vec — `to_pyarray` COPIES, forbidden), Pattern 3 (lines 263-272: `py.detach(|| ...)` GIL release, closure is Send + touches no Py objects), Pitfall 1 (`detach` renamed from `allow_threads`), Pitfall 3 (`into_pyarray` returns 1-D; reshape to N-D in the `.py` shim), Pitfall 4 (typed param rejects wrong dtype; no auto-cast — D-03)
    - crates/treelite-harness/src/lib.rs:99-128 (the four-fn-pointer dtype seam + the load-bearing comment lines 120-128: "an f32-input fixture MUST flow through the f32 entry point with NO f32→f64 pre-cast") — mirror as two monomorphized entry points
    - crates/treelite-gtil/src/lib.rs (`predict::<O>` :892 — signature `(model, data: &[O], num_row, config) -> Result<Vec<O>, GtilError>`; `Config`/`PredictKind` :16; `output_shape`/`Shape` :18; the flat `(num_row*num_target*max_num_class)` output doc); crates/treelite-gtil/src/config.rs (`Config` fields incl. `nthread` recorded-not-used :39-43)
    - crates/treelite-core/tests/serialize_pybuffer.rs:80-90 (the `.as_ptr()` equality proof template the zero-copy assertion mirrors)
    - treelite-mainline/python/treelite/gtil/gtil.py (`predict` :54, `predict_leaf` :87, `predict_per_tree` :123 — the D-01 signatures the shim mirrors; `backend=` is additive, added in 08-05) and gtil/__init__.py
    - 08-PATTERNS.md §"src/gtil.rs" and §"python/treelite_rs/gtil/__init__.py"
  </read_first>
  <action>
    In `src/gtil.rs`: two `#[pyfunction]`s `predict_f32(py, model: &Model, data: PyReadonlyArray2<'py, f32>, *, nthread=-1, pred_margin=false)` and `predict_f64(... f64 ...)`. Each: `data.as_slice()` (zero-copy, MEM-04) mapping `AsSliceError → TreeliteError::new_err("input array must be C-contiguous; call np.ascontiguousarray(arr) first")` (D-03); build `Config` from `nthread`/`pred_margin` (kind = Raw if pred_margin else Default); release the GIL with `py.detach(|| treelite_gtil::predict::<f32/f64>(&model.inner, slice, num_row, &cfg))`; map `GtilError → TreeliteError`; return `out.into_pyarray(py)` (1-D flat, zero-copy move). Add a `#[pyfunction] output_shape(model, kind) -> Vec<usize>` wrapping `treelite_gtil::output_shape`/`Shape` so the Python shim can reshape. Register both predict fns + output_shape into the `gtil` submodule in `src/lib.rs`. Python `gtil/__init__.py`: `predict(model, data, *, pred_margin=False, nthread=-1)` reads `model.input_type` to dispatch to `predict_f32`/`predict_f64` (Pitfall 2 — variant-derived), then reshapes the flat output to `(num_row, num_target, max_num_class)` via `output_shape` (a view, no copy — Pitfall 3). Also stub `predict_leaf`/`predict_per_tree` calling the same entry points with the appropriate kind/shape (full kinds parity is upstream-signature 1:1, D-01). Hand-write `gtil/__init__.pyi`. Flip `test_predict_ab.py` (A/B 1e-5 across fixtures, f32+f64, shape match) and `test_zero_copy.py` (data-pointer identity + non-contiguous/wrong-dtype raises) GREEN.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs/crates/treelite-py && uv run maturin develop 2>&1 | tail -3 && cd /home/user/Documents/workspace/treeline_rs && uv run pytest crates/treelite-py/tests/python/test_predict_ab.py crates/treelite-py/tests/python/test_zero_copy.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'PyReadonlyArray2' crates/treelite-py/src/gtil.rs` and `grep -q 'into_pyarray' crates/treelite-py/src/gtil.rs` (zero-copy return) and `grep -q 'detach' crates/treelite-py/src/gtil.rs` (GIL release)
    - `grep -q 'predict_f32' crates/treelite-py/src/gtil.rs` and `grep -q 'predict_f64' crates/treelite-py/src/gtil.rs` (two monomorphized entry points, no pre-cast)
    - `! grep -q 'to_pyarray' crates/treelite-py/src/gtil.rs` (no copying return)
    - `grep -q 'reshape' crates/treelite-py/python/treelite_rs/gtil/__init__.py`
    - `uv run pytest crates/treelite-py/tests/python/test_predict_ab.py -x` exits 0 (PY-02 / D-11 live A/B within 1e-5, f32 + f64, shape == upstream)
    - `uv run pytest crates/treelite-py/tests/python/test_zero_copy.py -x` exits 0 (MEM-04 no-copy + D-03 strict-or-error)
  </acceptance_criteria>
  <done>`treelite_rs.gtil.predict(model, X)` matches upstream within 1e-5 for f32 and f64 dense inputs across all loader fixtures, consuming the numpy buffer zero-copy and rejecting non-contiguous/wrong-dtype input with TreeliteError (PY-02, MEM-04). The binding is proven faithful end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → predict | untrusted numpy buffer (dtype/contiguity/shape) crosses into a Rust slice |
| model file bytes → loader | untrusted/adversarial model bytes parsed by treelite_xgboost/lightgbm |
| GIL-released region | the borrowed numpy buffer must stay valid while the GIL is detached |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | wrong-dtype / non-contiguous numpy → OOB slice | mitigate | typed `PyReadonlyArray2<T>` rejects wrong dtype before the body; `as_slice()` contiguity check → `TreeliteError` (D-03), never coerce (RESEARCH Pitfall 4) |
| T-08-04 | Denial of Service | mismatched num_feature vs data width → OOB row slice | mitigate | `treelite_gtil::predict::<O>` validates `data.len() >= num_row*num_feature` and rejects `num_feature < 0` → `InvalidInputShape` → `TreeliteError` (gtil lib.rs guard, verified this session) |
| T-08-05 | Tampering / DoS | malformed/adversarial model bytes → loader UB | mitigate | loaders return typed `XgbError`/`LgbError` (Phase 3/4, no transmute) → `TreeliteError`; no panic (D-06/D-07) |
| T-08-06 | Information Disclosure | borrowed numpy buffer use-after-free while GIL detached | mitigate | the `PyReadonlyArray` borrow guard is created before `py.detach` and lives across it; the closure is `Send`/`Ungil` and touches no Py objects (RESEARCH Pattern 3 soundness note) |
</threat_model>

<verification>
- `cargo build -p treelite-py` green; `cargo test --workspace` green (no regression).
- `uv run pytest crates/treelite-py/tests/python/test_frontend.py test_predict_ab.py test_zero_copy.py -x` all GREEN.
- Live A/B (D-11) within 1e-5 across XGB/LightGBM fixtures, both presets.
</verification>

<success_criteria>
- PY-01: load XGBoost (JSON/UBJSON/legacy) + LightGBM from Python; num_tree/num_feature == upstream.
- PY-02: dense predict over numpy matches upstream within 1e-5 (f32 + f64), zero-copy input, zero-copy 1-D return reshaped to upstream N-D.
- MEM-04: numpy buffer consumed zero-copy (data-pointer identity proven); strict-or-error on bad dtype/contiguity (D-03).
- Single `TreeliteError` (D-06) for every Rust error path.
</success_criteria>

<output>
Create `.planning/phases/08-pyo3-python-binding/08-02-SUMMARY.md` when done.
</output>
