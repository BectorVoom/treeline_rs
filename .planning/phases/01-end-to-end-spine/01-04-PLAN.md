---
phase: 01-end-to-end-spine
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - crates/treelite-harness/Cargo.toml
  - crates/treelite-harness/src/lib.rs
  - crates/treelite-harness/tests/run_equivalence.rs
  - crates/treelite-harness/tests/equivalence.rs
autonomous: true
requirements: [ERR-02]
must_haves:
  truths:
    - "The harness loads fixtures/golden.json {input, output, manifest}, runs load_xgboost_json then predict, and asserts every element within 1e-5 of the golden output (the spine test)"
    - "The harness reports the max observed |delta| as an f64 return from run_equivalence (satisfies Success Criterion 4 spine); EQV-04's full seeded harness across all model types is Phase 5 scope"
    - "The harness uses anyhow for error context throughout (ERR-02)"
    - "The harness warns (not fails) when the running glibc/OS differs from the manifest capture environment"
  artifacts:
    - path: "crates/treelite-harness/src/lib.rs"
      provides: "load golden, run pipeline, assert 1e-5, report max deviation, manifest check (anyhow)"
      min_lines: 50
    - path: "crates/treelite-harness/tests/run_equivalence.rs"
      provides: "unit test of run_equivalence against a hand-computed scalar tree (no golden.json dependency)"
      min_lines: 20
    - path: "crates/treelite-harness/tests/equivalence.rs"
      provides: "the end-to-end load->predict->1e-5 spine test against the committed golden"
      min_lines: 20
  key_links:
    - from: "crates/treelite-harness/tests/equivalence.rs"
      to: "treelite_xgboost::load_xgboost_json + treelite_gtil::predict"
      via: "runs the full pipeline against the fixture"
      pattern: "load_xgboost_json|predict"
    - from: "crates/treelite-harness/src/lib.rs"
      to: "fixtures/golden.json"
      via: "reads the frozen golden via serde_json"
      pattern: "golden"
---

## Phase Goal

**As a** treelite-rs consumer (and the project itself), **I want to** assert that the full load->predict pipeline matches the frozen upstream golden within 1e-5, **so that** the core fidelity value is proven end-to-end — the entire walking skeleton is closed and runnable.

<objective>
Implement `treelite-harness` — the 1e-5 equivalence instrument that loads the committed `golden.json`, runs `treelite_xgboost::load_xgboost_json` then `treelite_gtil::predict` over the committed input, and asserts every output element is within 1e-5 of the golden, reporting the max observed deviation. This closes the walking skeleton: load -> represent -> predict -> verify-against-golden.

Purpose: Wave 3 final vertical-slice step — the spine test. After this, `cargo test --workspace` proves the core value (Success Criterion 4). Implements ERR-02 (anyhow context in the dev/test crate), the EQV-02 subset (harness + one golden), and a max-observed-|delta| report that the spine test surfaces (the full seeded EQV-04 harness across all model types is Phase 5 scope). The manifest check (D-07) makes a future libm-divergence failure diagnosable.

Output: A working `treelite-harness` crate, a unit test of `run_equivalence` against a hand-computed scalar model, and the green end-to-end equivalence test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-end-to-end-spine/01-RESEARCH.md
@.planning/phases/01-end-to-end-spine/01-PATTERNS.md
@.planning/phases/01-end-to-end-spine/01-01-SUMMARY.md
@.planning/phases/01-end-to-end-spine/01-02-SUMMARY.md
@.planning/phases/01-end-to-end-spine/01-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols/files created in this plan (Wave 3):

- **Crate `treelite-harness`:**
  - `pub struct Golden { input: Vec<Vec<f32>>, output: Vec<f32>, manifest: Manifest }` (serde) + `Manifest` struct.
  - `pub fn load_golden(path: &str) -> anyhow::Result<Golden>`.
  - `pub fn run_equivalence(model_json_path: &str, golden: &Golden) -> anyhow::Result<f64>` — runs the pipeline, asserts 1e-5, returns the max observed |delta|.
  - `fn check_manifest(manifest: &Manifest)` — warns (does not fail) on environment drift.
  - `tests/run_equivalence.rs`: a unit test of `run_equivalence` against a hand-computed scalar tree model (no golden.json dependency).
  - `tests/equivalence.rs`: the end-to-end spine test against the committed golden (anyhow).
- Depends on `treelite-core`, `treelite-xgboost`, `treelite-gtil` (all prior waves) + workspace `anyhow`, `approx`, `serde`, `serde_json`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the harness library and unit-test run_equivalence against a hand-computed scalar model</name>
  <files>crates/treelite-harness/Cargo.toml, crates/treelite-harness/src/lib.rs, crates/treelite-harness/tests/run_equivalence.rs</files>
  <read_first>
    - .planning/phases/01-end-to-end-spine/01-RESEARCH.md (§Validation Architecture — Equivalence-Harness Design lines 536-541; §Code Examples — golden artifact shape lines 481-493; §Common Pitfalls Pitfall 4 — libm divergence + manifest rationale)
    - .planning/phases/01-end-to-end-spine/01-PATTERNS.md (treelite-harness section — role + 1e-5 instrument design; Shared Patterns — Error context (anyhow) in harness ONLY)
    - fixtures/golden.json (the frozen artifact this harness reads — produced by Plan 01-01)
    - crates/treelite-xgboost/src/lib.rs, crates/treelite-gtil/src/lib.rs (the pipeline entry points from Plans 01-02, 01-03)
  </read_first>
  <action>
    Create `crates/treelite-harness/Cargo.toml` inheriting `edition.workspace = true`, depending on `treelite-core`, `treelite-xgboost`, `treelite-gtil` (paths) plus workspace `anyhow`, `approx`, `serde` (derive), `serde_json`. This is a dev/test-facing crate — use `anyhow` for ALL error context (ERR-02); NEVER expose `anyhow` from a library public API (the three library crates already use `thiserror`). In `lib.rs` define serde structs `Golden { input: Vec<Vec<f32>>, output: Vec<f32>, manifest: Manifest }` and `Manifest { treelite: String, xgboost: Option<String>, os: String, arch: String, libc: serde_json::Value, python: Option<String> }` (match the keys the `capture_golden.py` script writes; use `serde_json::Value` for `libc` since `platform.libc_ver()` is a tuple). `pub fn load_golden(path: &str) -> anyhow::Result<Golden>` reads + parses with `.context("reading golden.json")`. `pub fn run_equivalence(model_json_path: &str, golden: &Golden) -> anyhow::Result<f64>`: read the model JSON with context, call `treelite_xgboost::load_xgboost_json(&json).context("loading fixture model")?`, flatten `golden.input` into a row-major `Vec<f32>` with `num_row = golden.input.len()`, call `treelite_gtil::predict(&model, &flat, num_row).context("predicting")?`, then for each `i` compute `delta = (rust[i] - golden.output[i]).abs() as f64`, track the max, and `approx::assert_abs_diff_eq!(rust[i], golden.output[i], epsilon = 1e-5)`. Return the max observed |delta|. `fn check_manifest(manifest: &Manifest)` compares the running `std::env::consts::OS`/`ARCH` (and, where readable, glibc) against the manifest and emits an `eprintln!` WARNING on mismatch — it must NOT fail (a libm-divergence failure should be diagnosable, not silently passed/failed by the manifest itself).

    Then write `crates/treelite-harness/tests/run_equivalence.rs` that exercises `run_equivalence` WITHOUT depending on `fixtures/golden.json`: hand-author a tiny self-contained XGBoost-JSON model string (or write it to a temp file via `std::env::temp_dir()`) — a single-tree `binary:logistic` model with one numerical root split and two leaves, `base_score = 0.5` (so the sigmoid margin transform is exactly 0.0, keeping the expected value hand-computable) — and construct a `Golden` in-memory whose `output` is the value computed BY HAND for that model: for an input row that routes to a known leaf with leaf_value `L`, the expected output is `sigmoid(1.0, L + 0.0) = 1/(1+exp(-L))` computed in f32. Assert (a) `run_equivalence` returns `Ok(max_dev)` with `max_dev < 1e-5` when the in-memory golden matches the hand-computed expectation, and (b) `run_equivalence` returns `Err` (the `assert_abs_diff_eq!` fails / surfaces) when the golden output is deliberately perturbed beyond 1e-5 — proving a numerically incorrect pipeline is actually CAUGHT by this function, not merely build-checked. This test is the safety net for the core-value assertion (it catches a wrong impl in Task 1 itself rather than deferring to Task 2's golden.json).
  </action>
  <behavior>
    - `run_equivalence` over a hand-authored single-tree `binary:logistic` model (`base_score = 0.5` so margin transform == 0.0) with a row routing to a leaf of value `L` returns `Ok(max_dev)` where `max_dev < 1e-5` against an in-memory golden whose output is the hand-computed `1.0_f32 / (1.0_f32 + (-L).exp())`.
    - When the in-memory golden output is perturbed by `> 1e-5`, `run_equivalence` returns `Err` (the 1e-5 assertion fires) — a numerically wrong pipeline is caught here, not silently passed.
    - `load_golden` on a malformed/missing path returns `Err` with an anyhow context chain (no raw panic).
  </behavior>
  <verify>
    <automated>cargo test -p treelite-harness --test run_equivalence</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness --test run_equivalence` passes: it asserts `run_equivalence` returns the correct `max_dev < 1e-5` against a hand-computed scalar model AND returns `Err` when the golden is perturbed beyond 1e-5 (the function's 1e-5 assertion is exercised, not just compiled).
    - `run_equivalence` returns the max observed |delta| (an `f64`), not just unit/bool.
    - `treelite-harness/Cargo.toml` depends on `anyhow` (ERR-02) and on all three library crates; it does NOT re-export `anyhow` types from a public library signature meant for downstream non-test use (the public fns return `anyhow::Result`, which is acceptable for a dev/test crate).
  </acceptance_criteria>
  <done>The harness library loads the golden, runs the pipeline, asserts 1e-5, reports max deviation, and warns on manifest drift — and a unit test against a hand-computed scalar model proves run_equivalence actually catches a >1e-5 deviation (not build-only).</done>
</task>

<task type="auto">
  <name>Task 2: The end-to-end equivalence spine test (load -> predict -> assert within 1e-5 of golden)</name>
  <files>crates/treelite-harness/tests/equivalence.rs</files>
  <read_first>
    - .planning/phases/01-end-to-end-spine/01-RESEARCH.md (§Validation Architecture — Phase Requirements → Test Map line 534 the spine test; Equivalence-Harness Design)
    - .planning/phases/01-end-to-end-spine/01-VALIDATION.md (Sampling Rate — phase gate is the equivalence test green within 1e-5)
    - crates/treelite-harness/src/lib.rs (the functions under test — produced in Task 1)
  </read_first>
  <action>
    In `crates/treelite-harness/tests/equivalence.rs` write `#[test] fn equivalence_within_1e5() -> anyhow::Result<()>`: resolve the fixture paths relative to the workspace root (use `env!("CARGO_MANIFEST_DIR")` joined with `../../fixtures/binary_logistic.model.json` and `../../fixtures/golden.json`), call `treelite_harness::load_golden(golden_path)?`, run `treelite_harness::check_manifest(&golden.manifest)`, then `let max_dev = treelite_harness::run_equivalence(model_path, &golden)?;`, and `println!("max observed |delta| = {max_dev:e}")`. The `assert_abs_diff_eq!` inside `run_equivalence` is the hard 1e-5 gate; the test additionally `assert!(max_dev < 1e-5)` to make the gate explicit in the test body. Use `anyhow::Result` as the test return type (ERR-02 — `?` propagation with context). This is THE spine test (Success Criterion 4): it proves load -> represent -> predict -> verify-against-golden end-to-end within 1e-5.
  </action>
  <verify>
    <automated>cargo test -p treelite-harness --test equivalence -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness --test equivalence` passes: loading the committed fixture, predicting, and asserting every output element is within 1e-5 of `golden.json` (EQV-02 subset, Success Criterion 4).
    - The test prints the max observed |delta| (`--nocapture` shows the `max observed |delta| = ...` line).
    - `cargo build --workspace && cargo test --workspace` is fully green (phase gate, Success Criterion 1).
  </acceptance_criteria>
  <done>The end-to-end equivalence test passes within 1e-5 against the committed golden, the max deviation is reported, and the full workspace test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden.json file → harness | Trusted committed fixture, but malformed JSON should surface as anyhow context, not a raw panic |
| running environment → manifest check | Environment drift (different glibc/OS than capture) is a diagnosability concern, not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information/DoS | malformed or missing golden.json | mitigate | `load_golden` uses `.context(...)` so a parse/read failure surfaces a clear anyhow error chain rather than an opaque panic (ERR-02) |
| T-04-02 | Repudiation/Correctness | undiagnosed libm/environment divergence | accept | `check_manifest` warns (does not fail) on OS/arch/glibc drift; D-07 manifest makes a 1e-5 failure on a different distro immediately diagnosable |
| T-04-SC | Tampering | crates.io installs (anyhow, approx, serde, serde_json) | accept | Pinned exact versions from RESEARCH §Package Legitimacy Audit (slopcheck [OK]); `approx` pinned `0.5.1` not the `0.6.0-rc2` pre-release (FND-02) |
</threat_model>

<verification>
- `cargo test -p treelite-harness --test run_equivalence` passes — run_equivalence is proven to catch a >1e-5 deviation against a hand-computed scalar model (not build-only).
- `cargo test -p treelite-harness --test equivalence` passes within 1e-5 against the committed golden.
- The max observed deviation is printed.
- The manifest check warns (not fails) on environment drift.
- `cargo build --workspace && cargo test --workspace` is fully green (phase gate).
</verification>

<success_criteria>
- The walking skeleton is closed: a model loads, predicts, and verifies within 1e-5 of the frozen upstream golden.
- `run_equivalence` is unit-tested against a hand-computed scalar model and proven to catch a >1e-5 deviation.
- The harness reports max deviation and uses anyhow throughout.
- The whole workspace builds and tests green.
</success_criteria>

<output>
Create `.planning/phases/01-end-to-end-spine/01-04-SUMMARY.md` when done.
</output>
