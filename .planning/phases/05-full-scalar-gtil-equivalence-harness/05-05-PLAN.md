---
phase: 05-full-scalar-gtil-equivalence-harness
plan: 05
type: execute
wave: 5
depends_on: ["05-01", "05-04"]
files_modified:
  - crates/treelite-harness/src/lib.rs
  - crates/treelite-harness/src/manifest.rs
  - crates/treelite-harness/tests/gtil_matrix.rs
must_haves:
  truths:
    - "The harness iterates the exhaustive matrix (model × preset × input-dtype × kind × {dense,sparse} × seed) over the committed fixtures/gtil/ goldens"
    - "Each cell asserts Rust prediction within 1e-5 of the upstream golden and reports max observed |delta| (EQV-03, EQV-04)"
    - "Each {dense,sparse} pair asserts Rust-dense == Rust-sparse on identical logical data (D-04)"
    - "The Manifest carries a backend field (scalar-cpu this phase) and check_manifest warns on drift"
    - "The runner is backend-parameterized so Phase 6 registers a runtime without a harness refactor (D-11)"
  artifacts:
    - path: "crates/treelite-harness/src/manifest.rs"
      provides: "Manifest extended with backend + provenance fields (D-09)"
      contains: "backend"
    - path: "crates/treelite-harness/src/lib.rs"
      provides: "Backend enum + PredictFn registry seam (D-11) + matrix runner helpers"
      contains: "Backend"
    - path: "crates/treelite-harness/tests/gtil_matrix.rs"
      provides: "GREEN exhaustive-matrix test with dense==sparse parity + max-dev report"
      contains: "assert_abs_diff_eq"
  key_links:
    - from: "crates/treelite-harness/tests/gtil_matrix.rs"
      to: "treelite_gtil::predict / predict_sparse"
      via: "matrix runner predict + parity assert"
      pattern: "predict_sparse|predict"
    - from: "crates/treelite-harness/src/lib.rs"
      to: "crates/treelite-harness/src/manifest.rs"
      via: "Manifest backend field consumed by check_manifest"
      pattern: "backend"
autonomous: true
requirements: [EQV-03, EQV-04]
---

<objective>
Build the exhaustive equivalence-matrix runner that turns the Plan-01 RED `gtil_matrix.rs` GREEN: iterate the committed `fixtures/gtil/` cross-product, assert each Rust prediction within 1e-5 of its upstream golden and report max observed `|delta|` (EQV-03, EQV-04), assert Rust-dense == Rust-sparse parity per pair (D-04), extend the `Manifest` with a `backend` field + provenance (D-09), and add the minimal backend-parameterized seam (D-11) so Phase 6 plugs a cubecl `R: Runtime` in by registration, not refactor.

Purpose: This is the 1e-5 measurement instrument every later phase trusts. It generalizes the existing per-loader golden-assert pattern into one uniform, backend-ready matrix harness. NO cubecl code this phase — only the seam design (one `Backend::ScalarCpu` variant + a predict-fn registry).
Output: extended `Manifest` (`manifest.rs`), `Backend`/`PredictFn` seam + runner helpers (`lib.rs`), GREEN `gtil_matrix.rs`.
</objective>

## Phase Goal

**As a** treelite-rs maintainer validating the inference engine, **I want to** assert Rust GTIL predictions within 1e-5 of frozen upstream-Treelite goldens across the full model × preset × input-dtype × kind × {dense,sparse} × seed matrix, **so that** every later compute backend has a trusted 1e-5 measurement instrument.

## Artifacts this phase produces

See `05-01-PLAN.md` "Artifacts this phase produces". This plan creates: the `Backend` enum + `PredictFn` registry, the `Manifest` `backend` field + provenance extension, and the GREEN exhaustive-matrix runner.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-CONTEXT.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md
@crates/treelite-harness/src/lib.rs
@crates/treelite-harness/tests/lightgbm.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Manifest with backend + provenance; add the backend-parameterized seam (D-09, D-11)</name>
  <files>crates/treelite-harness/src/manifest.rs, crates/treelite-harness/src/lib.rs</files>
  <read_first>
    - crates/treelite-harness/src/lib.rs (the existing `Manifest` struct at :92-108 with its `#[serde(default)]`-tolerant optional fields to MIRROR; `check_manifest` OS/arch drift warning at :242-263; the `run_equivalence` max-`|delta|` loop at :220-232 with the hard `1e-5` gate — the generalization target)
    - treelite-mainline/include/treelite/gtil.h (PredictKind enum — the seam's Backend enum is analogous: a small tag enum, NOT a trait-object hierarchy)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md (Pattern 4 "Backend-parameterized harness seam" — the MINIMAL seam: `enum Backend { ScalarCpu }` + `type PredictFn = fn(...) -> anyhow::Result<Vec<f64>>` + a `RunnerCase`; "Avoid over-engineering into a full trait object hierarchy"; D-09 manifest fields)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md (`crates/treelite-harness/src/lib.rs` section — extend Manifest in the `#[serde(default)]` style; the Backend/PredictFn seam shape; "Phase 6 registers (CubeclCpu, cubecl_predict) without touching the matrix iteration")
  </read_first>
  <action>
    Create `crates/treelite-harness/src/manifest.rs` (or extend the existing `Manifest` in `lib.rs` and re-export) adding to the `Manifest` struct, in the existing `#[serde(default)]`-tolerant style: `pub backend: String` (value `"scalar-cpu"` this phase, D-09), plus optional provenance keys `rustc: Option<String>`, `cubecl: Option<String>` (placeholder `"n/a"`), `seed: Option<u64>`, `sha256: Option<String>`, and per-framework version fields (`numpy`, `lightgbm`, `scikit_learn` etc.) as `#[serde(default)] Option<String>` so older manifests still parse. Keep the existing `treelite`/`xgboost`/`os`/`arch`/`libc`/`python` fields. Extend `check_manifest` to additionally `eprintln!` a (non-failing) warning when `manifest.backend != "scalar-cpu"` while running the scalar reference, and when `rustc`/`os`/`arch` drift — never fail (D-07/D-09 are diagnosability, not gates). Add the minimal backend seam to `lib.rs` (RESEARCH Pattern 4, NOT a trait object): `pub enum Backend { ScalarCpu }` (with a doc-comment listing the Phase-6/7 variants CubeclCpu/Cuda/Wgpu/Rocm as future), `pub type DensePredictFn = fn(&Model, &[f64], usize, &Config) -> anyhow::Result<Vec<f64>>` and a sparse counterpart, and a `pub struct RunnerCase { pub backend: Backend, pub dense: DensePredictFn, pub sparse: SparsePredictFn }`. Provide a `scalar_cpu_case()` constructor wiring `treelite_gtil::predict::<f64>` / `predict_sparse::<f64>` (bridging the typed `GtilError` → `anyhow` via `.map_err(|e| anyhow::anyhow!("{e}"))`). Document that Phase 6 registers `(CubeclCpu, cubecl_predict)` by adding a variant + a `RunnerCase` — no matrix-iteration change.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && cargo build -p treelite-harness 2>&1 | tail -5 && cargo test -p treelite-harness --no-run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The `Manifest` struct has a `backend` field (`grep -q "backend" crates/treelite-harness/src/manifest.rs crates/treelite-harness/src/lib.rs`).
    - New provenance fields use `#[serde(default)]` so existing fixtures (`golden_v5.manifest.json`, `xgb_3format.manifest.json`) still parse — `cargo test --workspace` keeps the existing manifest-consuming tests green.
    - `crates/treelite-harness/src/lib.rs` defines `pub enum Backend` with a `ScalarCpu` variant and a `PredictFn`/`RunnerCase` registry type (grep `Backend` + `RunnerCase`).
    - The seam is NOT a trait-object hierarchy: `grep -L "dyn " crates/treelite-harness/src/lib.rs` (no `dyn` backend trait) — a `fn` pointer registry per RESEARCH Pattern 4.
    - `scalar_cpu_case()` wires `treelite_gtil::predict` and `predict_sparse` (grep both).
    - `cargo build -p treelite-harness` and `cargo test -p treelite-harness --no-run` exit 0.
  </acceptance_criteria>
  <done>The `Manifest` carries a `backend` field + provenance (parsing old fixtures unchanged), `check_manifest` warns on backend/toolchain drift, and a minimal fn-pointer `Backend`/`RunnerCase` seam (scalar-cpu only) is in place for Phase-6 registration.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN exhaustive-matrix runner + dense==sparse parity + max-dev report (EQV-03, EQV-04, D-04)</name>
  <files>crates/treelite-harness/tests/gtil_matrix.rs</files>
  <read_first>
    - crates/treelite-harness/tests/gtil_matrix.rs (the Plan-01 RED scaffold to complete: the `#[ignore]` gate to remove, the fixture-dir iteration, the local-serde golden struct)
    - crates/treelite-harness/tests/lightgbm.rs (the full golden-assert skeleton: resolvers, `check_manifest` call, flatten → predict → max-`|delta|` loop with `approx::assert_abs_diff_eq!(epsilon = 1e-5)`, `eprintln!("... max |delta| = {max_dev:e}")`, the `.map_err(|e| anyhow::anyhow!("{e}")).context(...)` bridge)
    - crates/treelite-harness/src/lib.rs (the extended `Manifest` + `Backend`/`RunnerCase` seam from Task 1; the `NanF32` deserializer for the golden's NaN cells; `run_equivalence` max-dev pattern)
    - crates/treelite-gtil/src/lib.rs (the now-complete `predict`/`predict_sparse` + `Config`/`PredictKind` + `output_shape` — the runner calls these per cell)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md (Architecture diagram — the 5-step matrix loop: load matrix → run predict/predict_sparse → assert |Rust-golden|<1e-5 → assert dense==sparse → record max |delta|; D-01/D-03 exhaustive, no pruning)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md (`tests/gtil_matrix.rs` section — NEW matrix-specific additions: iterate the cross-product, dense==sparse parity assertion, both input dtypes)
  </read_first>
  <action>
    Complete `crates/treelite-harness/tests/gtil_matrix.rs`: remove the `#[ignore]` gate. Discover every `fixtures/gtil/*.golden.json` via `read_dir`, parse the filename to recover `(model, preset, input_dtype, kind, dense|sparse, seed)` (D-01/D-03 — iterate the full committed set, no invariance pruning). For each cell: load the model (via the matching loader), call `check_manifest(&golden.manifest)`, flatten the committed input matrix, select the right entry point from the `RunnerCase` (`predict` for dense, `predict_sparse` for sparse) with a `Config` whose `kind` matches the fixture, dispatch the f32-vs-f64 input path per the fixture `input_dtype`, then run the max-`|delta|` loop asserting `approx::assert_abs_diff_eq!(got, golden.output[i], epsilon = 1e-5)` (the HARD gate, never loosened) and track + `eprintln!("{cell} max |delta| = {max_dev:e}")` (EQV-04). Add the dense==sparse PARITY assertion (D-04): for each (dense, sparse) fixture pair on identical logical data, run BOTH Rust paths and assert equality (within a tight tolerance or bitwise) independent of the golden assert. Bridge typed `GtilError` → `anyhow` with `.map_err(|e| anyhow::anyhow!("{e}")).context(...)`. Ensure the test covers both presets, both input dtypes, all 4 kinds, and both {dense, sparse} (the committed set from Plan 01 guarantees coverage).
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && cargo test -p treelite-harness gtil_matrix -- --nocapture 2>&1 | tail -15 && cargo test --workspace 2>&1 | grep -E "test result"</automated>
  </verify>
  <acceptance_criteria>
    - `crates/treelite-harness/tests/gtil_matrix.rs` no longer contains an `#[ignore]` attribute on the matrix test (`grep -L "#\[ignore" ...` for the matrix test fn, or the count of ignore attrs drops to 0 for that test).
    - The test contains `approx::assert_abs_diff_eq!` with `epsilon = 1e-5` (the hard gate) AND an `eprintln!`/`println!` of `max |delta|` (EQV-04).
    - The test contains an explicit dense==sparse parity assertion distinct from the golden assert (D-04) — grep for a comparison between a `predict(...)` result and a `predict_sparse(...)` result.
    - The runner iterates the committed fixtures (`read_dir` over `fixtures/gtil`) and exercises both `predict` and `predict_sparse` and both input dtypes (f32 + f64) — confirmed by the cells run (visible in `--nocapture` output covering `.f64.` and `.sparse.` fixtures).
    - `cargo test -p treelite-harness gtil_matrix` exits 0 (all matrix cells within 1e-5) — the Plan-01 RED test is now GREEN (EQV-03).
    - `cargo test --workspace` fully green: no regression on any existing per-loader golden or the binary path.
  </acceptance_criteria>
  <done>The exhaustive-matrix runner is GREEN: every committed cell asserts within 1e-5 of its upstream golden, reports max |delta|, and dense==sparse parity holds; the full workspace suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures → harness | Fixtures are read-only frozen contract; the harness must parse old + new manifest shapes without failing. |
| typed GtilError → anyhow | Library errors are surfaced with context, never panicking across the harness boundary (ERR-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | manifest schema drift (old fixtures) | mitigate | New provenance fields are `#[serde(default)]` Option — old manifests parse; `check_manifest` warns (never fails) on backend/toolchain drift (D-09 diagnosability). |
| T-05-13 | Repudiation | a 1e-5 miss with no diagnosability | mitigate | EQV-04 max-`|delta|` report + manifest `backend`/`os`/`arch`/`libc` make a future miss diagnosable per backend (RESEARCH Pitfall 4). |
| T-05-14 | DoS | malformed fixture causing harness panic | accept | Fixtures are trusted committed artifacts; a parse error surfaces as an `anyhow` context chain (ERR-02), not a security event. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: nothing to install). |
</threat_model>

<verification>
- `cargo test -p treelite-harness gtil_matrix -- --nocapture` green; max `|delta|` printed per cell; cells span both presets, both input dtypes, all 4 kinds, dense + sparse.
- `cargo test --workspace` green — no regression.
- grep checks: `Manifest.backend` present; `Backend`/`RunnerCase` fn-pointer seam (no `dyn`); dense==sparse parity assert present; `#[ignore]` removed from the matrix test.
</verification>

<success_criteria>
- The exhaustive matrix asserts within 1e-5 across model types, both presets, all kinds (EQV-03) and reports max observed deviation (EQV-04).
- Dense==sparse parity holds on identical logical data (D-04, SC1).
- The Manifest carries a `backend` field + full provenance (D-09); the runner is backend-parameterized via a minimal fn-pointer seam (D-11), ready for Phase-6 registration.
</success_criteria>

<output>
Create `.planning/phases/05-full-scalar-gtil-equivalence-harness/05-05-SUMMARY.md` when done.
</output>
