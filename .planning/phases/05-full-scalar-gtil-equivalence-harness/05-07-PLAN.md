---
phase: 05-full-scalar-gtil-equivalence-harness
plan: 07
type: execute
wave: 7
depends_on: ["05-05", "05-06"]
files_modified:
  - fixtures/capture_gtil_matrix.py
  - crates/treelite-harness/tests/gtil_matrix.rs
gap_closure: true
must_haves:
  truths:
    - "A committed f64-input fixture whose postprocessor is sigmoid (or exponential) with large-margin rows is asserted against the 1e-5 gate, exercising the CR-01 f64-postprocessor path rather than silently absorbing it"
    - "Sparse cells consume the frozen CSR triple (data, indices, indptr) captured at freeze time, NOT a CSR re-derived from NaN-presence (WR-01)"
    - "At least one paired assertion ties the f32 and f64 cells of the same model/kind/seed together, so a silent f32→f64 pre-cast would FAIL the test (WR-06)"
    - "Every existing committed golden in fixtures/gtil/ still asserts within 1e-5 — the matrix is regenerated/extended, not broken"
  artifacts:
    - path: "fixtures/capture_gtil_matrix.py"
      provides: "A large-margin f64-input sigmoid/exponential model + freezes the actual CSR triple into the sparse golden payload"
      contains: "csr"
    - path: "crates/treelite-harness/tests/gtil_matrix.rs"
      provides: "Sparse cells load the frozen CSR verbatim; an f32-vs-f64 paired divergence assertion on the boundary row"
      contains: "indptr"
    - path: "fixtures/gtil/"
      provides: "New committed large-margin f64 sigmoid/exponential golden(s) + frozen CSR triples in the sparse payloads"
      contains: "golden"
  key_links:
    - from: "fixtures/capture_gtil_matrix.py"
      to: "fixtures/gtil/*.sparse.*.golden.json"
      via: "_freeze_cell writes the captured csr (data/indices/indptr) into the sparse payload"
      pattern: "csr|indptr"
    - from: "crates/treelite-harness/tests/gtil_matrix.rs"
      to: "fixtures/gtil/*.sparse.*.golden.json"
      via: "run_cell loads the frozen CSR for sparse cells instead of build_csr from NaN-presence"
      pattern: "indptr|col_ind"
autonomous: true
requirements: [GTIL-02, GTIL-04, EQV-01, EQV-02, EQV-03, EQV-04]
---

<objective>
Make the CR-01 f64-postprocessor fix and the WR-01 sparse-path guarantee actually MEASURED, and catch a silent input-dtype collapse (WR-06): (1) add a committed large-margin f64-input fixture whose postprocessor is `sigmoid` or `exponential` so the corrected f64 arithmetic from 05-06 is exercised against the 1e-5 gate rather than absorbed; (2) freeze the actual CSR triple (`data`, `indices`, `indptr`) into each sparse golden at capture time and have the runner load it verbatim, removing the "present NaN == absent" ambiguity of re-deriving the CSR from NaN-presence; (3) add a paired assertion that the f32 and f64 cells of the categorical-gap model differ on the `2^24`-boundary row, so a future f32→f64 pre-cast fails the test.

Purpose: 05-06 fixes the engine; this plan PROVES it. The large-margin f64 fixture is the gate that would have caught CR-01; the frozen CSR triple is the gate that closes WR-01's silent reconstruction gap; the paired f32/f64 divergence assertion closes WR-06's blind spot on the D-05 input-dtype axis. Goldens are regenerated via `uv run python` on the main tree (the venv/pyproject are untracked — no worktree isolation; per MEMORY.md run this on main).
Output: extended `capture_gtil_matrix.py` (large-margin f64 model + CSR-triple freeze), regenerated/extended `fixtures/gtil/*.golden.json`, updated `gtil_matrix.rs` (frozen-CSR load + f32/f64 paired assert).
</objective>

## Artifacts this phase produces

This plan (05-07) creates the following NEW symbols/fixtures:

- `fixtures/capture_gtil_matrix.py`: a new `build_large_margin_model(seed)` (XGBoost `binary:logistic` or a `count:poisson`/`exponential`-objective model) authored to produce large-margin rows; a `cat_idx`/`col_ind`/`row_ptr` (or `data`/`indices`/`indptr`) triple written into every sparse cell's payload by `_freeze_cell` (an `extra` payload key, e.g. `"csr": {...}`); a `main()` registration of the new model.
- `fixtures/gtil/`: the NEW committed `large_margin.f32.f64.<kind>.<dense|sparse>.s<seed>.golden.json` cells (and the existing cells re-frozen WITH the CSR triple in their sparse payloads).
- `crates/treelite-harness/tests/gtil_matrix.rs`: a `frozen_csr` loader path in `run_cell` (replacing `build_csr` for sparse cells); a new `assert_f32_f64_diverge` (or inline paired assertion) tying the f32 and f64 cells of the categorical-gap model on the `2^24`-boundary row.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-CONTEXT.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-REVIEW.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-06-PLAN.md
@fixtures/capture_gtil_matrix.py
@crates/treelite-harness/tests/gtil_matrix.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 fixture + WR-01 frozen CSR — add a large-margin f64 sigmoid/exponential model and freeze the real CSR triple into sparse goldens</name>
  <files>fixtures/capture_gtil_matrix.py</files>
  <read_first>
    - fixtures/capture_gtil_matrix.py:248-296 (`build_binary_model` / `build_leaf_vector_model` — the model-authoring pattern to mirror for a new large-margin model; `binary:logistic` uses the `sigmoid` postprocessor, the CR-01 target)
    - fixtures/capture_gtil_matrix.py:212-241 (`_dense_and_csr` — already builds the real `data`/`indices`/`indptr` triple from the presence mask; THIS is the triple to freeze, instead of letting the Rust side re-derive it)
    - fixtures/capture_gtil_matrix.py:309-366 (`capture_model` + `_freeze_cell` — the sparse cell freeze at :345-348 that currently writes only `dense_nan` as `input`; extend `_freeze_cell` to also write the captured CSR triple into the sparse payload via the `extra`/payload mechanism)
    - fixtures/capture_gtil_matrix.py:84-112 (`_manifest` — the `extra` merge hook; and `_json_safe` at :146-166 for non-finite-safe serialization of the CSR `data`)
    - fixtures/capture_gtil_matrix.py:369-398 (`main` — where the new model is registered alongside `binary` and `leaf_vec_mc`)
    - treelite-mainline/src/gtil/postprocessor.cc:33-52 (sigmoid/exponential f64 bodies — confirm the large-margin value range where f64 vs f32 `exp` diverges past 1e-7, so the fixture genuinely stresses the CR-01 path)
    - treelite-mainline/src/gtil/predict.cc:76-85 (`SparseMatrixAccessor::GetRow` — absent CSR entries materialize as NaN; the frozen triple must encode exactly the present set so the Rust sparse path is fed the real captured CSR)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-REVIEW.md (CR-01 large-margin-fixture requirement; WR-01 frozen-CSR-triple requirement)
  </read_first>
  <action>
    CR-01 fixture: Add `build_large_margin_model(seed)` mirroring `build_binary_model` but trained/constructed so prediction margins are LARGE (e.g. deep trees + high learning rate, or labels that drive scores well past ±10) — the regime where f64 `sigmoid`/`exp` and f32 `sigmoid`/`exp` differ by more than 1e-7. Use a postprocessor on the CR-01 surface: `binary:logistic` (→ `sigmoid`) is sufficient; optionally also a `count:poisson`-adjacent / `exponential` model for the `exponential` postprocessor. Register it in `main()` (e.g. `capture_model("large_margin", lm_model, lm_feat, lm_cat)`), so the full (preset × input_dtype × kind × {dense,sparse} × seed) cross-product is frozen for it — CRITICALLY including the `f64` input_dtype cells, which are the ones that exercise the corrected f64 postprocessor path. The committed f64 large-margin sigmoid golden is the cell that asserts against the 1e-5 gate in Task 2.

    WR-01 frozen CSR: Extend `_dense_and_csr` (or `capture_model`) to surface the real CSR triple (`data`, `indices`, `indptr` — already computed at :229-240) and have `_freeze_cell` write it into EVERY sparse cell's payload (e.g. a new `"csr": {"data": _json_safe(data), "indices": indices.tolist(), "indptr": indptr.tolist()}` key, with `data` run through `_json_safe` for non-finite safety). The dense cells keep their current payload (no CSR). Keep the existing capture-time D-04 assertion (dense-with-NaN == CSR) so the frozen triple is still proven equivalent to the dense path at freeze time. Re-run the capture on the MAIN worktree via `uv run python fixtures/capture_gtil_matrix.py` (the venv lives only on main, per MEMORY.md) and commit the regenerated `fixtures/gtil/*.golden.json` — the existing cells now additionally carry the frozen CSR, and the new `large_margin.*` cells are added.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && uv run python fixtures/capture_gtil_matrix.py 2>&1 | tail -8 && ls fixtures/gtil/large_margin.*.f64.*.golden.json 2>/dev/null | head && python3 -c "import json,glob; f=sorted(glob.glob('fixtures/gtil/*.sparse.*.golden.json'))[0]; d=json.load(open(f)); assert 'csr' in d and 'indptr' in d['csr'], 'sparse payload missing frozen csr triple'; print('csr triple present in', f)"</automated>
  </verify>
  <acceptance_criteria>
    - `build_large_margin_model` exists and is registered in `main()`: `grep -q "build_large_margin_model" fixtures/capture_gtil_matrix.py` and `grep -q "large_margin" fixtures/capture_gtil_matrix.py`.
    - The capture run produces committed f64-input large-margin cells: `ls fixtures/gtil/large_margin.*.f64.*.golden.json` lists at least the `default` (sigmoid post-processed) dense + sparse cells for both seeds.
    - Every sparse golden carries the frozen CSR triple: the verify command's `python3` assert confirms a `"csr"` object with `data`/`indices`/`indptr` in a sparse payload (and dense payloads do NOT need it).
    - The capture's existing capture-time D-04 (dense-with-NaN == CSR) assertion still passes for every cell (the script runs to completion without an AssertionError) — the frozen triple is proven equivalent at freeze time.
    - The large-margin f64 sigmoid golden values are NOT bit-identical to what the collapsed-f32 path would produce (this is the cell Task 2 asserts to 1e-5 against the corrected engine) — verified indirectly when Task 2's gate passes against the 05-06 engine but the margins are large enough to have failed the pre-05-06 collapsed-f32 engine.
  </acceptance_criteria>
  <done>A large-margin f64 sigmoid/exponential model is captured (its f64 cells exercise the CR-01 path), and every sparse golden carries the real captured CSR triple (data/indices/indptr); the regenerated fixtures are committed and the capture-time D-04 parity still holds.</done>
</task>

<task type="auto">
  <name>Task 2: WR-01 consume frozen CSR + WR-06 f32/f64 paired divergence + CR-01 1e-5 gate on the large-margin f64 cell</name>
  <files>crates/treelite-harness/tests/gtil_matrix.rs</files>
  <read_first>
    - crates/treelite-harness/tests/gtil_matrix.rs:188-228 (`build_csr` + the `IsNan` trait — the NaN-presence reconstruction that THIS task stops using for sparse cells, replacing it with the frozen-CSR load)
    - crates/treelite-harness/tests/gtil_matrix.rs:240-294 (`run_cell` — the sparse arms at :259-266 (f32) and :277-283 (f64) that call `build_csr`; replace the sparse path to load the frozen `golden.csr` triple verbatim into a `SparseCsr` view)
    - crates/treelite-harness/tests/gtil_matrix.rs:296-420 (the golden gate at :392-400, the dense==sparse parity assert at :402-416, the per-cell counters — where the WR-06 f32/f64 paired assertion is added and where the new large_margin f64 cell flows through the 1e-5 gate)
    - crates/treelite-harness/src/lib.rs (the local golden serde struct / `MatrixGolden` — extend it with an optional `csr` field deserialized from the frozen sparse payload; mirror the `#[serde(default)]` Option style the manifest uses)
    - crates/treelite-gtil/src/lib.rs (`SparseCsr` view shape — `data`/`col_ind`/`row_ptr`; the frozen `indices`/`indptr` map to `col_ind`/`row_ptr`)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-REVIEW.md (WR-01 "load it verbatim for sparse cells"; WR-06 "paired assertion that f32 and f64 cells ... differ on the 2^24-boundary row")
  </read_first>
  <action>
    WR-01: Add an optional `csr` field to the golden struct (the `data`/`indices`/`indptr` triple frozen in Task 1; `#[serde(default)]` Option so dense goldens parse unchanged). In `run_cell`, for a fixture whose `manifest.layout == "sparse"`, build the `SparseCsr` view from the FROZEN triple (`data`→`data`, `indices`→`col_ind`, `indptr`→`row_ptr`) instead of calling `build_csr` on the NaN-presence reconstruction. The f32 sparse arm reads the frozen `data` as `Vec<f32>`, the f64 arm as `Vec<f64>` (no pre-cast, D-05). Keep `build_csr` only if still needed for any dense→sparse parity construction; if it is now unused, remove it (and the `IsNan` trait) rather than leaving dead code. This makes the sparse path assert against the REAL captured CSR (closing the "present NaN == absent" ambiguity).

    WR-06: Add a paired assertion (e.g. `assert_f32_f64_diverge`) that loads the f32 and f64 cells of the SAME model/kind/seed (the categorical-gap model — `binary` or `leaf_vec_mc`, whichever carries the `2^24`-boundary categorical column) and asserts their outputs DIFFER on at least the `2^24`-boundary row — proving the input-dtype axis is meaningfully exercised, so a future silent f32→f64 pre-cast inside a backend would FAIL this assertion. If the golden values themselves already encode the divergence, assert that the two committed goldens differ on that row (and document the row index). Choose the cell pair deterministically (same `kind`/`seed`/`layout`, differing only in `input_dtype`).

    CR-01 gate: Ensure the new `large_margin.*.f64.*` cells flow through the existing 1e-5 golden gate (`assert_within(&own, &expected, 1e-5, ...)`) — no special-casing; the matrix runner already iterates every `fixtures/gtil/*.golden.json`, so the large-margin f64 sigmoid cell is asserted to 1e-5 against the 05-06-corrected engine. Add an explicit `eprintln!` (or assert) that at least one `large_margin.*.f64.*` cell ran and passed, so the CR-01 coverage is visible in `--nocapture` output.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && cargo test -p treelite-harness gtil_matrix -- --nocapture 2>&1 | grep -E "large_margin.*f64|test result|max \|delta\|" | tail -20 && cargo test --workspace 2>&1 | grep -E "test result"</automated>
  </verify>
  <acceptance_criteria>
    - The golden struct has an optional `csr` field and sparse cells load it: `grep -E "csr|indptr|col_ind" crates/treelite-harness/tests/gtil_matrix.rs` shows the frozen-triple load in the sparse arm of `run_cell`, and `build_csr` is no longer called for sparse cells (grep confirms `build_csr` is removed or only used for a non-sparse purpose).
    - A paired f32/f64 divergence assertion exists (WR-06): `grep -E "diverge|f32.*f64|input_dtype" crates/treelite-harness/tests/gtil_matrix.rs` shows an assertion that the f32 and f64 cells of the same model/kind/seed DIFFER on the boundary row (not just that each matches its own golden).
    - At least one `large_margin.*.f64.*` cell runs and passes the 1e-5 gate: the `--nocapture` output shows a `large_margin` `.f64.` cell with `max |delta| < 1e-5` PASS — this is the CR-01 cell that the pre-05-06 collapsed-f32 engine would have risked failing on large margins.
    - `cargo test -p treelite-harness gtil_matrix` exits 0 (all matrix cells, including the new large-margin f64 sigmoid cells and the frozen-CSR sparse cells, within 1e-5).
    - `cargo test --workspace` exits 0 — no regression to any existing golden or per-loader test.
  </acceptance_criteria>
  <done>Sparse cells assert against the real frozen CSR triple (WR-01 closed), the f32 and f64 cells of the categorical-gap model are asserted to diverge on the boundary row (WR-06 closed), and the large-margin f64 sigmoid cell passes the 1e-5 gate against the 05-06-corrected engine (CR-01 proven); the full workspace suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed sparse fixtures → harness | The frozen CSR triple is a read-only contract; the runner must load it verbatim (not re-derive it) so the sparse path is fed the real captured CSR, never a NaN-presence reconstruction that can diverge on a present-NaN cell. |
| input-dtype axis (f32 vs f64) → backend | The f32 and f64 cells are different computations (D-05); a silent f32→f64 pre-cast must be caught, not pass by matching each cell to its own golden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Spoofing | sparse CSR re-derived from NaN-presence (WR-01) | mitigate | Freeze + load the real CSR triple (data/indices/indptr); the capture-time D-04 assert still proves dense==sparse equivalence, so the sparse path is fed the genuine captured CSR, not a reconstruction that can mistake a present NaN for absent. |
| T-05-20 | Tampering | silent f32→f64 input pre-cast (WR-06) | mitigate | Paired f32/f64 divergence assertion on the `2^24`-boundary row fails if the two axes ever collapse to one computation. |
| T-05-21 | Repudiation | CR-01 f64-postprocessor defect with no gate (CR-01) | mitigate | A committed large-margin f64 sigmoid/exponential cell is asserted against the 1e-5 gate, making the deviation diagnosable (EQV-03/04) rather than silently absorbed. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; capture uses the already-present `uv` venv (treelite/numpy/scipy/xgboost capture-only, never in the Rust build graph, D-06). |
</threat_model>

<verification>
- `uv run python fixtures/capture_gtil_matrix.py` (run on the MAIN worktree) completes; the capture-time D-04 (dense==sparse) assert passes for every cell, including the new large-margin model.
- `cargo test -p treelite-harness gtil_matrix -- --nocapture` green: every committed cell within 1e-5 (including the new large-margin f64 sigmoid cells and the frozen-CSR sparse cells); max |delta| printed per cell.
- `cargo test --workspace` green — no regression.
- grep checks: golden `csr` field + frozen-triple load in the sparse arm; `build_csr` no longer used for sparse; f32/f64 paired divergence assertion present; a `large_margin.*.f64.*` cell visibly passing in `--nocapture`.
</verification>

<success_criteria>
- CR-01 is MEASURED: a committed large-margin f64 sigmoid (and/or exponential) fixture passes the 1e-5 gate against the 05-06-corrected f64-postprocessor engine — the gate that would have caught the original collapse (GTIL-04, EQV-03/04).
- WR-01: sparse cells assert against the real frozen CSR triple, not a NaN-presence reconstruction (GTIL-02, EQV-01/02).
- WR-06: the f32 and f64 cells of the categorical-gap model are asserted to diverge on the boundary row, so a silent input-dtype collapse fails the test.
- `cargo test --workspace` green; the frozen `fixtures/gtil/` matrix is extended and regenerated, never broken.
</success_criteria>

<output>
Create `.planning/phases/05-full-scalar-gtil-equivalence-harness/05-07-SUMMARY.md` when done.
</output>
