---
phase: 05-full-scalar-gtil-equivalence-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/capture_gtil_matrix.py
  - fixtures/gtil/
  - crates/treelite-harness/tests/gtil_matrix.rs
  - crates/treelite-gtil/src/lib.rs
  - crates/treelite-gtil/src/postprocessor.rs
autonomous: false
requirements: [EQV-01, EQV-02]
must_haves:
  truths:
    - "fixtures/gtil/ holds committed seeded input matrices + upstream-Treelite golden vectors for the matrix cells (dense + sparse CSR, f32 + f64 input, all 4 kinds)"
    - "Each captured golden carries a provenance manifest with a backend field set to scalar-cpu"
    - "Capture asserts dense-with-NaN == CSR at capture time (D-04 construction) before freezing"
    - "A RED equivalence-matrix test exists that fails until the GTIL surface is widened (Plans 02-04)"
  artifacts:
    - path: "fixtures/capture_gtil_matrix.py"
      provides: "One-time seeded capture: dense + CSR, both input dtypes, all 4 kinds, edge-seeded wide matrices"
      contains: "treelite.gtil.predict"
    - path: "crates/treelite-harness/tests/gtil_matrix.rs"
      provides: "RED exhaustive-matrix runner scaffold (ignored/expected-fail until widening lands)"
      contains: "fixtures/gtil"
  key_links:
    - from: "fixtures/capture_gtil_matrix.py"
      to: "fixtures/gtil/*.golden.json"
      via: "_write_golden frozen fixtures"
      pattern: "_write_golden"
    - from: "crates/treelite-harness/tests/gtil_matrix.rs"
      to: "fixtures/gtil/"
      via: "fixture path resolver"
      pattern: "gtil"
---

<objective>
Establish the frozen Phase-5 contract: a one-time seeded capture script that produces committed input matrices + upstream-Treelite golden vectors for the exhaustive matrix, plus a RED equivalence-matrix test scaffold and GTIL unit-test scaffolds that fail until the GTIL surface is widened (Plans 02-04).

Purpose: Per D-08 the committed matrices + goldens are the source of truth and CI never re-draws from a seed. This Wave-0 plan freezes that contract first so every later task verifies against a real upstream golden (Nyquist). Capture runs via `uv run python` on the MAIN tree because the venv/pyproject are untracked and absent from worktrees (MEMORY.md).
Output: `fixtures/capture_gtil_matrix.py`, `fixtures/gtil/*.golden.json` + `*.manifest.json` (frozen), `crates/treelite-harness/tests/gtil_matrix.rs` (RED), and unit-test scaffolds in `treelite-gtil`.
</objective>

## Phase Goal

**As a** treelite-rs maintainer validating the inference engine, **I want to** assert Rust GTIL predictions within 1e-5 of frozen upstream-Treelite goldens across the full model × preset × input-dtype × kind × {dense,sparse} × seed matrix, **so that** every later compute backend (cubecl/GPU) has a trusted 1e-5 measurement instrument.

## Artifacts this phase produces

This phase (Phase 5) creates the following NEW symbols/paths across its plans. Listed here for the source-grounding pass (these are NOT drift against upstream — they are intentionally new):
- `Config` struct + `PredictKind` enum (`crates/treelite-gtil/src/config.rs`) — Plan 02
- Public `Shape` descriptor + `output_shape()` fn (`crates/treelite-gtil/src/shape.rs`) — Plan 02
- Internal `Shape<'m>` renamed to `OutputLayout` (`crates/treelite-gtil/src/lib.rs`) — Plan 02
- `PredictOut` output-element trait + `predict`/`predict_sparse` generic over `O` (`lib.rs`) — Plan 02/04
- `SparseCsr` borrowed view / sparse accessor (`crates/treelite-gtil/src/accessor.rs`) — Plan 04
- New postprocessor fns `signed_square`, `hinge`, `multiclass_ova` (`postprocessor.rs`) — Plan 03
- `Backend` enum + `PredictFn` registry + manifest `backend` field (`crates/treelite-harness`) — Plan 05
- New `GtilError` variants: sparse `col_ind`/`row_ptr` bounds (`SparseColumnOutOfBounds`, `SparseRowPtrInvalid`) — Plan 04
- New fixture/manifest paths under `fixtures/gtil/` and capture script `fixtures/capture_gtil_matrix.py` — Plan 01

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-CONTEXT.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md
@fixtures/capture_lightgbm.py
@crates/treelite-harness/tests/lightgbm.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the seeded exhaustive-matrix capture script + freeze fixtures (EQV-01, EQV-02)</name>
  <files>fixtures/capture_gtil_matrix.py, fixtures/gtil/</files>
  <read_first>
    - fixtures/capture_lightgbm.py (the capture-script trio to copy verbatim: `_manifest(extra=None)`, `_payload_sha256(input_list, output_list)`, `_write_golden(name, payload)`; `SEED`/`RandomState`; the capture-time `treelite.gtil.predict` signature assert at lines ~191-198)
    - treelite-mainline/src/gtil/predict.cc (lines 58-97: SparseMatrixAccessor::GetRow — absent CSR entries materialize as `quiet_NaN()` at :81, NOT 0; the authoritative absent=NaN semantic the capture must reproduce)
    - treelite-mainline/python/treelite/gtil/gtil.py (the predict / predict_leaf / predict_per_tree entry points and their pred_type strings — the capture API for all 4 kinds)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md (Code Examples "Capture script skeleton"; Open Q1 sparse-with-NaN construction; Open Q2 corpus model selection; Pitfall 3 the `2**24+1` f32 categorical-gap edge value)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md (`fixtures/capture_gtil_matrix.py` section — NEW capture additions list; fixture naming `<model>.<preset>.<indtype>.<kind>.<dense|sparse>.s<seed>.golden.json`)
  </read_first>
  <action>
    Create `fixtures/capture_gtil_matrix.py` extending the `capture_lightgbm.py` trio (`_manifest`/`_payload_sha256`/`_write_golden`). Run it via `uv run python fixtures/capture_gtil_matrix.py` on the MAIN tree (venv/pyproject untracked — do NOT run in a worktree). It captures the exhaustive cross-product per D-01/D-03: select ONE representative model per capability axis (D-02 wide, few seeds) using Claude's Discretion / RESEARCH Open Q2 — reuse existing fixtures where they cover an axis (XGBoost `xgb_3format` binary-scalar; LightGBM `toy_categorical`/`sparse_categorical` for categorical+sparse from `treelite-mainline/tests/examples/`; existing sklearn RF/GB goldens for multiclass leaf-vector), and author a small fresh multiclass model in-script for the 4-way leaf-vector-broadcast axis if no vendored example covers it. For each (model × preset × input-dtype f32/f64 × seed in {1234, 5678} × {dense, sparse}): build a wide (≈100-300 row) `np.random.RandomState(seed)` matrix injected with edge values — `np.nan` (missing), `np.inf`/`-np.inf`, boundary thresholds, and `2**24 + 1` in a categorical feature column (the f32 representability-gap value, Pitfall 3). Capture all 4 kinds via `treelite.gtil.predict(model, Xc)` (default), `treelite.gtil.predict(model, Xc, pred_margin=True)` (raw), `treelite.gtil.predict_leaf(model, Xc)` (leaf_id), `treelite.gtil.predict_per_tree(model, Xc)` (score_per_tree). For the sparse cell, resolve RESEARCH Open Q1: build a presence mask, a dense matrix with `np.nan` in absent positions, a `scipy.sparse.csr_matrix` from the present positions, then ASSERT at capture time `np.allclose(treelite.gtil.predict(dense_with_nan), treelite.gtil.predict(csr_via_C_API), equal_nan=True)` BEFORE freezing both goldens (if scipy is absent, hand-build the CSR `data`/`indices`/`indptr` arrays from numpy per the Environment Availability fallback). Extend `_manifest` to add `backend: "scalar-cpu"`, `rustc`, a `cubecl: "n/a"` placeholder (D-09), every capture-framework version, the seed, and a per-fixture sha256. Add a capture-time assert that `treelite.gtil.predict` exists with the expected signature so an upstream API change is caught (mirror `capture_lightgbm.py:191-198`). Write every cell to `fixtures/gtil/<model>.<preset>.<indtype>.<kind>.<dense|sparse>.s<seed>.golden.json` (payload shape `{model_path, n_features, input, output, output_shape, manifest, sha256}`, mirroring `lightgbm_categorical.golden.json`). Freeze fixtures read-only; never hand-edit (D-08).
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && uv run python fixtures/capture_gtil_matrix.py && ls fixtures/gtil/*.golden.json | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `fixtures/capture_gtil_matrix.py` exists and `grep -q "treelite.gtil.predict" fixtures/capture_gtil_matrix.py` succeeds.
    - `grep -q "predict_leaf" fixtures/capture_gtil_matrix.py` AND `grep -q "predict_per_tree" fixtures/capture_gtil_matrix.py` succeed (all 4 kinds captured).
    - `grep -q "scalar-cpu" fixtures/capture_gtil_matrix.py` succeeds (manifest `backend` field, D-09).
    - `grep -q "2\*\*24" fixtures/capture_gtil_matrix.py` succeeds (f32 categorical-gap edge value seeded, Pitfall 3).
    - The script contains a capture-time dense-with-NaN == CSR assert (grep for `allclose` AND `equal_nan` OR an explicit equality assert with a comment citing D-04 / Open Q1).
    - Running the script exits 0 and produces ≥ 8 files under `fixtures/gtil/` covering both presets, both input dtypes, all 4 kinds, and both {dense, sparse} (verify by `ls fixtures/gtil/ | grep -c golden.json` returns ≥ 8, and the set includes at least one `.f64.` and one `.sparse.` filename).
    - Each `fixtures/gtil/*.golden.json` parses as JSON and contains a `manifest` object whose `backend` value equals `"scalar-cpu"`.
  </acceptance_criteria>
  <done>The exhaustive-matrix capture script runs on the main tree and freezes committed input matrices + upstream-Treelite goldens + scalar-cpu manifests under `fixtures/gtil/`, with the dense==sparse parity asserted at capture time.</done>
</task>

<task type="auto">
  <name>Task 2: RED equivalence-matrix test scaffold + GTIL unit-test scaffolds (Nyquist Wave 0)</name>
  <files>crates/treelite-harness/tests/gtil_matrix.rs, crates/treelite-gtil/src/lib.rs, crates/treelite-gtil/src/postprocessor.rs</files>
  <read_first>
    - crates/treelite-harness/tests/lightgbm.rs (the golden-assert skeleton to copy: `fixture_path`/`workspace_path` resolvers via `env!("CARGO_MANIFEST_DIR")`; local `#[derive(Deserialize)]` golden struct; `check_manifest` drift call; flatten → predict → max-`|delta|` loop with `approx::assert_abs_diff_eq!(epsilon = 1e-5)`; `.map_err(|e| anyhow::anyhow!("{e}"))` bridge)
    - crates/treelite-gtil/src/lib.rs (the existing `predict` signature and the `next_node_categorical` MINIMAL guard at :94-120 + the `apply_postprocessor` match at :592-648 — the unit-test scaffolds target the symbols that Plans 02-04 widen)
    - crates/treelite-gtil/src/postprocessor.rs (existing `#[cfg(test)] mod tests` idiom at :134-203; the new postprocessor tests follow the same hand-reference + `< 1e-7` shape)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-VALIDATION.md (Phase Requirements → Test Map; the Wave 0 gaps list naming the unit tests: categorical_guard, sparse NaN-fill, output_shape, missing_value, the 3 new postprocessors)
  </read_first>
  <action>
    Create `crates/treelite-harness/tests/gtil_matrix.rs` copying the `tests/lightgbm.rs` skeleton (resolvers, local-serde golden struct extended for the matrix payload `{model_path, n_features, input, output, output_shape, manifest}`, `check_manifest` call). Implement the iteration over the committed `fixtures/gtil/*.golden.json` set (discover via `std::fs::read_dir`), but gate the whole test body behind `#[ignore = "RED until GTIL surface widened in Plans 02-04"]` so it compiles and is explicitly red now (it cannot pass until `Config`/sparse/kinds land). Add, as `#[cfg(test)]` scaffolds in `crates/treelite-gtil/src/postprocessor.rs`, three FAILING unit tests for `signed_square`, `hinge`, `multiclass_ova` with hand-computed references (e.g. `signed_square(-3.0)` ⇒ `-9.0`; `hinge(0.5)` ⇒ `1.0`, `hinge(-0.5)` ⇒ `0.0`; `multiclass_ova` per-class sigmoid) — these reference the not-yet-existing fns so they are RED via compile-or-assert until Plan 03; mark each `#[ignore = "RED until Plan 03"]` if a missing symbol would otherwise break the build, OR reference them through a `// TODO Plan 03` stub comment that names the exact fn signatures to add. Add a `#[cfg(test)]` test in `crates/treelite-gtil/src/lib.rs` named `categorical_full_guard_red` asserting the FULL representability behavior (an f32 input value of `2.0**24 + 1.0` in a categorical feature is rejected as a non-match, i.e. routes the not-matched direction), `#[ignore = "RED until Plan 03 full categorical guard"]`. Each `#[ignore]` reason string is the Wave-0 MISSING marker the Nyquist gate reads.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && cargo test -p treelite-gtil -p treelite-harness --no-run 2>&1 | tail -5 && cargo test -p treelite-harness gtil_matrix -- --ignored --list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness --no-run` compiles `tests/gtil_matrix.rs` (exit 0) — the scaffold builds.
    - `crates/treelite-harness/tests/gtil_matrix.rs` contains `approx::assert_abs_diff_eq!` with `epsilon = 1e-5` (the hard gate, never loosened) and references `fixtures/gtil`.
    - `crates/treelite-harness/tests/gtil_matrix.rs` contains an `#[ignore` attribute whose reason mentions RED / Plans 02-04 (the Wave-0 MISSING marker).
    - `grep -c "signed_square\|hinge\|multiclass_ova" crates/treelite-gtil/src/postprocessor.rs` (after filtering comments via `grep -v '^[[:space:]]*//'`) is ≥ 3 (the three new postprocessor tests/stubs are present).
    - `cargo test -p treelite-gtil --no-run` exits 0 (unit-test scaffolds compile).
    - `cargo test --workspace` still passes for the EXISTING green suite (no regression): run `cargo test --workspace 2>&1 | grep -E "test result: ok"` and confirm the pre-existing tests remain ok (newly-ignored tests do not count as failures).
  </acceptance_criteria>
  <done>A RED `gtil_matrix.rs` matrix runner and RED unit-test scaffolds (categorical full guard + 3 postprocessors) exist, compile, and are explicitly ignored with Wave-0 reasons; the existing workspace suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture script → committed fixtures | Capture runs once on a trusted main tree; output fixtures become the frozen contract. |
| committed fixtures → CI test | Fixtures are read by the harness; integrity is tracked via per-fixture sha256 (provenance, not a security control). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | committed `fixtures/gtil/*.golden.json` | mitigate | Per-fixture sha256 in the manifest (D-09) records integrity; CI never regenerates (D-08), so a silent fixture edit is detectable by manifest diff. |
| T-05-02 | Information disclosure | capture script env | accept | No secrets; capture uses local venv only (RESEARCH Runtime State Inventory — no secrets/env vars). |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; capture uses Python packages already present in the untracked venv (RESEARCH Package Legitimacy Audit: nothing to install). |
</threat_model>

<verification>
- `uv run python fixtures/capture_gtil_matrix.py` exits 0 on the main tree and freezes ≥ 8 fixtures under `fixtures/gtil/`.
- `cargo test -p treelite-harness --no-run` and `cargo test -p treelite-gtil --no-run` both exit 0.
- `cargo test --workspace` keeps the pre-existing suite green (ignored RED tests excluded).
</verification>

<success_criteria>
- The exhaustive-matrix capture script is committed and produces frozen input matrices + upstream goldens + `scalar-cpu` manifests (EQV-01, EQV-02).
- The dense-with-NaN == CSR parity is asserted at capture time (D-04 construction resolved, Open Q1).
- A RED matrix runner + RED unit scaffolds exist and compile, providing the Wave-0 Nyquist targets for Plans 02-04.
</success_criteria>

<output>
Create `.planning/phases/05-full-scalar-gtil-equivalence-harness/05-01-SUMMARY.md` when done.
</output>
