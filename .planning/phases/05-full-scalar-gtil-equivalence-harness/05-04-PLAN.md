---
phase: 05-full-scalar-gtil-equivalence-harness
plan: 04
type: execute
wave: 4
depends_on: ["05-02", "05-03"]
files_modified:
  - crates/treelite-gtil/src/accessor.rs
  - crates/treelite-gtil/src/lib.rs
  - crates/treelite-gtil/src/error.rs
must_haves:
  truths:
    - "predict_sparse(model, csr, &config) materializes absent CSR entries as NaN (not 0) per row and shares evaluate_tree with the dense path"
    - "Rust-dense == Rust-sparse on identical logical data (D-04 parity holds structurally)"
    - "PredictKind::LeafId writes integer leaf node ids into the output buffer; no postprocess/average/base-score"
    - "PredictKind::ScorePerTree writes per-tree leaf scalar (or each leaf-vector element); no postprocess/average/base-score"
    - "Malformed CSR col_ind/row_ptr surface as typed GtilError, never OOB/panic"
  artifacts:
    - path: "crates/treelite-gtil/src/accessor.rs"
      provides: "SparseCsr borrowed view + per-row NaN-materialized get_row (GTIL-02)"
      contains: "SparseCsr"
    - path: "crates/treelite-gtil/src/lib.rs"
      provides: "predict_sparse entry point + PredictImpl dispatch for LeafId/ScorePerTree"
      contains: "predict_sparse"
    - path: "crates/treelite-gtil/src/error.rs"
      provides: "Sparse bounds-check error variants"
      contains: "Sparse"
  key_links:
    - from: "crates/treelite-gtil/src/lib.rs"
      to: "crates/treelite-gtil/src/accessor.rs"
      via: "predict_sparse builds NaN-filled rows then calls evaluate_tree"
      pattern: "get_row|SparseCsr"
autonomous: true
requirements: [GTIL-02, GTIL-03]
---

<objective>
Add the sparse CSR predict path (absent entries materialized as NaN, GTIL-02) sharing `evaluate_tree` with the dense path so dense↔sparse parity is structural (D-04), and the two remaining predict kinds — `LeafId` and `ScorePerTree` — wired into the `Config.kind` dispatch (GTIL-03). All bounds-checked → typed `GtilError`, never OOB/panic.

Purpose: This completes the GTIL surface: dense + sparse input, all 4 predict kinds. The sparse accessor materializes a per-row NaN scratch (exactly upstream `SparseMatrixAccessor`), which is both the absent=NaN semantic AND what makes dense==sparse parity free. The two new kinds write raw leaf data with NO postprocessing/averaging/base-score (Pitfall 5).
Output: `accessor.rs` (new SparseCsr view), `predict_sparse` + LeafId/ScorePerTree dispatch in `lib.rs`, sparse error variants in `error.rs`.
</objective>

## Phase Goal

**As a** treelite-rs maintainer validating the inference engine, **I want to** assert Rust GTIL predictions within 1e-5 of frozen upstream-Treelite goldens across the full model × preset × input-dtype × kind × {dense,sparse} × seed matrix, **so that** every later compute backend has a trusted 1e-5 measurement instrument.

## Artifacts this phase produces

See `05-01-PLAN.md` "Artifacts this phase produces". This plan creates: `SparseCsr` view + sparse accessor, `predict_sparse`, the `LeafId`/`ScorePerTree` dispatch arms, and the `SparseColumnOutOfBounds`/`SparseRowPtrInvalid` error variants.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-CONTEXT.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md
@.planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md
@crates/treelite-gtil/src/lib.rs
@crates/treelite-gtil/src/error.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sparse CSR accessor with per-row NaN materialization + predict_sparse (GTIL-02, D-04)</name>
  <files>crates/treelite-gtil/src/accessor.rs, crates/treelite-gtil/src/lib.rs, crates/treelite-gtil/src/error.rs</files>
  <read_first>
    - crates/treelite-gtil/src/lib.rs (the dense `predict` entry at :519-582 and `predict_preset` row-slicing at :302-303 — `predict_sparse` reuses `evaluate_tree`/`predict_preset` after building a NaN-filled row; the `InvalidInputShape` guard idiom at :524-548)
    - crates/treelite-gtil/src/error.rs (the existing variant style at :16-33: `#[derive(Debug, Error, PartialEq, Eq)]`, named struct fields, `#[error("...")]`, per-field doc citing the threat — the new sparse variants follow it)
    - treelite-mainline/src/gtil/predict.cc (lines 58-97 SparseMatrixAccessor: ctor :61, `GetRow` :72-86 — fill row with `quiet_NaN()` at :81 then write nonzeros at `col_ind[i]`; lines 410-413 `PredictSparse` builds the accessor and calls `PredictImpl`)
    - treelite-mainline/include/treelite/gtil.h (lines 85-88 `PredictSparse` signature: `data`/`col_ind`/`row_ptr` pointers — the borrowed-view shape)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md (Pattern 2 "Sparse accessor"; Anti-Patterns "Treating absent CSR entries as 0"; Security Domain table — col_ind/row_ptr bounds checks)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md (`accessor.rs` section + `lib.rs` "Sparse accessor pattern": single scratch row, bounds-check col_ind<num_feature and row_ptr monotone → typed error)
  </read_first>
  <behavior>
    - Test: a CSR row with entries at columns {0,3} fills a scratch row where columns {1,2,4..} are NaN and {0,3} hold the data values (absent = NaN, NOT 0).
    - Test: `predict_sparse` over a model equals `predict` (dense) when the dense matrix has NaN exactly where the CSR is absent (D-04 parity) — within bitwise/1e-5.
    - Test: `col_ind[i] >= num_feature` returns `GtilError::SparseColumnOutOfBounds`, never panics.
    - Test: non-monotonic `row_ptr` or `row_ptr[num_row] > data.len()` returns `GtilError::SparseRowPtrInvalid`, never panics.
  </behavior>
  <action>
    Create `crates/treelite-gtil/src/accessor.rs` with a borrowed `pub struct SparseCsr<'a, O> { pub data: &'a [O], pub col_ind: &'a [u64], pub row_ptr: &'a [u64] }` (the upstream `data`/`col_ind`/`row_ptr` shape, gtil.h:85-88), and a `get_row` helper porting `SparseMatrixAccessor::GetRow` (predict.cc:72-86): fill a `&mut [O]` scratch with `O::nan()` (absent = NaN, SC1), then `for i in row_ptr[r]..row_ptr[r+1] { scratch[col_ind[i] as usize] = data[i] }`. Bounds-check `col_ind[i] < num_feature` → `GtilError::SparseColumnOutOfBounds { col, num_feature }` and validate `row_ptr` is monotone non-decreasing with `row_ptr[num_row] <= data.len()` → `GtilError::SparseRowPtrInvalid { .. }` (do NOT panic). Add `pub mod accessor;` in lib.rs. Add a `pub fn predict_sparse<O: PredictOut>(model: &Model, csr: SparseCsr<'_, O>, num_row: usize, config: &Config) -> Result<Vec<O>, GtilError>` that validates `row_ptr.len() == num_row + 1`, then for each row builds the NaN-materialized scratch via `get_row` and feeds it through the SAME `evaluate_tree` / per-kind body the dense path uses (refactor `predict_preset` to accept a per-row accessor closure OR a pre-built row slice so dense and sparse share the traversal verbatim — D-04 parity becomes structural). Reuse a single scratch row (scalar single-thread, RESEARCH Pattern 2). Add the two `GtilError` variants in `error.rs` in the existing style with per-field docs citing the Security Domain threats. Un-`#[ignore]` / add the sparse NaN-fill and dense==sparse parity tests.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && cargo test -p treelite-gtil sparse 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `crates/treelite-gtil/src/accessor.rs` exists and contains `pub struct SparseCsr` with `data`/`col_ind`/`row_ptr` fields.
    - The sparse row-fill uses `O::nan()` for absent positions (`grep -q "nan()" crates/treelite-gtil/src/accessor.rs`) — never `0`/`zero()` for absent (anti-pattern check: the fill-then-overwrite order matches predict.cc:81).
    - `crates/treelite-gtil/src/lib.rs` exposes `pub fn predict_sparse` (grep) generic over `O: PredictOut`.
    - `crates/treelite-gtil/src/error.rs` contains `SparseColumnOutOfBounds` AND `SparseRowPtrInvalid` variants (grep both).
    - A test proves `predict_sparse(csr) == predict(dense_with_nan)` within 1e-5 on identical logical data (D-04 structural parity).
    - Malformed-CSR tests return the typed errors and do NOT panic: `cargo test -p treelite-gtil sparse` exits 0 including the OOB col_ind and bad row_ptr cases.
  </acceptance_criteria>
  <done>`predict_sparse` materializes absent CSR entries as NaN, shares `evaluate_tree` with the dense path (dense==sparse parity proven), and rejects malformed `col_ind`/`row_ptr` with typed errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PredictKind::LeafId + ScorePerTree dispatch (GTIL-03)</name>
  <files>crates/treelite-gtil/src/lib.rs</files>
  <read_first>
    - crates/treelite-gtil/src/lib.rs (the `Config.kind` dispatch added in Plan 02 — extend the Default/Raw match with LeafId/ScorePerTree; `evaluate_tree` returns the leaf node id; the `has_leaf_vector` helper at :261-268 and `tree.leaf_vector(leaf)` for the per-tree-vector case)
    - treelite-mainline/src/gtil/predict.cc (lines 325-345 `PredictLeaf`: `output_view(row_id, tree_id) = leaf_id` into an `Array2DView<InputT>`, no postprocess/average/base-score; lines 347-378 `PredictScoreByTree`: `Array3DView` sized `(num_row, num_tree, leaf_vector_shape[0]*leaf_vector_shape[1])`, `if HasLeafVector → output_view(r,t,i)=leafvec[i]` else `output_view(r,t,0)=tree.LeafValue(leaf)`, no postprocess; lines 380-396 PredictImpl dispatch)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-RESEARCH.md (Pattern 3 per-kind dispatch; Pitfall 5 score_per_tree shape with scalar-leaf models — product is 1, write at index 0; Assumption A4 leaf_id stores integer node ids cast into the InputT buffer)
    - .planning/phases/05-full-scalar-gtil-equivalence-harness/05-PATTERNS.md (`lib.rs` "Per-kind dispatch pattern" — LeafId/ScorePerTree bodies, reuse has_leaf_vector, no postprocess/average/base-score)
  </read_first>
  <behavior>
    - Test: `PredictKind::LeafId` output length is `num_row * num_tree` and each cell equals the leaf node id reached for that (row, tree), cast into the `O` buffer (A4) — matches the `predict_leaf` golden in `fixtures/gtil/`.
    - Test: `PredictKind::ScorePerTree` on a scalar-leaf model writes the raw leaf value at index 0 per (row, tree); output length `num_row * num_tree * 1` (Pitfall 5).
    - Test: `PredictKind::ScorePerTree` on a leaf-vector model writes each leaf-vector element; length `num_row * num_tree * (leaf_vector_shape[0]*leaf_vector_shape[1])`.
    - Test: neither kind applies postprocessor, averaging, or base-score (raw leaf data only) — matches its golden exactly.
  </behavior>
  <action>
    Extend the `Config.kind` match in `predict` (and `predict_sparse`) with two arms porting upstream verbatim. `PredictKind::LeafId` → a `predict_leaf` body (predict.cc:325-345): allocate `Vec<O>` of length `num_row * num_tree`, per row serial over tree_id, `output[r * num_tree + tree_id] = O::from_leaf_f64(leaf_id as f64)` (the integer node id cast into the InputT buffer, A4), with NO postprocessor/averaging/base-score. `PredictKind::ScorePerTree` → a `predict_score_by_tree` body (predict.cc:347-378): third-dim size `lvs = leaf_vector_shape[0] * leaf_vector_shape[1]` (≥1), allocate `Vec<O>` length `num_row * num_tree * lvs`, per (row, tree): if `has_leaf_vector(tree, leaf)` write each `tree.leaf_vector(leaf)[i]` cast to `O` at `(r, tree, i)`, else write the scalar `tree.leaf_value(leaf)` cast to `O` at `(r, tree, 0)` (Pitfall 5), with NO postprocessor/averaging/base-score. Bounds-safe leaf-vector access (reuse the `LeafVectorTooShort` pattern). Use `output_shape(model, num_row, config)` (Plan 02) to size the buffer for consistency. Remove the `UnsupportedPredictKind` placeholder error these arms replaced.
  </action>
  <verify>
    <automated>cd /home/user/Documents/workspace/treeline_rs && cargo test -p treelite-gtil 2>&1 | tail -8 && cargo test --workspace 2>&1 | grep -E "test result"</automated>
  </verify>
  <acceptance_criteria>
    - `crates/treelite-gtil/src/lib.rs` dispatch handles all 4 `PredictKind` variants with no remaining placeholder/`UnsupportedPredictKind` arm for LeafId/ScorePerTree (grep the match for all four variant names).
    - A `leaf_id` test asserts output length `num_row * num_tree` and the values match the `predict_leaf` golden in `fixtures/gtil/` (A4: integer node ids in the O buffer).
    - A `score_per_tree` scalar-leaf test writes the raw leaf at index 0 (third-dim size 1, Pitfall 5) and a leaf-vector test writes each element; both match their `predict_per_tree` goldens.
    - Neither kind applies postprocess/average/base-score: a test confirms `score_per_tree` output != the `default` output for a model with a non-identity postprocessor.
    - `cargo test --workspace` green (no regression on dense default/raw or the binary path).
  </acceptance_criteria>
  <done>All 4 predict kinds dispatch correctly: `LeafId` writes integer node ids, `ScorePerTree` writes per-tree leaf scalar/vector (no postprocess/average/base-score), both matching their upstream goldens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → predict_sparse(csr) | `data`/`col_ind`/`row_ptr` are untrusted; a corrupt CSR must never OOB-write the scratch row or OOB-slice data. |
| untrusted Model → score_per_tree | `leaf_vector_shape` drives the per-tree third dim; a malformed shape must not OOB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | sparse `col_ind[i] >= num_feature` | mitigate | Bounds-check each `col_ind[i] < num_feature` → `GtilError::SparseColumnOutOfBounds` before the scratch write (never an OOB write). |
| T-05-10 | Tampering / DoS | non-monotonic / out-of-range `row_ptr` | mitigate | Validate `row_ptr` monotone non-decreasing and `row_ptr[num_row] <= data.len()` and `row_ptr.len() == num_row+1` → `GtilError::SparseRowPtrInvalid` (no OOB slice). |
| T-05-11 | Tampering | malformed `leaf_vector_shape` in `score_per_tree` | mitigate | Bounds-safe leaf-vector access (reuse `LeafVectorTooShort`/`has_leaf_vector`) extended to the per-tree kind. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: nothing to install). |
</threat_model>

<verification>
- `cargo test -p treelite-gtil` green: sparse NaN-fill, dense==sparse parity, malformed-CSR typed errors, leaf_id, score_per_tree (scalar + leaf-vector).
- `cargo test --workspace` green — no regression on dense/binary paths.
- grep checks: `predict_sparse` present, `SparseCsr` present, both sparse error variants present, all 4 PredictKind variants dispatched.
</verification>

<success_criteria>
- Sparse CSR predict materializes absent entries as NaN and shares `evaluate_tree` with dense (GTIL-02), with dense==sparse parity proven (D-04, SC1).
- All 4 predict kinds work (GTIL-03); LeafId/ScorePerTree write raw leaf data with no postprocess/average/base-score.
- Malformed CSR is rejected with typed errors, never OOB/panic.
</success_criteria>

<output>
Create `.planning/phases/05-full-scalar-gtil-equivalence-harness/05-04-SUMMARY.md` when done.
</output>
