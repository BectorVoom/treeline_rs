---
phase: 09-memory-efficiency-hardening
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - crates/treelite-harness/src/bin/memory_report.rs
  - crates/treelite-harness/src/memory.rs
  - crates/treelite-harness/src/lib.rs
  - docs/MEMORY_REPORT.md
autonomous: true
requirements: [MEM-03]

must_haves:
  truths:
    - "jemalloc and mimalloc each build + run on Linux as #[global_allocator] in the memory_report bin (D-09)"
    - "the bin samples peak RSS / bytes-allocated and writes a committed docs/MEMORY_REPORT.md (D-10)"
    - "MEMORY_REPORT.md carries an 'Observational — NOT a CI gate' banner (D-10) + provenance from Manifest"
    - "the abi3 wheel (treelite-py) still has ZERO allocator deps (D-08)"
    - "v5 serializer still emits byte-identical golden_v5.bin / golden_v5_3format.bin"
    - "full equivalence harness green within 1e-5; cargo test --workspace + pytest pass (D-11)"
  artifacts:
    - path: "crates/treelite-harness/src/bin/memory_report.rs"
      provides: "Allocator-gated RSS sampler driving the report write"
      contains: "global_allocator"
    - path: "crates/treelite-harness/src/memory.rs"
      provides: "RSS/allocated sampler + markdown report writer (report.rs-structured)"
      contains: "fn"
    - path: "docs/MEMORY_REPORT.md"
      provides: "Committed observational before/after memory report"
      contains: "Observational"
  key_links:
    - from: "crates/treelite-harness/src/bin/memory_report.rs"
      to: "tikv_jemalloc_ctl::{epoch, stats} / /proc/self/statm"
      via: "epoch.advance() then stats::{allocated,resident}::read() (jemalloc) or statm field 2 (mimalloc/system)"
      pattern: "epoch"
    - from: "crates/treelite-harness/src/memory.rs"
      to: "crates/treelite-harness/src/manifest.rs Manifest"
      via: "report header provenance (os/arch/rustc)"
      pattern: "Manifest"
---

<objective>
MEM-03: wire jemalloc and mimalloc as runtime-selectable `#[global_allocator]`s — ONLY in the new
`memory_report` bin, behind the non-default mutually-exclusive features from Plan 01 (D-07/D-08) —
and flesh out the bin to sample peak RSS / bytes-allocated across a small benchmark model set, then
write a committed observational `docs/MEMORY_REPORT.md` (D-10, modeled on the Phase-7
`GPU_EQUIVALENCE_REPORT.md` precedent). Validate that both allocators build + run on the Linux/ROCm
dev box (D-09) and that the abi3 wheel still pulls ZERO allocator deps (D-08). This is the final
vertical slice: a runnable memory report + the proof that the allocator never leaks into the wheel,
with both hard invariants still green.

Purpose: closes MEM-03 and produces the committed evidence (D-10) that the hardening worked, without
a brittle CI threshold (the 1e-5 + golden green floor remains the real gate, D-11).
Output: filled-in memory_report bin + memory.rs sampler/writer, committed docs/MEMORY_REPORT.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-memory-efficiency-hardening/09-CONTEXT.md
@.planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md
@.planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md
@.planning/phases/09-memory-efficiency-hardening/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RSS/allocated sampler + report writer (memory.rs)</name>
  <files>crates/treelite-harness/src/memory.rs, crates/treelite-harness/src/lib.rs</files>
  <read_first>
    - crates/treelite-harness/src/report.rs (lines 186 `render_markdown`, 256 `render_json`, 281 `emit` — the exact report-writer structure to copy; the "Observational — NOT a CI gate" banner style ~200-204)
    - crates/treelite-harness/src/manifest.rs (line 36 `Manifest` struct — provenance os/arch/rustc/treelite to reuse in the header)
    - crates/treelite-harness/tests/gtil_matrix_gpu.rs (lines 111-115 `docs_path` helper; 330 the `#[ignore = "...regenerate docs/..."]` style; 492-493 the `emit(...)` caller)
    - .planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md (§NEW memory_report.rs 342-377: report structure, columns, RSS sampling method)
    - .planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md (lines 176-177 stats flow; Pattern 4/5 RSS sampling; Pitfall 5 epoch-before-read)
  </read_first>
  <action>
    Create `crates/treelite-harness/src/memory.rs` mirroring `report.rs`'s structure. Define a row struct
    (e.g. `MemRow { model: String, allocator: String, peak_rss_bytes: u64, bytes_allocated: u64, method: String }`)
    and `render_markdown(rows: &[MemRow], manifest: &Manifest) -> String` using `use std::fmt::Write as _;` +
    `writeln!` to build the table with the RESEARCH §MEM-03 columns: `Model | Allocator | peak resident (RSS) |
    bytes allocated | measurement method`, PLUS a `size_of::<Model>()` before/after row and a "1e-5 + golden
    green" attestation line. The header MUST carry Manifest provenance (os/arch/rustc) and the verbatim
    "Observational — NOT a CI gate" banner (copy report.rs:200-204 style, D-10). Add an `emit(rows, manifest,
    path) -> anyhow::Result<()>` that does `std::fs::create_dir_all(parent)` then `std::fs::write(path, md)`
    (report.rs:281-305 form). Provide a `sample_rss()` helper abstracted over allocator: jemalloc rows read
    via `tikv_jemalloc_ctl::{epoch, stats}` — call `epoch::advance()` (or the `epoch::mib()` read) BEFORE each
    `stats::allocated::read()` / `stats::resident::read()` (Pitfall 5: stats are epoch-cached); mimalloc + the
    default-system rows read `/proc/self/statm` field 2 × page size for RSS. Be explicit per-row about the
    method string. Gate the jemalloc-ctl calls behind `#[cfg(feature = "jemalloc")]` so the mimalloc/default
    builds compile without `tikv-jemalloc-ctl`. Export the module from `src/lib.rs` (`pub mod memory;`) using
    `anyhow` (binary/harness context, per CLAUDE.md). Do NOT install a `#[global_allocator]` in `lib.rs` or any
    `pub fn` — the static lives ONLY in the bin (Task 2).
  </action>
  <verify>
    <automated>cargo build -p treelite-harness && cargo build -p treelite-harness --features jemalloc && cargo build -p treelite-harness --features mimalloc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-harness` exits 0 (default, no allocator — statm path compiles)
    - `cargo build -p treelite-harness --features jemalloc` exits 0 (jemalloc-ctl stats path compiles)
    - `cargo build -p treelite-harness --features mimalloc` exits 0
    - `grep -c "Observational" crates/treelite-harness/src/memory.rs` >= 1 (banner present in render_markdown)
    - `grep -c "Manifest" crates/treelite-harness/src/memory.rs` >= 1 (provenance header)
    - no `#[global_allocator]` in src/memory.rs or src/lib.rs
  </acceptance_criteria>
  <done>memory.rs samples RSS/allocated (jemalloc-ctl epoch-then-read; statm otherwise) and renders the observational markdown with Manifest provenance + the NOT-a-CI-gate banner; compiles under all three feature builds; no global_allocator in any pub-fn crate.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the memory_report bin + generate committed docs/MEMORY_REPORT.md</name>
  <files>crates/treelite-harness/src/bin/memory_report.rs, docs/MEMORY_REPORT.md</files>
  <read_first>
    - crates/treelite-harness/src/bin/memory_report.rs (the Plan-01 skeleton: the 3 cfg-gated #[global_allocator] arms + compile_error guard)
    - crates/treelite-harness/src/memory.rs (the sampler + emit from Task 1)
    - crates/treelite-harness/tests/gtil_matrix_gpu.rs (111-115 docs_path; 492-493 emit caller — the load→sample→emit flow to mirror)
    - crates/treelite-harness/src/lib.rs (the existing RunnerCase / model-loading helpers to drive a small benchmark model set)
    - .planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md (§NEW memory_report.rs 342-377)
    - .planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md (D-10 report intent; benchmark model set is Claude's discretion — pick a representative small set, e.g. one XGBoost + one LightGBM + one sklearn model already wired in the harness)
  </read_first>
  <action>
    Flesh out `memory_report.rs` `fn main() -> anyhow::Result<()>`: load a small representative benchmark
    model set (Claude's discretion per D-06/D-10 — reuse the harness's existing model-loading helpers; one
    XGBoost-JSON + one LightGBM + one sklearn model is enough), run a prediction pass over each to drive
    allocation, sample RSS/bytes-allocated via `memory::sample_rss()` (the active allocator is whichever
    feature is enabled — the report row's `allocator` column reflects it), build the `MemRow` set including
    a `size_of::<Model>()` row, build a `Manifest`, then call `memory::emit(&rows, &manifest,
    &docs_path("MEMORY_REPORT.md"))`. Use the `docs_path` helper form from gtil_matrix_gpu.rs:111-115
    (`env!("CARGO_MANIFEST_DIR").join("../../docs")`). Print a short summary to stdout. The bin must run under
    `--features jemalloc` AND `--features mimalloc` (each writes the report rows for that allocator) AND under
    the default (system allocator via statm). Then RUN it once under each allocator to generate the committed
    `docs/MEMORY_REPORT.md` showing the before/after (system vs jemalloc vs mimalloc) narrative — append/merge
    rows so the committed file shows all three. Include the verbatim attestation line that the 1e-5 harness +
    golden byte-compare are green (D-11 evidence). Commit `docs/MEMORY_REPORT.md`. Do NOT add a brittle CI
    threshold assertion (D-10) — the report is observational; the gate is the 1e-5/golden green floor.
  </action>
  <verify>
    <automated>cargo run -p treelite-harness --bin memory_report --features jemalloc && cargo run -p treelite-harness --bin memory_report --features mimalloc && test -f docs/MEMORY_REPORT.md && grep -q Observational docs/MEMORY_REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p treelite-harness --bin memory_report --features jemalloc` exits 0 and writes docs/MEMORY_REPORT.md
    - `cargo run -p treelite-harness --bin memory_report --features mimalloc` exits 0
    - `cargo run -p treelite-harness --bin memory_report` (default/system allocator) exits 0
    - `docs/MEMORY_REPORT.md` exists, contains the "Observational" banner, rows for jemalloc + mimalloc + system, a size_of::<Model>() row, and the 1e-5/golden attestation line
    - `cargo build -p treelite-harness --bin memory_report --features jemalloc,mimalloc` FAILS (compile_error — mutual exclusion holds)
  </acceptance_criteria>
  <done>memory_report bin loads a benchmark set, samples RSS under each allocator, and writes the committed observational docs/MEMORY_REPORT.md; jemalloc + mimalloc both build+run on Linux (D-09); both-on still a compile_error.</done>
</task>

<task type="auto">
  <name>Task 3: Final invariant + wheel-isolation gate</name>
  <files>docs/MEMORY_REPORT.md</files>
  <read_first>
    - crates/treelite-harness/tests/golden_v5.rs (the golden gate)
    - .planning/phases/09-memory-efficiency-hardening/09-VALIDATION.md (§Per-Task Verification Map MEM-03 rows; §Manual-Only Verifications — the RSS narrative; D-11 floor)
    - crates/treelite-py/Cargo.toml (confirm no allocator dep crept into the wheel build graph)
    - MEMORY.md note: pytest via `uv run pytest` on the main tree
  </read_first>
  <action>
    Final gate for the phase. Confirm the abi3 wheel isolation (D-08): `cargo tree -p treelite-py` MUST
    show NO `jemalloc`/`mimalloc` node — the allocator deps are optional on the harness only and never
    reachable from the wheel. Re-run both hard invariants: the v5 golden byte-compare over BOTH fixtures,
    the full equivalence/matrix harness within 1e-5, the full workspace suite, and `uv run pytest
    crates/treelite-py`. Confirm `cargo test -p treelite-core --test model_invariants` still passes. The
    committed `docs/MEMORY_REPORT.md` is the observational evidence (D-10); the green floor here is the real
    pass/fail (D-11). If any allocator dep appears in the treelite-py tree, that is a D-08 violation — stop
    and move the dep back behind the harness feature gate.
  </action>
  <verify>
    <automated>test -z "$(cargo tree -p treelite-py 2>/dev/null | grep -E 'jemalloc|mimalloc')" && cargo test -p treelite-harness --test golden_v5 && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo tree -p treelite-py | grep -E "jemalloc|mimalloc"` prints nothing (D-08 wheel isolation)
    - `cargo test -p treelite-harness --test golden_v5` exits 0 (both fixtures byte-identical)
    - `cargo test -p treelite-harness` exits 0 (1e-5 equivalence/matrix — D-11)
    - `cargo test --workspace` exits 0
    - `uv run pytest crates/treelite-py` passes
    - `docs/MEMORY_REPORT.md` is committed
  </acceptance_criteria>
  <done>Wheel stays allocator-free; both hard invariants green (golden byte-identical + 1e-5/workspace/pytest); MEMORY_REPORT.md committed. MEM-03 closed and Phase 9 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build graph → abi3 wheel | The allocator crates must remain unreachable from `treelite-py` (footprint + supply-chain) |
| vendored *-sys C build → host | tikv-jemalloc-sys / libmimalloc-sys compile a vendored C allocator via build.rs (cc, no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Tampering | allocator deps reaching abi3 wheel | mitigate | Allocator crates are `optional = true` on `treelite-harness` ONLY, behind non-default features; the `#[global_allocator]` static lives ONLY in the `memory_report` bin (never a `pub fn`/lib). Verified by `cargo tree -p treelite-py \| grep -E "jemalloc\|mimalloc"` printing nothing (Task 3, blocking) |
| T-09-14 | Tampering | vendored *-sys C build supply chain | mitigate | Pinned versions (RESEARCH §Package Legitimacy Audit, all Approved); the `*-sys` crates do a local `cc` build with no network postinstall; maintained TiKV/Microsoft sources |
| T-09-15 | Denial of Service | both-allocator misconfiguration | mitigate | `#[cfg(all(feature="jemalloc", feature="mimalloc"))] compile_error!` prevents two `#[global_allocator]` statics (a hard build error, not a runtime fault) |
| T-09-16 | Information Disclosure | committed MEMORY_REPORT.md | N/A | Report contains only os/arch/rustc provenance + RSS numbers; no secrets/PII |
| T-09-17 | Repudiation | — | N/A | No audit/identity requirement; observational doc only |
</threat_model>

<verification>
- HARD INVARIANT 1: `cargo test -p treelite-harness --test golden_v5` green (both fixtures byte-identical)
- HARD INVARIANT 2: `cargo test --workspace` + `cargo test -p treelite-harness` (1e-5) + `uv run pytest crates/treelite-py` green
- D-08 wheel isolation: `cargo tree -p treelite-py | grep -E "jemalloc|mimalloc"` empty
- D-09: jemalloc + mimalloc each build+run on Linux in the memory_report bin
- D-10: committed docs/MEMORY_REPORT.md with observational banner + before/after rows
</verification>

<success_criteria>
- jemalloc + mimalloc runtime-selectable as #[global_allocator] in the memory_report bin only (D-07/D-08), both build+run on Linux (D-09)
- committed observational docs/MEMORY_REPORT.md with RSS/allocated rows + size_of::<Model>() + NOT-a-CI-gate banner (D-10)
- abi3 wheel has zero allocator deps (D-08)
- HARD INVARIANT 1 + 2 both green
- MEM-03 requirement closed; Phase 9 complete
</success_criteria>

<output>
Create `.planning/phases/09-memory-efficiency-hardening/09-04-SUMMARY.md` when done
</output>
