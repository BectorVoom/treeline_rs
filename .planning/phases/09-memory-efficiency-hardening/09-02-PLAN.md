---
phase: 09-memory-efficiency-hardening
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - crates/treelite-core/src/model.rs
  - crates/treelite-core/src/serialize/mod.rs
  - crates/treelite-builder/src/lib.rs
  - crates/treelite-builder/src/bulk.rs
  - crates/treelite-builder/src/concat.rs
  - crates/treelite-xgboost/src/lib.rs
  - crates/treelite-lightgbm/src/lib.rs
  - crates/treelite-sklearn/src/bulk.rs
  - crates/treelite-core/tests/model_invariants.rs
autonomous: true
requirements: [MEM-02]

must_haves:
  truths:
    - "Model metadata fields are SmallVec/CompactString-backed (per D-04) and deref byte-identically"
    - "builder::Metadata fields migrated in lockstep so assign blocks need no per-field churn"
    - "All loaders (xgboost/lightgbm/sklearn) write the migrated fields and compile"
    - "GTIL shape.rs needs no edit (slice/iter API deref-transparent) and stays green"
    - "treelite-py needs no edit (no metadata getter exists) and pytest stays green"
    - "v5 serializer still emits byte-identical golden_v5.bin / golden_v5_3format.bin (D-03/D-05)"
    - "full equivalence harness green within 1e-5; cargo test --workspace + pytest pass (D-11)"
    - "Model stays !Send; size_of::<Model>() stays within the model_invariants budget"
  artifacts:
    - path: "crates/treelite-core/src/model.rs"
      provides: "7 metadata fields as SmallVec<[i32;N]>/SmallVec<[f64;N]>/CompactString"
      contains: "SmallVec"
    - path: "crates/treelite-builder/src/lib.rs"
      provides: "Metadata struct fields migrated in lockstep with Model"
      contains: "SmallVec"
  key_links:
    - from: "crates/treelite-core/src/model.rs"
      to: "crates/treelite-core/src/serialize/mod.rs emit path"
      via: "Deref<[T]> / Deref<str> through le_bytes_of / b.string — UNCHANGED emit"
      pattern: "le_bytes_of"
    - from: "loaders (xgboost/lightgbm/sklearn)"
      to: "builder::Metadata"
      via: ".into() / smallvec![] at the BuilderMetadata literal sites"
      pattern: "BuilderMetadata"
---

<objective>
MEM-02: migrate the seven public `Model` metadata fields from `Vec<i32>`/`Vec<f64>`/`String`
to `SmallVec<[i32; N]>`/`SmallVec<[f64; N]>`/`CompactString` (D-04), and the matching
`builder::Metadata` fields in lockstep, updating every ripple site (serializer read-back,
bulk, concat, the three loaders) so the workspace compiles. The serializer EMIT path is
untouched (deref-transparent — PATTERNS lines 53-67) and the read-back path takes a `.into()`
at each assign. This is a single vertical slice: storage type changes, zero behavior change,
both hard invariants stay green before the plan ends.

Purpose: closes MEM-02. The slice is "swap the field types and prove the bytes/predictions
did not move" — the golden byte-compare (D-03/D-05) and the 1e-5 harness (D-11) are the gates.
Output: SmallVec/CompactString-backed Model + Metadata; all consumers updated; tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-memory-efficiency-hardening/09-CONTEXT.md
@.planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md
@.planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md
@.planning/phases/09-memory-efficiency-hardening/09-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate Model + builder::Metadata field types (the source of the ripple)</name>
  <files>crates/treelite-core/src/model.rs, crates/treelite-builder/src/lib.rs, crates/treelite-builder/src/bulk.rs, crates/treelite-builder/src/concat.rs</files>
  <read_first>
    - crates/treelite-core/src/model.rs (lines 68-84 field decls; 118-126 `new()` initializers — the in-place type swap)
    - crates/treelite-builder/src/lib.rs (Metadata struct 59-71; assign block 768-774; `expected_num_tree` 212; leaf_vector_shape product 208)
    - crates/treelite-builder/src/bulk.rs (assign block 229-235 — byte-identical to lib.rs 768-774)
    - crates/treelite-builder/src/concat.rs (clone/!=/extend_from_slice 79-150; Vec<i32> locals 90-91; assigns 149-150)
    - .planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md (§model.rs lines 160-191; §builder lib.rs 195-225; §bulk.rs 227-232; §concat.rs 236-258 — exact inline-N + minimal-change guidance)
    - .planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md (§MEM-02 inline-capacity rationale)
  </read_first>
  <behavior>
    - `size_of::<Model>()` stays under the Plan-01 `model_invariants` budget after the swap (Pitfall 2).
    - `Model` stays `!Send` (the `TreeBuf::Borrowed(*const T)` keeps it so; SmallVec/CompactString are Send-neutral).
    - `cargo test -p treelite-core` and `cargo test -p treelite-builder` stay green (existing builder/serialize roundtrip tests prove field semantics unchanged).
  </behavior>
  <action>
    Add `use smallvec::SmallVec;` and `use compact_str::CompactString;` to treelite-core/model.rs and
    declare the workspace deps (`smallvec = { workspace = true }`, `compact_str = { workspace = true }`)
    in treelite-core/Cargo.toml and treelite-builder/Cargo.toml. In `model.rs` swap the 7 field types
    per PATTERNS lines 176-182: `num_class: SmallVec<[i32; 1]>`, `leaf_vector_shape: SmallVec<[i32; 2]>`,
    `target_id: SmallVec<[i32; 1]>`, `class_id: SmallVec<[i32; 1]>`, `base_scores: SmallVec<[f64; 1]>`,
    `postprocessor: CompactString`, `attributes: CompactString`. Update `Model::new()` initializers
    (lines 118-126): `Vec::new()` → `SmallVec::new()`, `String::new()` → `CompactString::new("")` (or
    `CompactString::default()`). In `builder/lib.rs` migrate the `Metadata` struct fields (59-71) to the
    SAME SmallVec/CompactString types so the assign block 768-774 stays verbatim; line 774 becomes
    `metadata.attributes.unwrap_or_else(|| CompactString::from("{}"))` (keep `attributes: Option<CompactString>`).
    `expected_num_tree = metadata.target_id.len()` and the `leaf_vector_shape.iter()...product()` work
    unchanged (SmallVec shares the slice API). In `bulk.rs` apply the identical line-235 change
    (`CompactString::from("{}")`); 229-234 stay verbatim. In `concat.rs` the `.clone()`, `!=`, and
    `extend_from_slice(&m.target_id)` calls all work unchanged; change only the two `Vec<i32>` locals
    (90-91) to `SmallVec<[i32; 1]>` (or keep `Vec` locals and `.into()` at the 149-150 assigns — pick
    the form with the least churn). Do NOT add the smallvec `serde` feature (RESEARCH line 116 — no serde
    boundary). Do NOT change any field's serialized representation — SmallVec/CompactString deref to the
    identical payload (this is verified by the golden gate in Task 3).
  </action>
  <verify>
    <automated>cargo test -p treelite-core && cargo test -p treelite-builder</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-core -p treelite-builder` exits 0
    - `cargo test -p treelite-core` exits 0 (includes serialize_roundtrip + model_invariants size/!Send)
    - `cargo test -p treelite-builder` exits 0
    - `grep -c "SmallVec" crates/treelite-core/src/model.rs` >= 5
    - `grep -c "CompactString" crates/treelite-core/src/model.rs` >= 2
    - smallvec dep is on the 1.x line in both crate Cargo.toml files (no 2.0)
  </acceptance_criteria>
  <done>Model + Metadata fields are SmallVec/CompactString; new()/assign blocks updated with minimal churn; core + builder tests green; size_of budget + !Send invariants hold.</done>
</task>

<task type="auto">
  <name>Task 2: Update loaders + serializer read-back to the migrated types</name>
  <files>crates/treelite-core/src/serialize/mod.rs, crates/treelite-xgboost/src/lib.rs, crates/treelite-lightgbm/src/lib.rs, crates/treelite-sklearn/src/bulk.rs</files>
  <read_first>
    - crates/treelite-core/src/serialize/mod.rs (EMIT path 102-114 — confirm NO edit needed; read-back assigns 355-363 — wrap `.into()`; reads 303-311 still return Vec/String)
    - crates/treelite-xgboost/src/lib.rs (literal sites 193, 207-209, 222, 248-260 BuilderMetadata)
    - crates/treelite-lightgbm/src/lib.rs (342-364 BuilderMetadata literal)
    - crates/treelite-sklearn/src/bulk.rs (272-277 regressor + 366-371 multiclass BuilderMetadata literals)
    - crates/treelite-gtil/src/shape.rs (61-77 — READ-ONLY: confirm `.first()/.get()/.iter()` deref-transparent, no edit)
    - .planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md (§serialize read-back 132-157; §xgboost 262-279; §lightgbm 282-290; §sklearn 293-301; §gtil shape NO-CHANGE 305-314)
  </read_first>
  <action>
    In `serialize/mod.rs` leave the EMIT block (102-114) UNCHANGED — `SmallVec` derefs to `&[i32]` and
    `CompactString` to `&str`, so `le_bytes_of(&m.num_class)` / `b.string(&m.postprocessor)` compile and
    emit identical bytes (PATTERNS 53-67). At the read-back assigns (355-363) the `r.array(...)` /
    `r.string()` calls still return `Vec`/`String` (lines 303-311), so wrap each of the 7 assigns with
    `.into()`: `model.num_class = num_class.into();` etc. (`SmallVec: From<Vec<T>>` and
    `CompactString: From<String>` exist). In the three loaders, the `BuilderMetadata` literal fields now
    expect SmallVec/CompactString (Metadata migrated in Task 1): wrap the `Vec`-producing exprs with
    `.into()` and the string literals with `.into()`. xgboost (PATTERNS 262-279): `vec![num_class_param].into()`,
    `vec![0; num_tree].into()`, `booster.tree_info.clone().into()`, `leaf_vector_shape: vec![1,1].into()`
    (or `smallvec![1,1]`), `postprocessor: get_postprocessor(objective)?.into()`, base_scores `.into()`.
    lightgbm (342-364): `.into()` on the Vec locals/`vec![]` literals; `postproc.postprocessor.to_string()`
    → `postproc.postprocessor.into()` (or `.to_string().into()`). sklearn/bulk.rs (272-277, 366-371): both
    `BuilderMetadata` literals — `.into()` on each `vec![]`/`.to_vec()`, and `"identity".to_string()` /
    `"identity_multiclass".to_string()` → `.into()`. Do NOT edit `treelite-gtil/src/shape.rs` — verify it
    compiles unchanged (the slice/iter API derefs through SmallVec). Do NOT edit `treelite-py` (no metadata
    getter exists — D-05 is forward-looking; PATTERNS 318-329).
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p treelite-xgboost -p treelite-lightgbm -p treelite-sklearn -p treelite-gtil</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0
    - `cargo test -p treelite-xgboost -p treelite-lightgbm -p treelite-sklearn -p treelite-gtil` exits 0
    - `git diff --stat crates/treelite-gtil/src/shape.rs` shows NO change (deref-transparent)
    - `git diff --stat crates/treelite-py/` shows NO change (no metadata getter touched)
    - serialize/mod.rs EMIT block (102-114) unchanged; read-back assigns carry `.into()`
  </acceptance_criteria>
  <done>All loaders + the serializer read-back compile against the migrated types; gtil shape.rs and treelite-py untouched; whole workspace builds.</done>
</task>

<task type="auto">
  <name>Task 3: Prove both invariants green — golden byte-compare + 1e-5 + pytest</name>
  <files>crates/treelite-core/tests/model_invariants.rs</files>
  <read_first>
    - crates/treelite-harness/tests/golden_v5.rs (the byte-for-byte golden gate — `serializer_reproduces_golden_v5_byte_for_byte` at line 62; the 3-format variant)
    - crates/treelite-core/tests/model_invariants.rs (the Plan-01 size_of budget + !Send check — re-confirm it still passes post-swap)
    - .planning/phases/09-memory-efficiency-hardening/09-VALIDATION.md (§Per-Task Verification Map MEM-02 rows; D-11 floor)
    - MEMORY.md note: pytest runs via `uv run pytest` on the main tree (venv untracked)
  </read_first>
  <action>
    This is the MEM-02 gate task — no production edit beyond confirming/adjusting the size budget.
    Run the two hard-invariant gates and the equivalence harness. If `model_invariants`'
    `size_of::<Model>()` budget is now exceeded because the chosen inline N's bloated `Model`, that is a
    Pitfall-2 signal: reduce the inline N (e.g. `target_id`/`class_id` to the dominant shape) rather than
    raising the budget, then re-run — the budget is the guard, not a knob to relax. Confirm `Model` is
    still `!Send` (the documented check in model_invariants). Run the v5 golden byte-compare (BOTH
    golden_v5.bin and golden_v5_3format.bin), the full equivalence/matrix harness within 1e-5, the full
    workspace test suite, and the Python suite via `uv run pytest crates/treelite-py` (the list/str
    accessor contract — D-05). All MUST be green; any byte divergence in the golden compare means a field's
    deref is NOT byte-identical and the migration is wrong (stop and fix the offending field).
  </action>
  <verify>
    <automated>cargo test -p treelite-harness --test golden_v5 && cargo test -p treelite-harness && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness --test golden_v5` exits 0 (golden_v5.bin AND golden_v5_3format.bin byte-identical — D-03/D-05)
    - `cargo test -p treelite-harness` exits 0 (equivalence + gtil_matrix within 1e-5 — D-11)
    - `cargo test --workspace` exits 0
    - `uv run pytest crates/treelite-py` passes (list/str accessor contract preserved — D-05)
    - `cargo test -p treelite-core --test model_invariants` exits 0 (size_of within budget; !Send documented)
    - `cargo tree -p treelite-py | grep -E "jemalloc|mimalloc"` still prints nothing
  </acceptance_criteria>
  <done>Both hard invariants green: golden byte-identical (both fixtures) + 1e-5 harness + workspace + pytest. MEM-02 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v5 deserialize Reader → Model | Untrusted blob read-back; this plan only wraps `.into()` at assign sites, the element-wise bounds-checked decode (binary.rs) is unchanged |
| Python ↔ Rust accessors | list/str contract must hold; no getter touched this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-D | Denial of Service | v5 deserialize read path | mitigate | MEM-02 does NOT add any `cast_slice` over untrusted input — the read-back uses the existing element-wise bounds-checked `Reader::array`/`Reader::string` and only `.into()`-converts the resulting Vec/String. No new panic surface (RESEARCH Pitfall 4 / V5) |
| T-09-05 | Tampering | wire bytes | mitigate | SmallVec/CompactString deref to identical payload; golden byte-compare on BOTH fixtures is the gate (Task 3) |
| T-09-06 | Tampering | abi3 wheel footprint | mitigate | No allocator dep added; `cargo tree -p treelite-py` grep stays empty (Task 3 acceptance) |
| T-09-07 | Information Disclosure | — | N/A | Internal numerical types only; no PII/network/auth |
| T-09-08 | Spoofing | — | N/A | No identity/auth surface in this phase |
</threat_model>

<verification>
- HARD INVARIANT 1: `cargo test -p treelite-harness --test golden_v5` green — golden_v5.bin AND golden_v5_3format.bin byte-identical
- HARD INVARIANT 2: `cargo test --workspace` + `cargo test -p treelite-harness` (1e-5) + `uv run pytest crates/treelite-py` all green
- `cargo test -p treelite-core --test model_invariants` green (size_of budget + !Send)
- gtil shape.rs and treelite-py have zero diff
</verification>

<success_criteria>
- 7 Model fields + builder::Metadata fields are SmallVec/CompactString (D-04)
- serializer EMIT path unchanged; read-back assigns carry `.into()`
- all loaders compile and write the migrated fields
- HARD INVARIANT 1 + 2 both green (golden byte-identical + 1e-5/workspace/pytest)
- Model stays !Send; size_of within budget
- MEM-02 requirement closed
</success_criteria>

<output>
Create `.planning/phases/09-memory-efficiency-hardening/09-02-SUMMARY.md` when done
</output>
