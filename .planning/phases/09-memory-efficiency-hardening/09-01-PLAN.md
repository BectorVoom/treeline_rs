---
phase: 09-memory-efficiency-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - crates/treelite-harness/Cargo.toml
  - crates/treelite-harness/src/bin/memory_report.rs
  - crates/treelite-core/tests/model_invariants.rs
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03]

must_haves:
  truths:
    - "The 5 new crates are pinned in [workspace.dependencies] and resolve via cargo add --dry-run"
    - "harness has non-default, mutually-exclusive jemalloc/mimalloc features that build"
    - "abi3 wheel (treelite-py) has ZERO allocator deps in its tree"
    - "model_invariants test asserts size_of::<Model>() bound and documents !Send (compiles + passes)"
    - "v5 serializer still emits byte-identical golden_v5.bin / golden_v5_3format.bin"
    - "full equivalence harness green within 1e-5; cargo test --workspace + pytest pass"
  artifacts:
    - path: "crates/treelite-harness/src/bin/memory_report.rs"
      provides: "Allocator-gated bin skeleton with #[global_allocator] cfg arms + compile_error guard"
      contains: "global_allocator"
    - path: "crates/treelite-core/tests/model_invariants.rs"
      provides: "size_of::<Model>() budget test + documented !Send invariant"
      contains: "size_of::<Model>"
    - path: "crates/treelite-harness/Cargo.toml"
      provides: "jemalloc/mimalloc non-default mutually-exclusive features + optional deps"
      contains: "jemalloc"
  key_links:
    - from: "crates/treelite-harness/Cargo.toml"
      to: "tikv-jemallocator, tikv-jemalloc-ctl, mimalloc"
      via: "optional workspace deps gated behind features"
      pattern: "optional = true"
    - from: "Cargo.toml"
      to: "smallvec, compact_str, tikv-jemallocator, tikv-jemalloc-ctl, mimalloc"
      via: "[workspace.dependencies] pins"
      pattern: "smallvec"
---

<objective>
Wave 0 scaffolding for Phase 9. Add the 5 new pinned workspace dependencies, wire the
harness allocator features + the allocator-gated `memory_report` bin skeleton, and create
the `model_invariants` sanity test (`size_of::<Model>()` budget + documented `!Send`). This
plan introduces NO behavioral change to the model/serializer — it only lands the dependency
table, feature flags, and the Wave-0 test/bench scaffold that MEM-02 / MEM-01 / MEM-03 build on.

Purpose: every downstream task (MEM-02 field swap, MEM-01 recast, MEM-03 allocator report)
depends on these crates and the `memory_report` target existing. Per RESEARCH sequencing this
is the prerequisite wave; the VALIDATION.md "Wave 0 Requirements" list is the checklist.
Output: pinned deps, harness features, `memory_report.rs` skeleton, `model_invariants.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-memory-efficiency-hardening/09-CONTEXT.md
@.planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md
@.planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md
@.planning/phases/09-memory-efficiency-hardening/09-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Phase 9 creates these NEW symbols (drift-verification must EXCLUDE them as newly-created, not pre-existing):
- `[workspace.dependencies]` entries: `smallvec` (1.15.1), `compact_str` (0.9.1), `tikv-jemallocator` (0.7.0), `tikv-jemalloc-ctl` (0.7.0), `mimalloc` (0.1.52)
- Cargo features on `treelite-harness`: `jemalloc`, `mimalloc` (non-default, mutually exclusive)
- New bin target: `crates/treelite-harness/src/bin/memory_report.rs` (binary name `memory_report`)
- Optional helper: `crates/treelite-harness/src/memory.rs` (RSS sampler/markdown writer; created in this plan or Plan 04)
- New test file: `crates/treelite-core/tests/model_invariants.rs` with tests `model_size_not_bloated_by_smallvec` and the documented `_assert_not_send` check
- New committed doc: `docs/MEMORY_REPORT.md` (written by Plan 04)
- Type changes (Plan 02, not new symbols but new field types): `Model` and `builder::Metadata` fields become `SmallVec<[i32; N]>` / `SmallVec<[f64; N]>` / `CompactString`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pin the 5 new workspace dependencies</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (the `[workspace.dependencies]` table — `bytemuck` entry at line 27 is the in-place add analog)
    - .planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md (lines 86-118: exact version pins + feature notes; lines 120-136: legitimacy audit — all 5 Approved)
    - .planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md (§"Workspace dependency declaration", lines 87-89)
  </read_first>
  <action>
    Add five entries to the `[workspace.dependencies]` table in the root `Cargo.toml`, alongside
    the existing `bytemuck` pin. Use the exact pins from RESEARCH §Package Legitimacy Audit:
    `smallvec = { version = "1.15.1", features = ["const_new", "union"] }` (NOT smallvec 2.0-alpha —
    forbidden by FND-02), `compact_str = "0.9.1"`, `tikv-jemallocator = "0.7.0"`,
    `tikv-jemalloc-ctl = "0.7.0"`, `mimalloc = "0.1.52"`. Do NOT add the `serde` feature to
    smallvec/compact_str — RESEARCH line 116 confirms the migrated fields cross no serde boundary
    (the v5 serializer is hand-framed). Before committing the versions, run `cargo add --dry-run`
    for each crate to confirm the pinned version and that the named features exist for that version;
    if `const_new`/`union` are not present for smallvec 1.15.1, drop them and note the deviation.
    Do NOT add these to any crate's `[dependencies]` yet — Plan 02 wires smallvec/compact_str into
    treelite-core/builder/loaders, Plan 04 wires the allocators into the harness. This task is the
    workspace pin table only.
  </action>
  <verify>
    <automated>cargo metadata --format-version 1 >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo add --dry-run -p treelite-core smallvec@1.15.1` resolves without error
    - `cargo metadata --format-version 1` exits 0 (workspace still resolves)
    - `grep -E "smallvec|compact_str|tikv-jemallocator|tikv-jemalloc-ctl|mimalloc" Cargo.toml` shows all 5 pins
    - smallvec pin is on the `1.x` line (NOT `2.0`)
  </acceptance_criteria>
  <done>All 5 crates pinned in [workspace.dependencies] at the verified versions; workspace metadata resolves; no crate [dependencies] touched yet.</done>
</task>

<task type="auto">
  <name>Task 2: Harness allocator features + memory_report bin skeleton</name>
  <files>crates/treelite-harness/Cargo.toml, crates/treelite-harness/src/bin/memory_report.rs</files>
  <read_first>
    - crates/treelite-harness/Cargo.toml (the existing GPU `[features]` rocm/cuda/wgpu block + `cubecl = { workspace = true, optional = true }` — the exact optional-dep + non-default feature shape to mirror)
    - .planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md (§"Allocator isolation via optional dep + non-default features", lines 69-85; and the `#[global_allocator]` static excerpt lines 364-374)
    - crates/treelite-harness/src/report.rs (lines 186, 281: `render_markdown`/`emit` signatures — the report-writer precedent the bin will call in Plan 04)
    - .planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md (lines 88-94, 107-116: allocator crate roles + version-lock note)
  </read_first>
  <action>
    In `crates/treelite-harness/Cargo.toml`, mirror the existing GPU feature/optional-dep shape one
    tier stricter. Add to `[features]`: `jemalloc = ["dep:tikv-jemallocator", "dep:tikv-jemalloc-ctl"]`
    and `mimalloc = ["dep:mimalloc"]` (both non-default; neither in `default = []`). Add three optional
    deps: `tikv-jemallocator = { workspace = true, optional = true }`,
    `tikv-jemalloc-ctl = { workspace = true, optional = true }`,
    `mimalloc = { workspace = true, optional = true }`. Do NOT touch any other crate's Cargo.toml —
    the allocator deps stay confined to the harness (D-08), and especially never reach `treelite-py`.
    Then create `crates/treelite-harness/src/bin/memory_report.rs` as a SKELETON: place the
    `#[global_allocator]` static in three cfg-gated arms exactly per PATTERNS lines 364-374 — the
    jemalloc arm `#[cfg(all(feature = "jemalloc", not(feature = "mimalloc")))]` binding
    `tikv_jemallocator::Jemalloc`, the mimalloc arm `#[cfg(all(feature = "mimalloc", not(feature = "jemalloc")))]`
    binding `mimalloc::MiMalloc`, and a `#[cfg(all(feature = "jemalloc", feature = "mimalloc"))]
    compile_error!(...)` guard for the both-on case. Give it a `fn main() -> anyhow::Result<()>` that
    for now just prints which allocator is active (cfg-gated string) and returns `Ok(())` — the real
    RSS sampling + `MEMORY_REPORT.md` write is Plan 04. Use `anyhow` (binary, per CLAUDE.md). This bin
    MUST compile under the default (no-allocator-feature) build, under `--features jemalloc`, and under
    `--features mimalloc`; only the both-on combination must fail via compile_error.
  </action>
  <verify>
    <automated>cargo build -p treelite-harness --bin memory_report && cargo build -p treelite-harness --bin memory_report --features jemalloc && cargo build -p treelite-harness --bin memory_report --features mimalloc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-harness --bin memory_report` exits 0 (default, no allocator)
    - `cargo build -p treelite-harness --bin memory_report --features jemalloc` exits 0
    - `cargo build -p treelite-harness --bin memory_report --features mimalloc` exits 0
    - `cargo build -p treelite-harness --bin memory_report --features jemalloc,mimalloc` FAILS with the compile_error message
    - `grep -c global_allocator crates/treelite-harness/src/bin/memory_report.rs` >= 2
  </acceptance_criteria>
  <done>Harness has non-default mutually-exclusive jemalloc/mimalloc features; memory_report bin builds under each single allocator and the default; both-on combination is a compile_error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: model_invariants test (!Send + size_of budget) + wheel-isolation guard</name>
  <files>crates/treelite-core/tests/model_invariants.rs</files>
  <read_first>
    - crates/treelite-core/tests/tree_buf.rs (the core integration-test idiom: `use treelite_core::...; #[test] fn ...`)
    - crates/treelite-core/src/model.rs (lines 68-84: the metadata field decls whose size feeds the budget; the `!Send` source is the `*const T` in `TreeBuf::Borrowed`)
    - .planning/phases/09-memory-efficiency-hardening/09-PATTERNS.md (§"NEW crates/treelite-core/tests/model_invariants.rs", lines 381-407 — the exact test pattern)
    - .planning/phases/09-memory-efficiency-hardening/09-RESEARCH.md (line 75: Model MUST stay !Send; Pitfall 2: over-large inline N bloats Model)
  </read_first>
  <behavior>
    - Test `model_size_not_bloated_by_smallvec`: asserts `std::mem::size_of::<Model>()` is under a fixed
      byte budget. Establish the budget NOW against the CURRENT (pre-MEM-02) `Model` by reading the
      printed size once, then set the budget to `max(current_size, 512)` so the test passes today AND
      stays a meaningful guard after Plan 02's SmallVec/CompactString swap (Plan 02 re-checks it).
    - Documented `!Send` invariant: a `_assert_not_send` fn containing a commented-out
      `requires_send::<Model>()` line that MUST NOT be uncommented (it would fail to compile). The
      live assertion is the comment + the fact that `Model` holds `TreeBuf::Borrowed(*const T)`.
  </behavior>
  <action>
    Create `crates/treelite-core/tests/model_invariants.rs` per PATTERNS lines 381-407. Write the
    `model_size_not_bloated_by_smallvec` runtime `#[test]` asserting
    `std::mem::size_of::<Model>() <= BUDGET` with a `{}` size in the failure message. Determine BUDGET
    by building/running once to read the current size, then pick a round budget at or just above it
    (the PATTERNS example uses 512 — use that unless the current size already exceeds it, in which case
    round up to the next 64-byte multiple and note why in a comment). Add the `_assert_not_send` fn
    with the documented commented-out `requires_send::<Model>()` static check. Do NOT add a `trybuild`
    dependency — the repo has none; the `!Send` half is a documented commented assertion (PATTERNS line
    416-417). This test must pass against the CURRENT model (it is the Wave-0 baseline that Plan 02's
    field swap must not break).
  </action>
  <verify>
    <automated>cargo test -p treelite-core --test model_invariants</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-core --test model_invariants` exits 0
    - `grep -c "size_of::<Model>" crates/treelite-core/tests/model_invariants.rs` >= 1
    - File contains the documented `_assert_not_send` fn with the commented `requires_send::<Model>()` line
    - `cargo test --workspace` exits 0 (no regression)
    - `cargo tree -p treelite-py | grep -E "jemalloc|mimalloc"` prints nothing
  </acceptance_criteria>
  <done>model_invariants.rs passes against the current Model with a size_of budget and documented !Send check; full workspace green; treelite-py tree has no allocator deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build graph → abi3 wheel | The `treelite-py` cdylib must never transitively pull an allocator crate (supply-chain + footprint) |
| (none new for data) | This plan adds deps/features/tests only; no new untrusted-input parsing path is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | allocator deps reaching abi3 wheel | mitigate | Allocator crates are `optional = true` on `treelite-harness` ONLY, gated behind non-default features; verified by `cargo tree -p treelite-py \| grep -E "jemalloc\|mimalloc"` printing nothing (acceptance criterion, Task 3) |
| T-09-02 | Spoofing/Tampering | new package supply chain | mitigate | All 5 crates pinned to exact versions from RESEARCH §Package Legitimacy Audit (all `[OK]`/Approved); vendored `*-sys` C builds are `cc` builds with no network postinstall |
| T-09-SC | Tampering | cargo install of new packages | mitigate | All packages Approved in RESEARCH legitimacy table (no `[ASSUMED]`/`[SUS]`/`[SLOP]`); no human checkpoint required |
| T-09-03 | Information Disclosure | — | N/A | No PII, no network, no auth surface; internal numerical library |
| T-09-04 | Denial of Service (deserialize) | — | N/A (this plan) | Deserialize read path untouched here; addressed in MEM-01 plan (T-09-D) |
</threat_model>

<verification>
- `cargo metadata --format-version 1` exits 0 (workspace resolves with new pins)
- `cargo build -p treelite-harness --bin memory_report` builds under default, `--features jemalloc`, `--features mimalloc`; fails (compile_error) under both
- `cargo test -p treelite-core --test model_invariants` exits 0
- `cargo test --workspace` exits 0
- `cargo test -p treelite-harness --test golden_v5` exits 0 (golden byte-compare untouched — invariant 1)
- `cargo tree -p treelite-py | grep -E "jemalloc|mimalloc"` prints nothing (invariant: wheel isolation)
</verification>

<success_criteria>
- 5 deps pinned in [workspace.dependencies] at verified versions (smallvec on 1.x)
- harness jemalloc/mimalloc features build (single-on), both-on is a compile_error
- memory_report bin skeleton with the 3 cfg-gated #[global_allocator] arms exists
- model_invariants.rs passes (size_of budget + documented !Send)
- HARD INVARIANT 1: golden_v5.bin / golden_v5_3format.bin still byte-identical (no model/serializer change in this plan)
- HARD INVARIANT 2: cargo test --workspace + pytest green within 1e-5
- abi3 wheel has zero allocator deps
</success_criteria>

<output>
Create `.planning/phases/09-memory-efficiency-hardening/09-01-SUMMARY.md` when done
</output>
