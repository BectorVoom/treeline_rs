---
phase: 02-builder-serialization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Cargo.toml
  - crates/treelite-builder/Cargo.toml
  - crates/treelite-builder/src/lib.rs
  - crates/treelite-builder/src/error.rs
  - crates/treelite-builder/src/concat.rs
  - crates/treelite-builder/src/bulk.rs
  - crates/treelite-builder/tests/validation.rs
  - crates/treelite-builder/tests/concat.rs
  - crates/treelite-builder/tests/bulk.rs
autonomous: true
requirements: [BLD-01, BLD-02, BLD-03]
must_haves:
  truths:
    - "A fluent ModelBuilder constructs a model node-by-node and rejects orphaned nodes, dangling child keys, duplicate keys, leaf+test mix, and negative keys with a typed error, while accepting forward-referenced child keys."
    - "ConcatenateModelObjects merges multiple same-variant models into one, extending target_id/class_id, and rejects variant/num_target/num_class/leaf_vector_shape mismatch."
    - "BulkConstructTree builds a tree from pre-validated bulk arrays, bypassing node-by-node validation by construction, producing the same tree a per-node build would."
  artifacts:
    - path: "crates/treelite-builder/src/lib.rs"
      provides: "ModelBuilder state machine (BLD-01, D-07/D-08)"
      exports: ["ModelBuilder"]
      min_lines: 120
    - path: "crates/treelite-builder/src/error.rs"
      provides: "BuilderError thiserror enum with locality fields"
      contains: "OrphanedNode"
    - path: "crates/treelite-builder/src/concat.rs"
      provides: "ConcatenateModelObjects (BLD-02)"
      contains: "fn concatenate"
    - path: "crates/treelite-builder/src/bulk.rs"
      provides: "BulkConstructTree bypass path (BLD-03, D-09)"
      contains: "fn bulk_construct_tree"
  key_links:
    - from: "crates/treelite-builder/src/lib.rs"
      to: "treelite_core::Model"
      via: "CommitModel produces a Model"
      pattern: "treelite_core::(Model|Tree|TreeBuf)"
    - from: "crates/treelite-builder/Cargo.toml"
      to: "treelite-core"
      via: "path dependency"
      pattern: "treelite-core"
---

<objective>
Create the new `treelite-builder` crate (D-10): a fluent, always-strict `ModelBuilder` state machine that validates node well-formedness at `EndNode` and tree topology (orphans, dangling child keys via forward-ref resolution) at `EndTree` (BLD-01, D-07/D-08); a `ConcatenateModelObjects` free function (BLD-02); and a `BulkConstructTree` validation-bypass fast path (BLD-03, D-09). All three semantics are ported verbatim from the vendored upstream C++ — fidelity, not invention.

Purpose: Loaders (Phase 3/4) need a validated construction target, and the XGBoost loader is rewired through it (Plan 05). This is the vertical "user can build & validate a model" slice.
Output: The `treelite-builder` crate with its three modules + error enum + three test files; root `Cargo.toml` gains the member.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-builder-serialization/02-CONTEXT.md
@.planning/phases/02-builder-serialization/02-RESEARCH.md
@.planning/phases/02-builder-serialization/02-PATTERNS.md
</context>

## Phase Goal (developer-facing user story)

**As a** treelite-rs loader developer, **I want to** a fluent strict `ModelBuilder` plus concat and a bulk fast path, **so that** I can construct a validated `Model` node-by-node (rejecting malformed topologies) or in bulk, instead of hand-assembling `Tree` columns.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the treelite-builder crate + BuilderError + the fluent ModelBuilder state machine (BLD-01)</name>
  <files>Cargo.toml, crates/treelite-builder/Cargo.toml, crates/treelite-builder/src/lib.rs, crates/treelite-builder/src/error.rs, crates/treelite-builder/tests/validation.rs</files>
  <behavior>
    - Rejects `StartNode(key)` with `key < 0` → typed error.
    - Rejects a duplicate node key (same key twice in one tree) → `DuplicateNodeKey { key }`.
    - Rejects a node that is both a leaf and a test (state machine makes the second call illegal) → typed error.
    - Rejects `NumericalTest` whose `left_child_key == right_child_key`, or where a child key equals the current node key → typed error.
    - At `EndTree`: a child key referencing a node never declared → `DanglingChildKey { key }`.
    - At `EndTree`: a declared node unreachable from root (index 0) → `OrphanedNode { key }`.
    - Accepts a tree where a parent node is declared BEFORE its children are declared (forward reference resolves at EndTree, not EndNode) — must NOT be rejected.
    - `CommitModel` before `GetNumTree() == expected_num_tree` → typed error; success returns a `treelite_core::Model`.
  </behavior>
  <read_first>
    - crates/treelite-xgboost/Cargo.toml (the exact workspace-inheritance manifest shape to copy)
    - crates/treelite-xgboost/src/error.rs (thiserror enum with locality fields + `#[error(transparent)] #[from]` core bubble-up convention)
    - crates/treelite-gtil/src/error.rs (bounds-carrying variant pattern)
    - crates/treelite-xgboost/src/lib.rs:151-207 (the column-fill → `TreeBuf::from_owned` → `Tree::new` finalize pattern EndTree reuses)
    - crates/treelite-core/src/tree.rs + crates/treelite-core/src/model.rs (the build target: Tree columns, ModelVariant, header fields)
    - crates/treelite-core/src/tests or crates/treelite-core/tests/tree_model.rs (inline Tree/Model construction test style)
    - treelite-mainline/src/model_builder/model_builder.cc:50-388 (the authoritative state machine + EndTree orphan/forward-ref resolution)
    - treelite-mainline/include/treelite/model_builder.h (the interface surface: StartTree/EndTree, StartNode/EndNode, NumericalTest/CategoricalTest, LeafScalar/LeafVector, Gain/DataCount/SumHess, InitializeMetadata, CommitModel)
    - .planning/phases/02-builder-serialization/02-RESEARCH.md § "Pattern 1" (the 5-state machine + every check + the EndTree orphan loop) and § Pitfall 6 (forward-ref resolution timing)
    - .planning/phases/02-builder-serialization/02-PATTERNS.md § "crates/treelite-builder/src/lib.rs" and § "crates/treelite-builder/src/error.rs"
  </read_first>
  <action>
    Add `"crates/treelite-builder"` to root `Cargo.toml` `[workspace] members`. Create `crates/treelite-builder/Cargo.toml` copying the xgboost manifest shape: `name = "treelite-builder"`, `version/edition/license` via `.workspace = true`, deps `treelite-core = { path = "../treelite-core" }`, `thiserror.workspace = true`. (Omit `serde_json` unless you port `GetModelBuilder(json_str)` — not required by BLD-01.) Create `error.rs` with a `#[derive(Debug, Error)] pub enum BuilderError` carrying locality fields per upstream fatal paths: `DanglingChildKey { key: i32 }`, `OrphanedNode { key: i32 }`, `DuplicateNodeKey { key: i32 }`, `NegativeNodeKey { key: i32 }`, `LeafTestConflict { key: i32 }`, `SelfOrEqualChildKey { node: i32 }`, `SplitIndexOutOfRange { split_index: i32, num_feature: i32 }`, `LeafVectorSizeMismatch { expected: usize, got: usize }`, `WrongState { expected: &'static str }`, `CommitTreeCountMismatch { expected: usize, got: usize }`, plus `#[error(transparent)] Core(#[from] treelite_core::CoreError)`. Module doc-comment cites `model_builder.cc`. In `lib.rs`, implement `ModelBuilder` as a 5-state machine (`ExpectTree → ExpectNode → ExpectDetail → NodeComplete`, plus `ModelComplete`) per RESEARCH Pattern 1. Methods: `new(metadata...) `/`InitializeMetadata`, `start_tree`, `start_node(key)` (reject `key<0` + duplicate via the node-key map — discretion: `BTreeMap<i32,i32>` to mirror upstream `std::map` iteration for deterministic orphan-error key selection), `numerical_test(split_index, threshold, op, default_left, left_key, right_key)` storing children as RAW keys (no resolution yet), `categorical_test(...)`, `leaf_scalar(value)` / `leaf_vector(vec)` (mutual exclusion enforced by the state machine), `gain`/`data_count`/`sum_hess` (legal in both ExpectDetail and NodeComplete), `end_node`, `end_tree` (build the orphaned bool vector all-true-except-root, resolve each non-leaf node's raw child keys via `node_id_map.at()` → `DanglingChildKey` on miss, mark children non-orphaned, then any still-orphaned → `OrphanedNode`; this check is ALWAYS on — do NOT port `SetValidationFlag`/`flag_check_orphaned_nodes_` per D-08), and `commit_model` (require metadata initialized + `num_tree == expected_num_tree` → `Model`). Every upstream `TREELITE_CHECK`/`TREELITE_LOG(FATAL)` becomes a `Result<_, BuilderError>` arm carrying the offending key/index (D-07 locality). EndTree builds the `Tree<f32>` via the column-fill → `TreeBuf::from_owned` pattern from xgboost `build_tree`. Write `tests/validation.rs` covering every `<behavior>` bullet (one `#[test]` per case) using inline builder calls; include the forward-reference acceptance case explicitly. Annotate emitted checks with their `model_builder.cc:NNN` lines.
  </action>
  <verify>
    <automated>cargo test -p treelite-builder --test validation</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-builder` exits 0.
    - `cargo test -p treelite-builder --test validation` passes with a distinct `#[test]` for each of: negative key, duplicate key, leaf+test conflict, equal/self child key, dangling child key, orphaned node, forward-reference ACCEPTED, and commit tree-count mismatch.
    - The forward-reference test builds a tree declaring the root (with children keyed 1,2) BEFORE declaring nodes 1 and 2, ends the tree, and asserts `Ok` (proves resolution is at EndTree, not EndNode — RESEARCH Pitfall 6).
    - Each rejection test asserts the specific `BuilderError` variant (e.g. `matches!(err, BuilderError::OrphanedNode { key: 2 })`), proving locality fields carry the offending key.
    - `grep -c 'flag_check_orphaned\|SetValidationFlag' crates/treelite-builder/src/lib.rs` returns 0 (D-08: toggle NOT ported).
  </acceptance_criteria>
  <done>The `treelite-builder` crate builds; `ModelBuilder` is an always-strict 5-state machine that validates per-node at EndNode and topology at EndTree with forward-ref resolution, every rejection carrying a located `BuilderError`; `validation.rs` covers all behaviors green.</done>
</task>

<task type="auto">
  <name>Task 2: ConcatenateModelObjects (BLD-02) + BulkConstructTree (BLD-03, D-09)</name>
  <files>crates/treelite-builder/src/concat.rs, crates/treelite-builder/src/bulk.rs, crates/treelite-builder/src/lib.rs, crates/treelite-builder/tests/concat.rs, crates/treelite-builder/tests/bulk.rs</files>
  <read_first>
    - treelite-mainline/src/model_concat.cc:19-71 (ConcatenateModelObjects: header-copy-from-objs[0], same-variant check, num_target/num_class/leaf_vector_shape match, deep-clone trees, Extend target_id/class_id; note it does NOT check postprocessor/base_scores equality)
    - treelite-mainline/src/model_loader/sklearn_bulk.cc:36-211 (the ONLY upstream BulkConstructTree: sklearn-shaped signature, leaf detection `children_left[i]==-1`, internal `cmp=kLE default_left=true`, gain formula, has_categorical_split_=false)
    - crates/treelite-core/src/model.rs (ModelVariant match shape lines 44-49; header fields to copy)
    - crates/treelite-core/src/tree_buf.rs:80-83 (`deep_copy` deep-copy primitive concat uses)
    - crates/treelite-xgboost/src/lib.rs:151-207 (the exact column-fill→`from_owned` structural template bulk reuses, minus check_dim per D-09)
    - .planning/phases/02-builder-serialization/02-RESEARCH.md § "Pattern 4" (concat), § "Pattern 3" (bulk bypass semantics), § "Open Questions Q1" (bulk signature scope — port the sklearn-shaped fn verbatim now, wire to a sklearn loader in Phase 4)
    - .planning/phases/02-builder-serialization/02-PATTERNS.md § "concat.rs" and § "bulk.rs"
  </read_first>
  <action>
    `concat.rs`: implement `pub fn concatenate(objs: &[&Model]) -> Result<Option<Model>, BuilderError>` porting `model_concat.cc:19-71` verbatim. Empty input → `Ok(None)`. Copy header (`num_feature`, `task_type`, `average_tree_output`, `num_target`, `num_class`, `leaf_vector_shape`, `postprocessor`, `sigmoid_alpha`, `ratio_c`, `base_scores`, `attributes`) from `objs[0]`. For each input: require the SAME `ModelVariant` discriminant as `objs[0]` (else a typed `VariantMismatch`-style error), and require matching `num_target`/`num_class`/`leaf_vector_shape` (else typed error). Deep-clone each tree via `Tree`/`TreeBuf::deep_copy` and append; `Extend` (concatenate) `target_id` and `class_id`. Post-assert `target_id.len() == class_id.len() == total_num_tree`. Do NOT add postprocessor/base_scores cross-input equality checks (upstream lacks them — RESEARCH Pattern 4 caveat). `bulk.rs`: implement `pub fn bulk_construct_tree(...)` with the sklearn-shaped signature from `sklearn_bulk.cc` (`children_left/right`, `feature`, `threshold`, `value`, `n_node_samples`, `weighted_n_node_samples`, `impurity`, `is_classifier`, plus the target/class shaping params upstream takes) returning a `Tree<f64>` (sklearn uses f64) or generic over the preset type per upstream — match upstream's element types. It bypasses ALL validation by construction (D-09): single fill loop, `children_left[i]==-1`→leaf (`split_index=-1, cmp=kNone, threshold=0, gain=0, gain_present=false`), else internal (`cmp=kLE, default_left=true`, gain computed via the sklearn impurity-reduction formula), `data_count`/`sum_hess` always present, `has_categorical_split=false`; `TreeBuf::from_owned` each column. Document the bypass in a doc-comment citing D-09 + `sklearn_bulk.cc`. Wire both modules into `lib.rs` (`pub mod concat; pub mod bulk; pub use ...`). Write `tests/concat.rs` (merge two small built models → tree count sums, target_id/class_id extended, variant-mismatch rejected, empty→None) and `tests/bulk.rs` (build a tree from small bulk arrays and assert its columns equal what a per-node build of the same logical tree produces — proving bypass parity; assert no validation ran by feeding input the fluent builder would accept).
  </action>
  <verify>
    <automated>cargo test -p treelite-builder --test concat --test bulk</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-builder --test concat` passes: merging two N- and M-tree models yields N+M trees; `target_id`/`class_id` lengths == N+M; a different-variant input is rejected with the typed variant-mismatch error; empty slice → `Ok(None)`.
    - `cargo test -p treelite-builder --test bulk` passes: `bulk_construct_tree` on a small sklearn-shaped input produces a tree whose `node_type`/`cleft`/`cright`/`threshold`/`cmp`/`leaf_value` columns match an equivalent per-node build, with leaves having `cmp=kNone` and internals `cmp=kLE, default_left=true` (RESEARCH Pattern 3).
    - `grep -c 'D-09\|sklearn_bulk' crates/treelite-builder/src/bulk.rs` ≥ 1 (bypass documented).
    - `grep -c 'postprocessor\|base_scores' crates/treelite-builder/src/concat.rs` shows NO equality-check use beyond the copy-from-objs[0] (no upstream-absent checks added — reviewer-auditable).
  </acceptance_criteria>
  <done>`concatenate` merges same-variant models with exact upstream semantics (extending ids, rejecting mismatches, empty→None) and `bulk_construct_tree` builds a tree from pre-validated sklearn-shaped arrays bypassing validation, both green in their test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → ModelBuilder | User-supplied node keys, child keys, split_index, leaf-vector sizes cross here (untrusted integers). |
| caller → bulk_construct_tree | Pre-validated bulk arrays cross here; D-09 explicitly trusts them (bypass) — the trust is the caller's contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-B01 | Tampering/DoS | `start_node` / test methods | mitigate | Reject `key < 0`, `split_index >= num_feature`, equal/self child keys with typed `BuilderError` before they reach a `usize` cast or index (Phase 1 `require_non_negative` discipline). |
| T-02-B02 | DoS | `commit_model` / EndTree | mitigate | Tree-count and orphan checks bound the topology; no unbounded recursion (reachability is an iterative mark over the bool vector). |
| T-02-B03 | Tampering | `bulk_construct_tree` | accept | D-09 documents the bypass: bulk input is the caller's pre-validated contract; the sklearn loader (Phase 4) owns producing well-formed arrays. No external/untrusted bytes reach this path in Phase 2. |
| T-02-SC | Tampering | crate manifest deps | mitigate | No new third-party packages (only path dep on in-repo `treelite-core` + workspace-pinned `thiserror`); package-legitimacy gate N/A per RESEARCH § Package Legitimacy Audit. |
</threat_model>

<verification>
- `cargo build --workspace` succeeds (new member compiles, no overlap with Plan 03's files).
- `cargo test -p treelite-builder` passes all three test files.
- No `SetValidationFlag` toggle ported (D-08); no upstream-absent concat checks added (BLD-02 fidelity).
</verification>

<success_criteria>
- BLD-01: fluent always-strict ModelBuilder rejects orphan/dangling/duplicate/leaf-test/negative-key, accepts forward refs, with located typed errors.
- BLD-02: ConcatenateModelObjects merges same-variant models, extending ids, rejecting mismatches, empty→None.
- BLD-03: BulkConstructTree builds from pre-validated bulk input, bypassing validation by construction (D-09).
</success_criteria>

<output>
Create `.planning/phases/02-builder-serialization/02-02-SUMMARY.md` when done.
</output>
