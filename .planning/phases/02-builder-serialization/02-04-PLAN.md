---
phase: 02-builder-serialization
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - crates/treelite-core/src/serialize/json.rs
  - crates/treelite-core/src/serialize/fields.rs
  - crates/treelite-core/src/serialize/mod.rs
  - crates/treelite-core/src/lib.rs
  - crates/treelite-core/Cargo.toml
  - crates/treelite-core/tests/dump_json.rs
  - crates/treelite-core/tests/fields.rs
autonomous: true
requirements: [SER-03, SER-04]
must_haves:
  truths:
    - "DumpAsJSON emits a model as a JSON object whose key names, nesting, and types mirror upstream json_serializer.cc so a Rust dump is value-diffable against a C++ dump."
    - "Field accessors expose model header fields and per-tree node fields for inspection, with upstream read-only fields (version/num_tree/type tags) exposed read-only."
  artifacts:
    - path: "crates/treelite-core/src/serialize/json.rs"
      provides: "DumpAsJSON (SER-03, D-04)"
      contains: "fn dump_as_json"
      min_lines: 60
    - path: "crates/treelite-core/src/serialize/fields.rs"
      provides: "Model/Tree field accessors (SER-04)"
      contains: "num_feature"
  key_links:
    - from: "crates/treelite-core/src/serialize/json.rs"
      to: "treelite_core::enums"
      via: "reuse as_str() enum string forms"
      pattern: "as_str"
---

<objective>
Complete the serialization surface: `DumpAsJSON` (SER-03) emitting a model as JSON whose structure mirrors upstream `json_serializer.cc` exactly (D-04, value-diffable), reusing the already-correct enum `as_str()` spellings; and typed field accessors (SER-04) exposing model header + per-tree node fields for inspection, preserving upstream's read-only fields. This is the "inspect a model" vertical slice that completes Phase 2's persistence/introspection goal.

Purpose: Python (Phase 8) and debugging need JSON dump + field inspection; D-04 fidelity enables JSON-level equivalence checking against C++ dumps.
Output: `serialize/json.rs` + `serialize/fields.rs`; module + lib wiring; `serde_json` dep on core; dump + fields tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-builder-serialization/02-CONTEXT.md
@.planning/phases/02-builder-serialization/02-RESEARCH.md
@.planning/phases/02-builder-serialization/02-PATTERNS.md
@crates/treelite-core/src/model.rs
@crates/treelite-core/src/tree.rs
</context>

## Phase Goal (developer-facing user story)

**As a** treelite-rs consumer, **I want to** dump a `Model` to structured JSON and read its header/tree fields via accessors, **so that** I can inspect and diff a model against the upstream C++ dump.

<tasks>

<task type="auto">
  <name>Task 1: DumpAsJSON mirroring json_serializer.cc structure (SER-03, D-04)</name>
  <files>crates/treelite-core/src/serialize/json.rs, crates/treelite-core/src/serialize/mod.rs, crates/treelite-core/src/lib.rs, crates/treelite-core/Cargo.toml, crates/treelite-core/tests/dump_json.rs</files>
  <read_first>
    - treelite-mainline/src/json_serializer.cc:135-229 (the model-level JSON structure: threshold_type, leaf_output_type, num_feature, task_type, average_tree_output, num_target, num_class, leaf_vector_shape, target_id, class_id, postprocessor, sigmoid_alpha, ratio_c, base_scores, attributes, trees[])
    - treelite-mainline/src/json_serializer.cc:81-133 (WriteNode: per-node key set — node_id always; leaf → leaf_value scalar/array; internal → split_feature_id, default_left, node_type string, then numerical comparison_op+threshold OR categorical category_list_right_child+category_list, then left_child/right_child; conditional data_count/sum_hess/gain)
    - crates/treelite-core/src/enums.rs (the `as_str()` forms already implemented: TaskType `kBinaryClf`, TreeNodeType `leaf_node`/`numerical_test_node`/`categorical_test_node`, Operator `<`/`<=`, DType `float32` — reuse, do NOT re-spell)
    - crates/treelite-core/src/model.rs + tree.rs (the fields to emit + the `has_leaf_vector`/`is_leaf` predicates)
    - crates/treelite-xgboost/src/lib.rs (serde_json usage pattern)
    - crates/treelite-core/Cargo.toml (where to add `serde_json.workspace = true`)
    - .planning/phases/02-builder-serialization/02-RESEARCH.md § "Pattern 6" (exact structure + per-node keys + the float-formatting caveat: compare PARSED values, not raw bytes — A4/Q3), § Open Question 3
    - .planning/phases/02-builder-serialization/02-PATTERNS.md § "serialize/json.rs"
  </read_first>
  <action>
    Add `serde_json.workspace = true` to `crates/treelite-core/Cargo.toml` `[dependencies]`. Create `serialize/json.rs` with `pub fn dump_as_json(m: &Model) -> serde_json::Value` (or `-> String`) porting `json_serializer.cc:135-229` structurally: emit a JSON object with keys in upstream order — `threshold_type`/`leaf_output_type` (via `DType::as_str` → `"float32"`), `num_feature`, `task_type` (via `TaskType::as_str`), `average_tree_output`, `num_target`, `num_class`, `leaf_vector_shape`, `target_id`, `class_id`, `postprocessor`, `sigmoid_alpha`, `ratio_c`, `base_scores`, `attributes`, then `trees: [ { num_nodes, has_categorical_split, nodes: [...] } ]`. Per node port `WriteNode` (json_serializer.cc:81-133): always `node_id`; if leaf → `leaf_value` (scalar, or array when `has_leaf_vector`); if internal → `split_feature_id`, `default_left`, `node_type` (via `TreeNodeType::as_str`), then for numerical `comparison_op` (via `Operator::as_str`) + `threshold`, for categorical `category_list_right_child` + `category_list`, then `left_child`, `right_child`; conditional tail `data_count` (if present), `sum_hess` (if present), `gain` (if present) keyed off the `*_present` columns. Reuse the existing enum `as_str()` — do not re-implement the non-uniform spellings. Wire `pub mod json` into `serialize/mod.rs` and re-export `dump_as_json` from core `lib.rs`. Write `tests/dump_json.rs`: dump the binary:logistic fixture model and assert the structure VALUE-level — parse the dump, assert the top-level keys exist with correct types, `task_type == "kBinaryClf"`, `threshold_type == "float32"`, `trees` is an array of length == num_tree, each tree has `nodes`, an internal node has `comparison_op == "<"` and a `threshold`, a leaf node has a `leaf_value`. Compare parsed values (not raw bytes) per D-04/Q3.
  </action>
  <verify>
    <automated>cargo test -p treelite-core --test dump_json</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-core --test dump_json` passes.
    - The test asserts (value-level) the model-object keys in upstream set, `task_type == "kBinaryClf"`, `threshold_type == "float32"`, a numerical node with `comparison_op == "<"` + a `threshold`, and a leaf with `leaf_value` — proving structural fidelity to `json_serializer.cc`.
    - `grep -c 'as_str' crates/treelite-core/src/serialize/json.rs` ≥ 3 (reuses existing enum string forms, not re-spelled).
    - The test compares PARSED JSON values, not raw byte strings (no `assert_eq!` on a whole serialized JSON string — float formatting may differ, A4/Q3).
  </acceptance_criteria>
  <done>`dump_as_json` emits a model as JSON whose keys/nesting/types mirror upstream `json_serializer.cc`, reusing the existing enum `as_str()` spellings; the test asserts structure at the value level.</done>
</task>

<task type="auto">
  <name>Task 2: Model/Tree field accessors (SER-04)</name>
  <files>crates/treelite-core/src/serialize/fields.rs, crates/treelite-core/src/serialize/mod.rs, crates/treelite-core/src/lib.rs, crates/treelite-core/tests/fields.rs</files>
  <read_first>
    - treelite-mainline/src/field_accessor.cc:16-249 (GetHeaderField / GetTreeField / SetHeaderField surface — the ~20 header + ~25 tree field names, and which fields are read-only and reject Set: major_ver/minor_ver/patch_ver/threshold_type/leaf_output_type/num_tree/num_opt_field_*)
    - treelite-mainline/include/treelite/tree.h:480-520 (GetHeaderField signature + the field set)
    - crates/treelite-core/src/tree.rs:107-158 (the existing getter convention — small `pub fn name(&self, nid) -> T` methods doc-commented with the upstream line; SER-04 accessors follow this exact shape)
    - crates/treelite-core/src/model.rs (header fields + the Plan 01 private bookkeeping fields exposed read-only)
    - .planning/phases/02-builder-serialization/02-RESEARCH.md § "Pattern 7" (recommend typed methods now — `model.num_feature()`, `tree.threshold(nid)` — defer the string-dispatch `GetHeaderField(name)->Frame` shape to Phase 8; preserve read-only fields)
    - .planning/phases/02-builder-serialization/02-PATTERNS.md § "serialize/fields.rs"
  </read_first>
  <action>
    Create `serialize/fields.rs` providing typed read accessors (RESEARCH Pattern 7 recommendation — idiomatic now, string-dispatch deferred to Phase 8) for the SER-04 surface the success criterion names ("expose model/tree fields for inspection"). For `Model`: read accessors for the header fields not already publicly readable plus the Plan-01 private bookkeeping fields exposed READ-ONLY (`major_ver()`, `minor_ver()`, `patch_ver()`, `num_tree()`, `threshold_type()`, `leaf_output_type()`, `num_opt_field_per_model()`) — these are read-only upstream and MUST NOT get a setter (field_accessor.cc rejects Set on them). Since most `Model` header fields are already `pub`, add accessor methods only where a private field needs a read path (the Plan-01 bookkeeping fields) and a small `get_header_field`-style convenience if it aids tests; do not expose setters for the read-only set. For `Tree`: confirm/extend the existing per-node getters (tree.rs:107-158 already covers left_child/right_child/split_index/is_leaf/leaf_value/threshold/comparison_op/has_leaf_vector) with any missing inspection accessor named by SER-04 (e.g. `node_type(nid)`, `default_left(nid)`, `gain(nid)` gated on `gain_present`). Wire into `serialize/mod.rs` + re-export from `lib.rs` as needed. Doc-comment each accessor with its upstream `field_accessor.cc:NNN` / `tree.h:NNN` line. Write `tests/fields.rs`: build/load a model, assert `model.num_feature()` and the staged `model.num_tree()`/`model.threshold_type()` (after `stage_serialization_fields`) return correct values, and per-tree `tree.threshold(nid)`/`tree.is_leaf(nid)`/`tree.leaf_value(nid)` return the expected node values; assert the read-only fields have NO setter (compile-level — simply do not provide one; document this).
  </action>
  <verify>
    <automated>cargo test -p treelite-core --test fields</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-core --test fields` passes.
    - The test asserts `model.num_feature()` matches the loaded value and (after staging) `model.num_tree()` == tree count and `model.threshold_type()` == `DType::kFloat32`.
    - The test asserts per-node `tree.threshold(nid)` / `tree.is_leaf(nid)` / `tree.leaf_value(nid)` return correct values for an internal node and a leaf node.
    - `grep -c 'pub fn set_major_ver\|pub fn set_num_tree\|pub fn set_threshold_type' crates/treelite-core/src/serialize/fields.rs` returns 0 (read-only fields get NO setter — field_accessor.cc fidelity).
    - Each accessor is doc-commented with its upstream source line.
  </acceptance_criteria>
  <done>Typed read accessors expose model header (incl. read-only bookkeeping) + per-tree node fields for inspection, with no setter on upstream read-only fields; `fields.rs` test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `&Model` → dump/accessors | Read-only inspection of an already-constructed model; no untrusted external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-J01 | Information disclosure | `dump_as_json` | accept | Dump exposes only the model the caller already holds; no secrets, no PII, no new trust boundary. Low-value target. |
| T-02-J02 | Tampering | field accessors | mitigate | Read-only fields (version/num_tree/type tags) expose NO setter (field_accessor.cc fidelity), preventing bookkeeping corruption that would break serialize fidelity. |
| T-02-SC | Tampering | core manifest dep add | mitigate | Only `serde_json` (already workspace-pinned and used in Phase 1) is added to `treelite-core`; no new third-party package; legitimacy gate N/A. |
</threat_model>

<verification>
- `cargo build --workspace` succeeds (serde_json added to core; no file overlap with Plan 05).
- `cargo test -p treelite-core --test dump_json --test fields` green.
</verification>

<success_criteria>
- SER-03: DumpAsJSON emits a model as JSON structurally matching upstream json_serializer.cc (value-diffable, D-04).
- SER-04: field accessors expose model/tree fields for inspection, read-only fields without setters.
</success_criteria>

<output>
Create `.planning/phases/02-builder-serialization/02-04-SUMMARY.md` when done.
</output>
