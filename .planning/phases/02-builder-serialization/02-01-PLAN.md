---
phase: 02-builder-serialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/treelite-core/src/model.rs
  - crates/treelite-core/src/tree.rs
  - fixtures/capture_golden_v5.py
  - fixtures/golden_v5.bin
  - fixtures/golden_v5.manifest.json
autonomous: false
requirements: [SER-01]
must_haves:
  truths:
    - "The Model carries the private serialization bookkeeping scalars the v5 header needs (version triple, type tags, num_tree, opt-field count), each borrow-able for a frame's lifetime."
    - "A frozen upstream-produced golden v5 blob and its toolchain/libm manifest exist in fixtures/, captured once from the installed treelite==4.7.0 wheel."
  artifacts:
    - path: "crates/treelite-core/src/model.rs"
      provides: "6 private bookkeeping fields on Model + a serialize-time staging method"
      contains: "num_tree_"
    - path: "crates/treelite-core/src/tree.rs"
      provides: "num_opt_field_per_tree/node scalar fields on Tree (default 0)"
      contains: "num_opt_field_per_tree"
    - path: "fixtures/golden_v5.bin"
      provides: "Frozen authoritative v5 byte stream from the upstream wheel"
    - path: "fixtures/golden_v5.manifest.json"
      provides: "treelite/xgboost versions, OS/arch, libc, sha256, nbytes manifest"
      contains: "sha256"
  key_links:
    - from: "fixtures/capture_golden_v5.py"
      to: "fixtures/binary_logistic.model.json"
      via: "treelite.frontend.load_xgboost_model + serialize_bytes"
      pattern: "serialize_bytes"
---

<objective>
Lay the Phase 2 foundation: extend `Model` and `Tree` with the private serialization bookkeeping fields the v5 header/per-tree walk requires (deferred from Phase 1), and capture the frozen D-02 golden v5 blob + manifest from the installed upstream `treelite==4.7.0` wheel. This is the ground-truth artifact every serialization task validates against, and the staging fields are the lifetime anchor the zero-copy PyBuffer frames borrow (Pattern 5).

Purpose: Without the golden blob, byte-fidelity (D-01) cannot be checked; without the private staging fields, the recomputed header scalars (version, num_tree, type tags) have no `'a`-lived home for a borrowed frame to point at.
Output: Modified `model.rs`/`tree.rs`; `fixtures/capture_golden_v5.py`; committed `fixtures/golden_v5.bin` + `fixtures/golden_v5.manifest.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-builder-serialization/02-CONTEXT.md
@.planning/phases/02-builder-serialization/02-RESEARCH.md
@.planning/phases/02-builder-serialization/02-PATTERNS.md
</context>

## Phase Goal (developer-facing user story)

**As a** treelite-rs loader/serializer developer, **I want to** a `Model` that carries the recomputed v5 bookkeeping scalars plus a frozen upstream golden blob to diff against, **so that** I can prove byte-for-byte v5 fidelity (D-01) instead of guessing the wire format.

> Note: the ROADMAP Phase 2 `**Goal:**` line is outcome-shaped, not bolded user-story format. The story above is derived faithfully from that goal and the 4 success criteria; it does not invent scope. If a strict user-story ROADMAP goal is later required, run `/gsd mvp-phase 2`.

<tasks>

<task type="auto">
  <name>Task 1: Add private serialization bookkeeping fields to Model and Tree</name>
  <files>crates/treelite-core/src/model.rs, crates/treelite-core/src/tree.rs</files>
  <read_first>
    - crates/treelite-core/src/model.rs (the deferred-fields comment at lines 85-87; `Model` struct + `Model::new` init block lines 90-110)
    - crates/treelite-core/src/tree.rs (the deferred-fields comment at lines 73-74; `Tree::new` init block lines 79-104)
    - crates/treelite-core/src/enums.rs (the `DType` enum — `threshold_type_`/`leaf_output_type_` use it)
    - treelite-mainline/include/treelite/tree.h:556-567 (upstream private bookkeeping field declarations)
    - .planning/phases/02-builder-serialization/02-RESEARCH.md § "Pattern 5" (the recomputed-scalar lifetime subtlety driving why these fields exist) and § "Header field walk" code example
    - .planning/phases/02-builder-serialization/02-PATTERNS.md § "crates/treelite-core/src/model.rs (modify)" and § "crates/treelite-core/src/tree.rs (modify)"
  </read_first>
  <action>
    In `model.rs`, replace the deferred-fields comment (model.rs:85-87) with 6 private fields on `Model`: `num_tree_: u64`, `num_opt_field_per_model_: i32`, `major_ver_: i32`, `minor_ver_: i32`, `patch_ver_: i32`, `threshold_type_: DType`, `leaf_output_type_: DType` (the type-tag pair plus the version triple plus num_tree and opt-field count — 7 named fields covering the 6 conceptual groups). Make them module-private (no `pub`). Import `DType` from `crate::enums`. Initialize all in `Model::new` to inert defaults (`0` for the ints/num_tree, `DType::kFloat32` or the existing `kInvalid` variant for the type tags — pick the upstream-matching default per tree.h). Add an inherent method `stage_serialization_fields(&mut self)` that recomputes and writes these fields at serialize time exactly as RESEARCH § "Header field walk" specifies: `major_ver_=4, minor_ver_=7, patch_ver_=0` (the 4.7.0 constants, NOT 5,x,x — see RESEARCH Pitfall 1), `num_tree_ = <count of trees in the variant> as u64`, `threshold_type_`/`leaf_output_type_` from the variant (`F32`→`DType::kFloat32`, `F64`→`DType::kFloat64`), `num_opt_field_per_model_ = 0`. Add private read accessors (`major_ver(&self) -> i32`, etc.) the serializer (a sibling in-crate module) will use to borrow these — keep them `pub(crate)`. In `tree.rs`, replace the deferred comment (tree.rs:73-74) with two `pub` scalar fields `num_opt_field_per_tree: i32` and `num_opt_field_per_node: i32`, both initialized to `0` in `Tree::new`. Annotate each new field with its upstream `tree.h:NNN` line per the doc-comment convention.
  </action>
  <verify>
    <automated>cargo build -p treelite-core && cargo test -p treelite-core --lib</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-core` exits 0 with the new fields present.
    - `grep -v '^\s*//' crates/treelite-core/src/model.rs | grep -c 'num_tree_\|major_ver_\|threshold_type_\|num_opt_field_per_model_'` returns ≥ 4 (fields declared, not just commented).
    - `grep -c 'num_opt_field_per_tree\|num_opt_field_per_node' crates/treelite-core/src/tree.rs` returns ≥ 2.
    - `stage_serialization_fields` sets `major_ver_=4, minor_ver_=7, patch_ver_=0` (assert via a `#[test]` in model.rs that builds an F32 `Model` with N trees, calls the staging method, and asserts the version triple is `(4,7,0)` and `num_tree_ == N` and `threshold_type_ == DType::kFloat32`).
    - Existing Phase 1 core tests still pass (no regression).
  </acceptance_criteria>
  <done>`Model` carries the 7 private bookkeeping fields with a `stage_serialization_fields` recompute method asserting `(4,7,0)` + correct num_tree + type tags; `Tree` carries the two opt-field scalars defaulting to 0; `treelite-core` builds and its tests pass.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture the D-02 golden v5 blob (one-time, local venv)</name>
  <what-built>Nothing automated yet — this is the one-time D-02 golden v5 blob capture that requires the developer's local Python `.venv` with `treelite==4.7.0` + `xgboost==3.2.0` installed (the wheel is the ground-truth tool; CI never runs this).</what-built>
  <how-to-verify>
    1. Confirm the venv: `python -c "import treelite, xgboost; print(treelite.__version__, xgboost.__version__)"` prints `4.7.0 3.2.0`.
    2. The executor will have written `fixtures/capture_golden_v5.py` (see Task 3). Run it: `python fixtures/capture_golden_v5.py`.
    3. Confirm it wrote `fixtures/golden_v5.bin` (non-empty) and `fixtures/golden_v5.manifest.json` (contains `sha256`, `nbytes`, `treelite: "4.7.0"`).
    4. Confirm the first 12 bytes of the blob decode as the version triple `4, 7, 0` as little-endian int32 (RESEARCH Summary finding 1 / Pitfall 1): `python -c "import struct; b=open('fixtures/golden_v5.bin','rb').read(12); print(struct.unpack('<iii', b))"` must print `(4, 7, 0)`. If it prints `(5, x, x)` STOP — the version-constant assumption (A1) is wrong and the serializer constants must be revisited before any further serialization work.
  </how-to-verify>
  <resume-signal>Type "approved" once golden_v5.bin + manifest are committed and the first-12-bytes check prints (4, 7, 0); or paste the actual tuple if it differs.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the golden v5 capture script and freeze the blob + manifest</name>
  <files>fixtures/capture_golden_v5.py, fixtures/golden_v5.bin, fixtures/golden_v5.manifest.json</files>
  <read_first>
    - fixtures/capture_golden.py (the whole Phase 1 capture script — path-resolution block lines 32-35, wheel-load API line 47, manifest keys lines 71-78; D-02 mirrors this exactly)
    - fixtures/binary_logistic.model.json (the same fixture; keeps sigmoid exercised per RESEARCH § Specific Ideas)
    - .planning/phases/02-builder-serialization/02-RESEARCH.md § "D-02 golden blob capture" code example (lines 514-532) — the `serialize_bytes()` call + manifest keys (`sha256`, `nbytes`)
    - .planning/phases/02-builder-serialization/02-PATTERNS.md § "fixtures/capture_golden_v5.py ... (D-02)"
    - crates/treelite-harness/src/lib.rs (the `Manifest` struct shape the Rust side will later deserialize)
  </read_first>
  <action>
    Create `fixtures/capture_golden_v5.py` modeled exactly on `fixtures/capture_golden.py`: reuse its `HERE = os.path.dirname(os.path.abspath(__file__))` path-resolution block; load the SAME `binary_logistic.model.json` via `treelite.frontend.load_xgboost_model(MODEL_PATH)` (confirm the exact API against the installed wheel — the Phase 1 script used this call and it worked). Call `model.serialize_bytes()` to obtain the authoritative v5 byte stream (this routes to `TreeliteSerializeModelToBytes` → `SerializeToBuffer`, RESEARCH § Wire Format). Write the raw bytes to `fixtures/golden_v5.bin` in binary mode. Build a manifest dict with EXACTLY the Phase 1 keys (`treelite`, `xgboost`, `os` via `platform.platform()`, `arch` via `platform.machine()`, `libc` via `platform.libc_ver()`, `python` via `platform.python_version()`) PLUS `sha256` (`hashlib.sha256(blob).hexdigest()`), `nbytes` (`len(blob)`), and `source_fixture` (`"binary_logistic.model.json"`); write it to `fixtures/golden_v5.manifest.json` with `json.dump(..., indent=2)`. Add a top-of-file docstring matching the Phase 1 "run ONCE, commit, CI never regenerates" discipline. Then RUN the script (this requires the local venv from Task 2's checkpoint) and commit the produced `golden_v5.bin` + `golden_v5.manifest.json`. Do NOT hand-fabricate the blob — it must come from the wheel.
  </action>
  <verify>
    <automated>test -s fixtures/golden_v5.bin && python -c "import json,hashlib;m=json.load(open('fixtures/golden_v5.manifest.json'));b=open('fixtures/golden_v5.bin','rb').read();assert m['sha256']==hashlib.sha256(b).hexdigest();assert m['nbytes']==len(b);assert m['treelite']=='4.7.0';import struct;assert struct.unpack('<iii',b[:12])==(4,7,0)"</automated>
  </verify>
  <acceptance_criteria>
    - `fixtures/golden_v5.bin` exists and is non-empty.
    - `fixtures/golden_v5.manifest.json` parses as JSON and its `sha256` matches `sha256(golden_v5.bin)` and `nbytes` matches the file size (the verify command asserts both).
    - `manifest.treelite == "4.7.0"`.
    - First 12 bytes of `golden_v5.bin` unpack as little-endian `(4, 7, 0)` (the verify command asserts this — settles RESEARCH Assumption A1 / Open Question Q2 empirically).
    - `capture_golden_v5.py` reuses the Phase 1 path-resolution + manifest-key conventions (structural diff against `capture_golden.py`).
  </acceptance_criteria>
  <done>The capture script exists, was run against the installed wheel, and produced a committed `golden_v5.bin` whose first 12 bytes are `(4,7,0)` and whose sha256/nbytes match the committed manifest; this is the frozen D-02 ground truth.</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` succeeds with the new core fields.
- `cargo test -p treelite-core --lib` passes (including the new `stage_serialization_fields` test).
- `fixtures/golden_v5.bin` + `fixtures/golden_v5.manifest.json` are committed; first-12-bytes == (4,7,0); manifest sha256/nbytes consistent.
</verification>

<success_criteria>
- Model/Tree carry the deferred private serialization bookkeeping fields with a correct serialize-time staging method (anchors Pattern 5 zero-copy frames).
- The frozen upstream golden v5 blob + manifest exist and empirically confirm the `4,7,0` version header (de-risks every downstream serialization task).
</success_criteria>

<output>
Create `.planning/phases/02-builder-serialization/02-01-SUMMARY.md` when done.
</output>
