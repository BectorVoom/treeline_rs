---
phase: 02-builder-serialization
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - crates/treelite-xgboost/Cargo.toml
  - crates/treelite-xgboost/src/lib.rs
  - crates/treelite-xgboost/src/error.rs
autonomous: false
requirements: [BLD-01]
must_haves:
  truths:
    - "The XGBoost-JSON loader constructs its Model by driving the treelite-builder ModelBuilder (StartTree/StartNode/NumericalTest/LeafScalar/EndNode/EndTree/CommitModel) instead of hand-assembling Tree columns."
    - "After rewiring, the Phase 1 1e-5 equivalence harness still passes (predictions unchanged within 1e-5 of the frozen golden)."
  artifacts:
    - path: "crates/treelite-xgboost/src/lib.rs"
      provides: "load_xgboost_json rewired through the builder (D-11)"
      contains: "ModelBuilder"
    - path: "crates/treelite-xgboost/Cargo.toml"
      provides: "treelite-builder path dependency (D-11)"
      contains: "treelite-builder"
  key_links:
    - from: "crates/treelite-xgboost/src/lib.rs"
      to: "treelite_builder::ModelBuilder"
      via: "loader emits builder calls"
      pattern: "ModelBuilder|start_tree|numerical_test|commit_model"
    - from: "crates/treelite-harness/tests/equivalence.rs"
      to: "load_xgboost_json"
      via: "1e-5 regression gate (unchanged)"
      pattern: "load_xgboost_json"
---

<objective>
Rewire the Phase 1 XGBoost-JSON loader to construct its `Model` by driving the new `treelite-builder` `ModelBuilder` (D-11) instead of hand-assembling `Tree` columns, and prove the rewiring preserves predictions by keeping the existing 1e-5 equivalence harness green. This closes Phase 2 success criterion 1's second half ("the skeleton XGBoost-JSON loader from Phase 1 is rewired to build through it and still verifies within 1e-5") and end-to-end exercises BLD-01 through a real loader.

Purpose: The builder must be proven against a real construction path, not just unit tests; the 1e-5 harness is the regression gate that proves no prediction drift.
Output: `treelite-xgboost` gains a `treelite-builder` dep and emits builder calls; the harness stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-builder-serialization/02-CONTEXT.md
@.planning/phases/02-builder-serialization/02-RESEARCH.md
@.planning/phases/02-builder-serialization/02-PATTERNS.md
@crates/treelite-xgboost/src/lib.rs
</context>

## Phase Goal (developer-facing user story)

**As a** treelite-rs user loading an XGBoost-JSON model, **I want to** the loader to build through the validated `ModelBuilder`, **so that** malformed topologies are caught by one shared strict builder and predictions stay bit-equivalent to upstream within 1e-5.

<tasks>

<task type="auto">
  <name>Task 1: Add the builder dependency and rewire load_xgboost_json through ModelBuilder (D-11)</name>
  <files>crates/treelite-xgboost/Cargo.toml, crates/treelite-xgboost/src/lib.rs, crates/treelite-xgboost/src/error.rs</files>
  <read_first>
    - crates/treelite-xgboost/src/lib.rs (the WHOLE file — `build_tree` lines 151-207 hand-assembles columns; `load_xgboost_json` lines 217-288 hand-assembles the Model + header finalize; the per-node leaf/internal branch lines 172-192 maps 1:1 to LeafScalar vs NumericalTest; the `require_non_negative`/`check_dim` validators stay)
    - crates/treelite-xgboost/Cargo.toml (where to add the builder dep)
    - crates/treelite-xgboost/src/error.rs (add a `#[error(transparent)] Builder(#[from] treelite_builder::BuilderError)` arm if loader propagates builder errors)
    - crates/treelite-builder/src/lib.rs (the ModelBuilder API produced by Plan 02 — exact method names/signatures: start_tree, start_node, numerical_test, leaf_scalar, end_node, end_tree, commit_model, InitializeMetadata)
    - crates/treelite-builder/src/error.rs (BuilderError variants for the `#[from]` bubble-up)
    - treelite-mainline/src/model_loader/ (XGBoost JSON path — confirms the loader-emits-builder-calls shape upstream uses)
    - .planning/phases/02-builder-serialization/02-RESEARCH.md § "System Architecture Diagram" (loader → builder call sequence)
    - .planning/phases/02-builder-serialization/02-PATTERNS.md § "crates/treelite-xgboost/src/lib.rs (rewire through builder, D-11)" and § "crates/treelite-xgboost/Cargo.toml (modify)"
  </read_first>
  <action>
    Add `treelite-builder = { path = "../treelite-builder" }` to `crates/treelite-xgboost/Cargo.toml` `[dependencies]` (D-11). Add `#[error(transparent)] Builder(#[from] treelite_builder::BuilderError)` to `XgbError` in `error.rs` so builder failures propagate as typed loader errors (keep `thiserror` only — no `anyhow`). Rewrite `build_tree` and the model-assembly leg of `load_xgboost_json` to drive the `ModelBuilder`: KEEP the existing `parse_scalar`/`require_non_negative`/`check_dim` validation (it runs BEFORE emitting builder calls — RESEARCH/PATTERNS note the validators stay). Initialize the builder metadata from the same header values the current finalize computes (num_feature, task_type, num_target, num_class, leaf_vector_shape, postprocessor, sigmoid_alpha, ratio_c, average_tree_output, attributes, base_scores, target_id, class_id — the version-gated base_score→margin transform stays exactly as-is). For each tree: `start_tree`; for each node `i` in `0..num_nodes`: `start_node(i as i32)`; if `left_children[i] == -1` → `leaf_scalar(split_conditions[i])`; else → `numerical_test(split_index=split_indices[i], threshold=split_conditions[i], op=Operator::kLT, default_left=(default_left[i]!=0), left_key=left_children[i], right_key=right_children[i])`; `end_node`; after all nodes `end_tree`. Finally `commit_model()` → `Model`, then set any header fields the builder metadata API does not cover the same way the current code does (preserve the exact header values so predictions are unchanged). Propagate every builder `Result` with `?` (mapped into `XgbError::Builder`). Do NOT change the objective→postprocessor map, the f64 base_score margin transform, or the F32-only variant choice — only the construction mechanism changes.
  </action>
  <verify>
    <automated>cargo build -p treelite-xgboost && cargo test -p treelite-xgboost</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-xgboost` exits 0 with the new builder dependency.
    - `grep -c 'ModelBuilder\|start_tree\|numerical_test\|leaf_scalar\|commit_model' crates/treelite-xgboost/src/lib.rs` ≥ 4 (loader now drives the builder).
    - `grep -c 'TreeBuf::from_owned' crates/treelite-xgboost/src/lib.rs` shows the hand-assembly column fills are GONE from the tree-build path (the builder owns column assembly now) — i.e. the count drops to 0 in the rewired build path (any remaining is incidental, document if so).
    - `treelite-builder` appears in `crates/treelite-xgboost/Cargo.toml` `[dependencies]`.
    - Existing `treelite-xgboost` unit tests (if any) still pass.
  </acceptance_criteria>
  <done>`load_xgboost_json` constructs its Model by driving `treelite_builder::ModelBuilder` (validators preserved, builder errors propagated as `XgbError::Builder`), and `treelite-xgboost` builds against the new dependency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 1e-5 regression gate after the builder rewiring</name>
  <what-built>The XGBoost-JSON loader now builds its Model through the validated `ModelBuilder` (D-11). The 1e-5 equivalence harness is the regression gate: it loads the binary:logistic fixture through the rewired loader, predicts the committed input matrix, and asserts every output is within 1e-5 of the frozen golden — proving the rewiring did not perturb predictions.</what-built>
  <how-to-verify>
    1. Run the regression gate: `cargo test -p treelite-harness --test equivalence` — it MUST pass.
    2. Run the full workspace suite: `cargo test --workspace` — all crates green (builder unit tests, serialize round-trip + golden byte-compare, dump/fields, and the equivalence harness).
    3. Inspect the printed `max observed |delta|` from the equivalence test — it should remain at or near the Phase 1 value (`0e0` / well under 1e-5). A sudden jump means the rewiring changed a node value.
  </how-to-verify>
  <resume-signal>Type "approved" once `cargo test --workspace` is green and the equivalence max |delta| is still < 1e-5; or paste the failure / the new max delta if it regressed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XGBoost-JSON string → loader → ModelBuilder | Untrusted model JSON crosses into the loader; the loader's existing validators (`require_non_negative`, `check_dim`) plus the builder's strict validation both guard it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-X01 | Tampering / DoS | loader → builder node emission | mitigate | Existing `require_non_negative`/`check_dim` run before builder calls; the builder additionally rejects negative keys / dangling children / orphans — defense-in-depth, no panic (ERR-01). |
| T-02-X02 | Tampering | builder error propagation | mitigate | Builder failures surface as typed `XgbError::Builder` (no panic crosses the loader boundary); the 1e-5 harness proves no silent value drift. |
| T-02-SC | Tampering | xgboost manifest dep add | mitigate | Only an in-repo path dep on `treelite-builder` is added; no third-party package; legitimacy gate N/A. |
</threat_model>

<verification>
- `cargo test --workspace` green: builder, serialize (round-trip + golden), dump/fields, and the equivalence harness all pass.
- `cargo test -p treelite-harness --test equivalence` proves predictions stay within 1e-5 after the rewiring (D-11 regression gate).
</verification>

<success_criteria>
- BLD-01 / D-11: the XGBoost-JSON loader builds through the validated ModelBuilder and still verifies within 1e-5 of the frozen golden (Phase 2 success criterion 1, second half).
</success_criteria>

<output>
Create `.planning/phases/02-builder-serialization/02-05-SUMMARY.md` when done.
</output>
