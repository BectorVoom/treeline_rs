---
phase: 06-cubecl-gtil-kernels-cpu-backend
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - crates/treelite-harness/Cargo.toml
  - crates/treelite-harness/src/lib.rs
  - crates/treelite-harness/src/manifest.rs
  - crates/treelite-harness/tests/gtil_matrix_cubecl.rs
  - crates/treelite-cubecl/tests/determinism.rs
autonomous: true
requirements: [GPU-02, GPU-01, GPU-05]
user_setup: []

must_haves:
  truths:
    - "Backend::CubeclCpu + cubecl_cpu_case() register the cubecl kernels (dense f32/f64) and the scalar fallback (sparse f32/f64) into the existing RunnerCase seam with NO change to the matrix iteration"
    - "The cubecl backend asserts the SAME frozen fixtures/gtil/ goldens scalar-cpu passes, within 1e-5, across every cell"
    - "Output is bit-identical across two runs of the same input (SC2 determinism)"
    - "Each cell records cubecl-kernel vs scalar-fallback provenance (D-06)"
  artifacts:
    - path: "crates/treelite-harness/src/lib.rs"
      provides: "Backend::CubeclCpu variant + cubecl_cpu_case() constructor"
      contains: "CubeclCpu"
    - path: "crates/treelite-harness/tests/gtil_matrix_cubecl.rs"
      provides: "Sibling matrix gate: frozen goldens to 1e-5 on cubecl + per-cell provenance + SC2 determinism"
      contains: "cubecl_cpu_case"
    - path: "crates/treelite-cubecl/tests/determinism.rs"
      provides: "Two-run .to_bits() bit-identity (SC2)"
      contains: "to_bits"
  key_links:
    - from: "crates/treelite-harness/src/lib.rs"
      to: "treelite_cubecl::predict_cpu"
      via: "cubecl_cpu_case dense slots (D-01/D-05)"
      pattern: "treelite_cubecl::predict_cpu"
    - from: "crates/treelite-harness/tests/gtil_matrix_cubecl.rs"
      to: "fixtures/gtil/*.golden.json"
      via: "1e-5 assertion against the identical frozen goldens"
      pattern: "fixtures/gtil"
---

<objective>
The registration-not-refactor capstone (D-11): add `Backend::CubeclCpu` + `cubecl_cpu_case()` to the existing `RunnerCase` seam (dense f32/f64 → cubecl kernels per D-01/D-05; sparse f32/f64 → `treelite_gtil` scalar fallback per D-02), record per-cell `cubecl-kernel` vs `scalar-fallback` provenance (D-06), and drive the SAME frozen `fixtures/gtil/` golden matrix through cubecl to 1e-5 via a thin sibling test (`gtil_matrix_cubecl.rs`) — plus the SC2 two-run bit-identity determinism gate. This makes the cubecl CPU backend the validated GTIL engine (GPU-02) auditable cell-by-cell.

Purpose: Everything before this plan built the engine; this plan registers it and proves it against the committed instrument with NO change to the matrix iteration (the whole Phase-5 seam existed for this). The sibling test reuses `gtil_matrix.rs`'s dispatch body but constructs `cubecl_cpu_case()` and asserts its OWN provenance (never the `"scalar-cpu"` literal at `gtil_matrix.rs:474`). Per-cell provenance (D-06) guarantees "1e-5 on cubecl-cpu" can never quietly mean "validated on scalar fallback" — sparse/categorical cells are recorded as `scalar-fallback`, the dense numerical cells as `cubecl-kernel`. The "widen the f32 result AFTER predict" discipline is preserved verbatim (no pre-cast, Pitfall 6).
Output: A green `gtil_matrix_cubecl` matrix gate (1e-5 + provenance), a green `determinism` test (SC2), and the `Backend::CubeclCpu` registration — the phase's GPU-02 success criterion met and auditable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-CONTEXT.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md
@crates/treelite-harness/src/lib.rs
@crates/treelite-harness/tests/gtil_matrix.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Backend::CubeclCpu + cubecl_cpu_case() + per-cell provenance (D-06)</name>
  <files>crates/treelite-harness/Cargo.toml, crates/treelite-harness/src/lib.rs, crates/treelite-harness/src/manifest.rs</files>
  <read_first>
    - crates/treelite-harness/src/lib.rs:53-60 (the `Backend` enum + the reserved `CubeclCpu` comment at :59), :65-73 (the four fn-pointer slot types — reuse unchanged), :89-101 (`RunnerCase` struct — do NOT change), :107-134 (`scalar_cpu_case()` — the exact template incl. the "widen the f32 RESULT after predict" discipline at :111-116/:124-126)
    - crates/treelite-harness/src/manifest.rs:25-27 (`default_backend()` → `"scalar-cpu"`) and :55-60 (the per-cell `#[serde(default)] backend` field — already provenance-capable) and the `check_manifest` warn-only contract (:18-19)
    - crates/treelite-harness/Cargo.toml (add `treelite-cubecl` as a dev-dep, mirroring the existing `treelite-gtil`/`treelite-xgboost` dev-deps)
    - crates/treelite-cubecl/src/lib.rs (plan 04 — `treelite_cubecl::predict_cpu::<f32>`/`::<f64>` the dense slots call)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md "Code Examples" `cubecl_cpu_case()` excerpt + "Architectural Responsibility Map" (backend registration row; per-cell provenance row)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md "lib.rs (MOD — backend registration)" + "manifest.rs (MOD — per-cell provenance)"
  </read_first>
  <action>
    In `crates/treelite-harness/Cargo.toml`, add `treelite-cubecl = { path = "../treelite-cubecl" }` as a dev-dependency. In `crates/treelite-harness/src/lib.rs`: add the `CubeclCpu` variant to the `Backend` enum (replacing the reserved comment at :59). Add `pub fn cubecl_cpu_case() -> RunnerCase` mirroring `scalar_cpu_case()` EXACTLY except: `backend: Backend::CubeclCpu`; `dense_f32` calls `treelite_cubecl::predict_cpu::<f32>(model, data, num_row, cfg).map_err(|e| anyhow::anyhow!("{e}"))?` then widens the f32 RESULT to f64 with `out.into_iter().map(|v| v as f64).collect()` (D-01/D-05; NEVER a pre-cast — Pitfall 6); `dense_f64` calls `treelite_cubecl::predict_cpu::<f64>` and returns it; `sparse_f32`/`sparse_f64` keep pointing at `treelite_gtil::predict_sparse` (D-02 scalar fallback). Do NOT change the `RunnerCase` struct or the slot types. For per-cell provenance (D-06): the `backend` manifest field is already per-cell and `#[serde(default)]`; provenance is RECORDED at assertion time — the matrix sibling (Task 2) tags each cell `"cubecl-kernel"` (dense numerical) vs `"scalar-fallback"` (sparse, or categorical-fallback) based on the executed path. If a `check_manifest`-style drift warning is wanted, copy the existing warn-only `eprintln!` analog (manifest.rs:134-140) — NEVER fail the gate from a manifest check.
  </action>
  <verify>
    <automated>cargo build -p treelite-harness --tests && cargo test --workspace --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-harness --tests` compiles with `Backend::CubeclCpu` + `cubecl_cpu_case()`.
    - `grep -n 'CubeclCpu' crates/treelite-harness/src/lib.rs` shows the enum variant AND the case constructor.
    - `cubecl_cpu_case().dense_f32`/`dense_f64` call `treelite_cubecl::predict_cpu`; `sparse_f32`/`sparse_f64` call `treelite_gtil::predict_sparse` (verify by reading — D-02 fallback preserved).
    - The f32 dense slot widens the RESULT after predict (`map(|v| v as f64)`), NOT a pre-cast of input data.
    - The `RunnerCase` struct and slot type aliases are unchanged: `git diff crates/treelite-harness/src/lib.rs` shows additions only around the enum + a new `cubecl_cpu_case` fn (no edits to lines 65-101).
  </acceptance_criteria>
  <done>`Backend::CubeclCpu` + `cubecl_cpu_case()` registered additively (dense→cubecl, sparse→scalar fallback) with the result-widen-after-predict discipline; the seam's struct/slots untouched; provenance recording wired for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: gtil_matrix_cubecl sibling gate (1e-5 + provenance) + SC2 determinism</name>
  <files>crates/treelite-harness/tests/gtil_matrix_cubecl.rs, crates/treelite-cubecl/tests/determinism.rs</files>
  <read_first>
    - crates/treelite-harness/tests/gtil_matrix.rs:45 (imports), :290-381 (`run_cell` — the dtype/layout dispatch body to REUSE), :417-576 (the `gtil_matrix()` iteration + the `backend == "scalar-cpu"` assert at :474 the sibling must REPLACE with its own provenance assert), :531-576 (per-kind/per-layout 1e-5 epsilon asserts + dense==sparse parity)
    - crates/treelite-harness/src/lib.rs (plan 05 Task 1 — `cubecl_cpu_case()` to construct instead of `scalar_cpu_case()`)
    - crates/treelite-cubecl/tests/determinism.rs (the Wave-0 RED scaffold to turn green)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md "Pitfall 5" (SC2 disjoint-per-row writes → `out_a.to_bits() == out_b.to_bits()`) + "Validation Architecture" (the `gtil_matrix_cubecl` + `determinism` test commands) + "Wave 0 Gaps" (sibling test, not a refactor)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md "gtil_matrix_cubecl.rs (NEW — reuse the matrix runner)" + the "Caution" note about line :474
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-VALIDATION.md (the D-11 smell guard: sibling file or RunnerCase-list loop; do NOT reshape `gtil_matrix()`)
  </read_first>
  <action>
    Replace the Wave-0 stub in `crates/treelite-harness/tests/gtil_matrix_cubecl.rs` with the real sibling gate: reuse the `gtil_matrix.rs` dispatch body (`run_cell` dtype/layout arms, the per-kind 1e-5 epsilon asserts, dense==sparse parity) but construct `let case = cubecl_cpu_case();` and iterate the SAME frozen `fixtures/gtil/*.golden.json` cells, asserting each OWN-layout result within 1e-5 of its golden via `approx::assert_abs_diff_eq!(..., epsilon = 1e-5)`. Do NOT restructure `gtil_matrix()` (D-11 smell guard) — this is a thin new file; share helpers by `mod`-including or duplicating the small `run_cell`/golden-load helpers as needed, but never reshape the original test's per-cell iteration. REPLACE the `backend == "scalar-cpu"` assert (gtil_matrix.rs:474) with a per-cell PROVENANCE assert: dense numerical cells must record `"cubecl-kernel"`, sparse cells (and any categorical-fallback model) must record `"scalar-fallback"` (D-06) — the executed path, tagged at assertion time. Turn `crates/treelite-cubecl/tests/determinism.rs` green: run `treelite_cubecl::predict_cpu` twice on the same input and assert element-wise `out_a[i].to_bits() == out_b[i].to_bits()` (SC2). No pre-cast; no D-04 hedge.
  </action>
  <verify>
    <automated>cargo test -p treelite-harness --test gtil_matrix_cubecl && cargo test -p treelite-cubecl determinism && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness --test gtil_matrix_cubecl` passes — every frozen `fixtures/gtil/` cell asserts within 1e-5 on the cubecl backend, across model×preset×dtype×kind×{dense,sparse}×seed.
    - The sibling asserts per-cell provenance: dense numerical → `"cubecl-kernel"`, sparse/categorical-fallback → `"scalar-fallback"` (D-06); it does NOT assert the `"scalar-cpu"` literal.
    - `cargo test -p treelite-cubecl determinism` passes — two-run element-wise `.to_bits()` bit-identity (SC2).
    - `gtil_matrix.rs` is UNCHANGED: `git diff --stat crates/treelite-harness/tests/gtil_matrix.rs` shows no modifications (D-11 registration-not-refactor).
    - `cargo test --workspace` green — the scalar `gtil_matrix` gate, loader, and serializer gates all still pass (no regression; the binary `(num_row,1,1)` path unaffected).
  </acceptance_criteria>
  <done>The cubecl backend passes the identical frozen golden matrix to 1e-5 with per-cell provenance, the SC2 two-run determinism gate is green, the original `gtil_matrix.rs` is untouched, and the full workspace suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen goldens → cubecl assertion | the committed `fixtures/gtil/` vectors are the trusted oracle; cubecl output is compared against them |
| executed path → recorded provenance | the cell's tag (`cubecl-kernel`/`scalar-fallback`) must reflect what actually ran |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Repudiation | a scalar-fallback cell passing as a kernel result | mitigate | D-06 per-cell provenance: dense numerical cells assert `"cubecl-kernel"`, sparse/categorical-fallback assert `"scalar-fallback"`; the gate cannot silently credit a fallback as a kernel pass. |
| T-06-13 | Repudiation | non-determinism masquerading as a 1e-5 pass | mitigate | The `determinism.rs` SC2 gate asserts two-run `.to_bits()` bit-identity; kernels write disjoint per-row cells with no tree-axis atomic (plan 04 grep gate) so determinism holds structurally. |
| T-06-14 | Tampering | hidden refactor of the frozen matrix runner | mitigate | `git diff --stat gtil_matrix.rs` must show zero changes; the cubecl gate is a thin sibling (D-11). No frozen golden is regenerated — cubecl asserts the identical committed vectors. |
| T-06-SC | Tampering | crate-manager installs (cubecl/bytemuck reach the harness via dev-dep) | mitigate | Deps already APPROVED + compiled in plan 01 (the [SLOP] verdicts were wrong-registry false positives); adding the `treelite-cubecl` path dev-dep pulls no new external crate beyond the already-resolved tree. |
</threat_model>

<verification>
- `cargo test -p treelite-harness --test gtil_matrix_cubecl` green (frozen matrix to 1e-5 on cubecl + per-cell provenance).
- `cargo test -p treelite-cubecl determinism` green (SC2 two-run bit-identity).
- `git diff --stat crates/treelite-harness/tests/gtil_matrix.rs` shows no change (D-11).
- `cargo test --workspace` green (no regression to scalar/loader/serializer gates).
</verification>

<success_criteria>
- `Backend::CubeclCpu` + `cubecl_cpu_case()` register the cubecl kernels (dense f32/f64) and scalar fallback (sparse f32/f64) with no change to the matrix iteration.
- The cubecl backend passes the identical frozen `fixtures/gtil/` goldens within 1e-5 across every cell.
- Output is bit-identical across two runs of the same input (SC2).
- Each cell records `cubecl-kernel` vs `scalar-fallback` provenance (D-06); the original `gtil_matrix.rs` is untouched.
</success_criteria>

<output>
Create `.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-05-SUMMARY.md` when done.
</output>
