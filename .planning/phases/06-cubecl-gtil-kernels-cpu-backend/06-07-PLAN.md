---
phase: 06-cubecl-gtil-kernels-cpu-backend
plan: 07
type: execute
wave: 2
depends_on: ["06-06"]
files_modified:
  - fixtures/capture_gtil_matrix.py
  - fixtures/gtil/lgbm_numerical.model.bin
  - fixtures/gtil/mixedwidth.model.bin
  - fixtures/gtil/lgbm_numerical.*.golden.json
  - fixtures/gtil/mixedwidth.*.golden.json
autonomous: true
gap_closure: true
requirements: [GPU-02]

must_haves:
  truths:
    - "The gtil_matrix_cubecl gate exercises at least one non-kLT (LightGBM numerical, kLE) model class against frozen upstream-Treelite goldens within 1e-5 — proving the CR-01 operator-coverage fallback gate produces correct predictions on the model class that was silently wrong before"
    - "The gtil_matrix_cubecl gate exercises at least one mixed-width fixture whose threshold is not exactly representable in f32 (e.g. 0.1) within 1e-5 — proving the CR-02 f64-promoted comparison routes the f32-input cells to the same child as the scalar reference"
    - "Per-cell provenance (D-06) records the LightGBM (kLE) cells as scalar-fallback, never cubecl-kernel, while the dense kLT cells remain cubecl-kernel — the kernel_cells > 0 guard still holds"
    - "The full workspace test suite is green with the two new fixtures committed and part of the matrix"
  artifacts:
    - path: "fixtures/capture_gtil_matrix.py"
      provides: "build_lgbm_numerical_model + build_mixedwidth_model + their capture_model + _freeze_model_bin calls in main()"
      contains: "build_lgbm_numerical_model"
    - path: "fixtures/gtil/lgbm_numerical.model.bin"
      provides: "frozen v5 byte stream of the kLE LightGBM model the goldens were captured from"
    - path: "fixtures/gtil/mixedwidth.model.bin"
      provides: "frozen v5 byte stream of the f64-preset model with an f32-unrepresentable threshold"
  key_links:
    - from: "fixtures/gtil/lgbm_numerical.*.golden.json"
      to: "crates/treelite-harness/tests/gtil_matrix_cubecl.rs"
      via: "auto-discovered *.golden.json matrix cell loaded by the gate"
      pattern: "lgbm_numerical"
    - from: "fixtures/gtil/mixedwidth.*.golden.json"
      to: "crates/treelite-harness/tests/gtil_matrix_cubecl.rs"
      via: "auto-discovered *.golden.json matrix cell loaded by the gate"
      pattern: "mixedwidth"
---

<objective>
Lock the CR-01 and CR-02 fixes (shipped in 06-06) against REAL upstream-Treelite
goldens by adding the two previously-missing model classes to the frozen GTIL
matrix and re-running the cubecl matrix gate. Before 06-06 the matrix passed
green only because all 96 cells were XGBoost-derived (all kLT, no f32-boundary
thresholds, well-formed leaf vectors) — the green-while-buggy state that hid the
gaps. This plan makes the gate actually exercise the previously-uncovered classes.

A fix without a fixture that exercises the previously-uncovered class does not
close the gap. 06-06 added the unit/provenance tests; this plan adds the
end-to-end upstream-golden coverage that makes the matrix gate meaningful.

Purpose: prove "the cubecl CPU backend is validated to 1e-5" (GPU-02) holds for
the FULL supported model set — including the LightGBM (kLE) class that routes to
the scalar fallback and the mixed-width threshold that the f64-promoted
comparison now handles — not just XGBoost.

Output: two new frozen fixture families (a LightGBM numerical model with kLE
operators; an f64-preset model with an f32-unrepresentable threshold), each with
its model.bin and the full dtype × kind × {dense,sparse} × seed golden
cross-product, captured from upstream Treelite and committed read-only, plus a
green matrix gate + green workspace.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-VERIFICATION.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-CONTEXT.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend capture_gtil_matrix.py with the kLE LightGBM and mixed-width models, then capture + commit their goldens</name>
  <files>fixtures/capture_gtil_matrix.py, fixtures/gtil/lgbm_numerical.model.bin, fixtures/gtil/mixedwidth.model.bin, fixtures/gtil/lgbm_numerical.*.golden.json, fixtures/gtil/mixedwidth.*.golden.json</files>
  <read_first>
    - fixtures/capture_gtil_matrix.py:255-475 — the model-authoring section (build_binary_model / build_leaf_vector_model / build_large_margin_model), capture_model (the dtype × kind × {dense,sparse} × seed cross-product + the D-04 dense-NaN==CSR assert), _freeze_cell, _freeze_model_bin, and main() (where each model is authored + captured + its model.bin frozen). New models follow these patterns verbatim.
    - fixtures/capture_lightgbm.py:96-180 — how a LightGBM model is built/saved (booster.save_model to .txt) and loaded via treelite.frontend.load_lightgbm_model — the frontend entry point that yields a <f64,f64> preset with Operator::kLE numerical splits (the kLE model class CR-01 needs).
    - crates/treelite-lightgbm/src/lib.rs:265-280 — confirmation the loaded LightGBM numerical model emits kLE on every numerical split (so the matrix cell routes through the CR-01 fallback gate).
    - crates/treelite-harness/tests/gtil_matrix_cubecl.rs:340-459 — the gate auto-discovers EVERY fixtures/gtil/*.golden.json, deserializes {manifest.model}.model.bin via treelite_core::deserialize, and asserts 1e-5. The new fixtures must follow the exact {model}.{preset}.{indtype}.{kind}.{layout}.s{seed}.golden.json naming and carry a model.bin named after manifest.model so the gate loads them with no Rust change.
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-CONTEXT.md (D-06 provenance, D-02 fallback) — the kLE cells are EXPECTED to run the scalar fallback; that is correct and recorded as data, not a failure.
  </read_first>
  <action>
    Extend fixtures/capture_gtil_matrix.py with two new model authors, mirroring the existing
    build_*_model functions and main() wiring:

    (1) build_lgbm_numerical_model(seed): train a small NUMERICAL-only LightGBM regressor (no
    categorical_feature; a handful of leaves/rounds is enough), save it to a .txt, load it via
    `treelite.frontend.load_lightgbm_model` — this yields the <f64,f64> preset with Operator::kLE on
    every split (the CR-01 kLE class). Return (model, n_feat, cat_col) like the other authors.

    (2) build_mixedwidth_model(seed): author a model whose threshold is NOT exactly representable in
    f32 (e.g. a split at 0.1, or another f64 value that falls strictly between two adjacent f32
    values), so the f32-input cells stress CR-02's f64-promoted comparison. The simplest reliable
    route is a tiny single-or-few-split model built so at least one node's threshold is an
    f32-unrepresentable f64 (a LightGBM-frontend model gives the <f64,f64> preset and exact f64
    thresholds; if using the XGBoost frontend instead, verify the emitted threshold is f32-
    unrepresentable). Inject input rows that straddle the threshold (values just below and just above
    0.1 in both f32 and f64) so a wrong f32 narrowing would route to the wrong child and diverge from
    the upstream golden. Document in the docstring WHY 0.1 (or the chosen value) is f32-unrepresentable
    and that the f32-input cells are the CR-02 stressor.

    In main(): author each new model with SEEDS[0], call capture_model("lgbm_numerical", ...) and
    capture_model("mixedwidth", ...), and `_freeze_model_bin("lgbm_numerical.model.bin", ...)` /
    `_freeze_model_bin("mixedwidth.model.bin", ...)` so the gate deserializes the EXACT object that
    produced the goldens (no frontend re-drift) — exactly as large_margin does. Ensure each
    model's input matrix actually reaches the new model's split thresholds (extend _build_edge_matrix
    usage or the per-model row construction so the straddle rows are present); keep the capture-time
    D-04 dense-NaN==CSR parity assert intact.

    Then RUN the capture ON THE MAIN WORKTREE (the uv venv/pyproject live only here, per MEMORY.md —
    do NOT attempt this from a git worktree): `uv run python fixtures/capture_gtil_matrix.py`. Confirm
    it re-emits the existing binary/leaf_vec_mc/large_margin goldens unchanged AND writes the new
    lgbm_numerical.*.golden.json, mixedwidth.*.golden.json, lgbm_numerical.model.bin, and
    mixedwidth.model.bin. Commit all new fixtures read-only. Do NOT hand-edit any golden.json.
  </action>
  <verify>
    <automated>ls fixtures/gtil/lgbm_numerical.model.bin fixtures/gtil/mixedwidth.model.bin && ls fixtures/gtil/lgbm_numerical.*.golden.json | head -1 && ls fixtures/gtil/mixedwidth.*.golden.json | head -1 && grep -c 'build_lgbm_numerical_model\|build_mixedwidth_model' fixtures/capture_gtil_matrix.py</automated>
  </verify>
  <acceptance_criteria>
    - `fixtures/capture_gtil_matrix.py` defines `build_lgbm_numerical_model` and `build_mixedwidth_model` and calls `capture_model(...)` + `_freeze_model_bin(...)` for each in `main()`; `grep -c 'build_lgbm_numerical_model\|build_mixedwidth_model' fixtures/capture_gtil_matrix.py` ≥ 4 (2 defs + 2 calls minimum).
    - `fixtures/gtil/lgbm_numerical.model.bin` and `fixtures/gtil/mixedwidth.model.bin` exist (non-empty).
    - At least one `fixtures/gtil/lgbm_numerical.*.golden.json` and one `fixtures/gtil/mixedwidth.*.golden.json` exist, following the {model}.{preset}.{indtype}.{kind}.{layout}.s{seed}.golden.json naming.
    - The mixed-width model's docstring documents the f32-unrepresentable threshold rationale.
    - The capture script ran cleanly (its capture-time D-04 parity + gtil.predict signature asserts passed); the pre-existing binary/leaf_vec_mc/large_margin goldens are byte-unchanged (`git status --porcelain fixtures/gtil/binary.*.golden.json` shows no modifications).
  </acceptance_criteria>
  <done>The two new model classes (kLE LightGBM numerical; f32-unrepresentable-threshold mixed-width) are captured from upstream Treelite and committed as frozen matrix fixtures with their model.bin.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the cubecl matrix gate + full workspace; confirm the new classes are exercised and provenance is honest</name>
  <files>(verification only — no source edits expected; if the gate surfaces a real fidelity divergence, that is a CR-01/CR-02 escape to fix in 06-06's files)</files>
  <read_first>
    - crates/treelite-harness/tests/gtil_matrix_cubecl.rs:340-459 — the gate loop, the eprintln per-cell provenance report, and the `kernel_cells > 0` / `f32_cells > 0` / `f64_cells > 0` guards. After this plan the report must list lgbm_numerical cells as scalar-fallback and mixedwidth f32 cells passing the 1e-5 gate.
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-06-SUMMARY.md — the CR-01/CR-02/CR-03 fixes this gate now validates against real goldens.
  </read_first>
  <action>
    Run `cargo test -p treelite-harness --test gtil_matrix_cubecl -- --nocapture` and confirm: (a) it
    is GREEN; (b) the per-cell report includes lgbm_numerical.* cells tagged `(scalar-fallback)`
    (the CR-01 gate routed the kLE model to the scalar reference) and mixedwidth.* f32-input cells
    within 1e-5 (the CR-02 f64-promoted comparison matched upstream); (c) the kernel_cells > 0 guard
    still holds (the XGBoost dense kLT cells remain cubecl-kernel provenance). Then run
    `cargo test --workspace` and confirm the whole suite is green with the new fixtures present.

    If the matrix gate FAILS on a new cell, that is a real fidelity divergence the 06-06 fixes did
    not fully close — diagnose against the scalar twin (crates/treelite-gtil/src/lib.rs next_node /
    leaf-vector paths) and correct the fix in the 06-06-owned files (lib.rs gate, traversal.rs
    comparison). Do NOT loosen the 1e-5 epsilon, edit a golden, or delete a fixture to make the gate
    pass — the gate is the contract. Record the worst observed |delta| per new family in the summary.
  </action>
  <verify>
    <automated>cargo test -p treelite-harness --test gtil_matrix_cubecl -- --nocapture 2>&1 | tail -25 && cargo test --workspace 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness --test gtil_matrix_cubecl` passes (exit 0) with the new fixtures present.
    - The gate's stderr report shows at least one `lgbm_numerical.*` cell tagged `(scalar-fallback)` and at least one `mixedwidth.*` `f32` cell passing the 1e-5 gate.
    - The gate's kernel_cells > 0 guard still passes (XGBoost dense cells remain cubecl-kernel).
    - `cargo test --workspace` is fully green (0 failures) with the two new fixture families committed.
    - No golden.json was edited and the 1e-5 epsilon was not loosened (`grep -n '1e-5\|epsilon' crates/treelite-harness/tests/gtil_matrix_cubecl.rs` unchanged from 06-06).
  </acceptance_criteria>
  <done>The cubecl matrix gate now exercises the kLE LightGBM and mixed-width classes against frozen upstream goldens within 1e-5, provenance is honest per cell, and the full workspace is green — GPU-02 "validated to 1e-5" holds for the full supported model set.</done>
</task>

</tasks>

<verification>
- `cargo test -p treelite-harness --test gtil_matrix_cubecl -- --nocapture` — green; report lists lgbm_numerical (scalar-fallback) + mixedwidth (1e-5 pass) cells.
- `cargo test --workspace` — fully green with the new fixtures.
- `ls fixtures/gtil/lgbm_numerical.model.bin fixtures/gtil/mixedwidth.model.bin` — both present.
- `git status --porcelain fixtures/gtil/binary.*.golden.json fixtures/gtil/leaf_vec_mc.*.golden.json fixtures/gtil/large_margin.*.golden.json` — no modifications (re-capture is reproducible/idempotent).
- `git diff --stat -- crates/treelite-harness/tests/gtil_matrix.rs` — 0 lines (D-11).
</verification>

<success_criteria>
- The kLE LightGBM numerical model class and the f32-unrepresentable-threshold mixed-width class are part of the frozen GTIL matrix, captured from upstream Treelite.
- The cubecl matrix gate passes 1e-5 on both new classes and records honest per-cell provenance (kLE → scalar-fallback, dense kLT → cubecl-kernel).
- GPU-02 ("cubecl CPU backend validated to 1e-5") holds for the full supported model set, not just XGBoost.
- Full workspace green; gtil_matrix.rs byte-unchanged (D-11); no golden edited, no epsilon loosened.
</success_criteria>

<output>
Create `.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-07-SUMMARY.md` when done.
</output>
