---
phase: 06-cubecl-gtil-kernels-cpu-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/treelite-core/Cargo.toml
  - crates/treelite-core/src/tree_buf.rs
  - crates/treelite-cubecl/Cargo.toml
  - crates/treelite-cubecl/src/lib.rs
  - crates/treelite-cubecl/src/error.rs
  - crates/treelite-cubecl/tests/spike.rs
  - crates/treelite-cubecl/tests/upload.rs
  - crates/treelite-cubecl/tests/determinism.rs
  - crates/treelite-harness/tests/gtil_matrix_cubecl.rs
autonomous: true
requirements: [GPU-01, GPU-05]
user_setup: []

must_haves:
  truths:
    - "The workspace builds with the new treelite-cubecl crate registered and cubecl 0.10.0 + bytemuck pinned"
    - "TreeBuf::as_bytes() returns a zero-copy byte view of a numeric column"
    - "RED test scaffolds for spike, upload, determinism, and the cubecl matrix sibling compile and are #[ignore] with MISSING reason strings"
  artifacts:
    - path: "crates/treelite-cubecl/Cargo.toml"
      provides: "New kernel crate manifest with cubecl(cpu)/bytemuck/treelite-core/treelite-gtil/thiserror deps"
      contains: "cubecl"
    - path: "crates/treelite-cubecl/src/lib.rs"
      provides: "predict_cpu host-launcher signature stub + module exports"
      contains: "predict_cpu"
    - path: "crates/treelite-cubecl/src/error.rs"
      provides: "CubeclError thiserror enum"
      contains: "thiserror"
    - path: "crates/treelite-core/src/tree_buf.rs"
      provides: "TreeBuf::as_bytes() additive accessor gated on T: Pod"
      contains: "as_bytes"
    - path: "crates/treelite-harness/tests/gtil_matrix_cubecl.rs"
      provides: "RED sibling matrix test registering cubecl_cpu_case (ignored until kernels land)"
      contains: "cubecl"
  key_links:
    - from: "Cargo.toml [workspace.members]"
      to: "crates/treelite-cubecl"
      via: "member registration"
      pattern: "treelite-cubecl"
    - from: "crates/treelite-core/src/tree_buf.rs"
      to: "bytemuck::cast_slice"
      via: "as_bytes zero-copy view"
      pattern: "bytemuck::cast_slice"
---

<objective>
Wave 0 foundation: scaffold the new `treelite-cubecl` crate, pin `cubecl 0.10.0` (features `["cpu"]`) and `bytemuck` in `[workspace.dependencies]`, add the additive zero-copy `TreeBuf::as_bytes()` accessor (SC3/GPU-05 enabler), and create the RED test scaffolds (spike, upload, determinism, and the `gtil_matrix_cubecl` sibling) that all later waves turn green.

Purpose: Establish every contract the kernel work plugs into BEFORE any kernel exists, so Waves 1-4 are pure fill-in. Per Nyquist, the test files exist first (RED, `#[ignore]` with MISSING reason strings) so no later task ships without a pre-existing automated check. Registration-not-refactor (D-11): this plan only ADDS a crate, a workspace member, two deps, one accessor, and sibling test files — it touches nothing in the green scalar reference or the existing `gtil_matrix.rs`.
Output: A compiling workspace with `treelite-cubecl` registered, `TreeBuf::as_bytes()`, and four RED test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-CONTEXT.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This phase (across plans 01–05) creates the following NEW symbols. The plan-review-convergence source-grounding pass MUST exclude these from drift verification — they do not exist before this phase:

- Crate `treelite-cubecl` (`crates/treelite-cubecl/`) with modules `lib`, `error`, `upload`, `kernels::{traversal, postproc, default_raw, leaf_id, score_per_tree}`.
- `treelite_cubecl::predict_cpu::<F>(&Model, &[F], usize, &Config) -> Result<Vec<F>, CubeclError>` — the host launcher.
- `treelite_cubecl::CubeclError` — the crate's `thiserror` enum.
- `#[cube]` helpers: `descend`, `next_node`, the 10 postprocessor ports (`sigmoid`, `sigmoid_f64`, `exponential`, `exponential_standard_ratio`, `logarithm_one_plus_exp`, `softmax`, `softmax_f64`, `signed_square`, `hinge`, `multiclass_ova`, `identity`/`identity_multiclass`).
- `#[cube(launch)]` kernels: `predict_default`/`predict_raw` (in `default_raw.rs`), `predict_leaf_id`, `predict_score_per_tree`.
- `upload.rs` helpers: per-column ragged-SoA concatenation + `tree_node_offset`/`tree_leafvec_offset` prefix-sum index + `client.create` uploaders.
- `treelite_core::TreeBuf::<T: Pod>::as_bytes()` — additive zero-copy byte view.
- `treelite_harness::Backend::CubeclCpu` (new enum variant) + `treelite_harness::cubecl_cpu_case()` (new `RunnerCase` constructor).
- Per-cell provenance recording in `treelite-harness` manifest path (`cubecl-kernel` vs `scalar-fallback`).
- New test files: `crates/treelite-cubecl/tests/{spike,upload,determinism}.rs`, `crates/treelite-harness/tests/gtil_matrix_cubecl.rs`.
- Workspace: `cubecl = { version = "0.10.0", features = ["cpu"] }` and `bytemuck = { version = "1", features = ["derive"] }` in `[workspace.dependencies]`; `crates/treelite-cubecl` in `[workspace.members]`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold treelite-cubecl crate + pin cubecl/bytemuck + register member</name>
  <files>Cargo.toml, crates/treelite-cubecl/Cargo.toml, crates/treelite-cubecl/src/lib.rs, crates/treelite-cubecl/src/error.rs, crates/treelite-core/Cargo.toml</files>
  <read_first>
    - Cargo.toml (root — the `[workspace.members]` list and `[workspace.dependencies]` table to extend)
    - crates/treelite-gtil/Cargo.toml (the crate-manifest analog: path deps + workspace thiserror, dev-dep approx)
    - crates/treelite-gtil/src/lib.rs:892 (the `predict` host-entry signature shape `predict_cpu` mirrors: `(&Model, &[F], usize, &Config) -> Result<…>`)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md "Standard Stack" + "Package Legitimacy Audit" (cubecl 0.10.0 features=["cpu"], bytemuck 1; the slopcheck [SLOP] verdict is a wrong-registry false positive — APPROVED, do NOT remove)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md "crate scaffold" entry
  </read_first>
  <action>
    In root `Cargo.toml`: add `"crates/treelite-cubecl"` to `[workspace.members]`; add to `[workspace.dependencies]`: `cubecl = { version = "0.10.0", features = ["cpu"] }` and `bytemuck = { version = "1", features = ["derive"] }`. In `crates/treelite-core/Cargo.toml`: add `bytemuck = { workspace = true }` as a dependency (needed by `as_bytes` in Task 2). Create `crates/treelite-cubecl/Cargo.toml` with `name = "treelite-cubecl"`, `version.workspace`/`edition.workspace`/`license.workspace`, and dependencies `treelite-core = { path = "../treelite-core" }`, `treelite-gtil = { path = "../treelite-gtil" }`, `cubecl = { workspace = true }`, `bytemuck = { workspace = true }`, `thiserror = { workspace = true }`; dev-dep `approx = { workspace = true }`. Create `crates/treelite-cubecl/src/error.rs` defining a `CubeclError` `thiserror` enum mirroring the `GtilError` discipline — variants for `InvalidInputShape`, `FeatureIndexOutOfBounds`, `NodeIndexOutOfBounds`, and an `Unsupported` catch-all; no panic, no `anyhow` in this library crate. Create `crates/treelite-cubecl/src/lib.rs` with `pub mod error;` (re-export `CubeclError`), `pub mod upload;` and `pub mod kernels;` declared as empty placeholder modules with a doc comment, and a `pub fn predict_cpu<F>(model: &Model, data: &[F], num_row: usize, cfg: &Config) -> Result<Vec<F>, CubeclError>` stub that returns `Err(CubeclError::Unsupported(...))` for now (filled in Wave 3). Bound `F` minimally so the crate compiles. Do NOT pull `cubecl` symbols into the stub body yet — only declare the dependency so the crate resolves. Run `cargo build -p treelite-cubecl` to force the cubecl dependency tree to resolve (this is itself the strongest legitimacy proof per RESEARCH — the spike gate). FINALLY, pin the cubecl 0.10.0 API surface BEFORE any kernel code is authored (retires RESEARCH assumptions A1/A3 at the source): after the dependency resolves, confirm the canonical method names against the installed crate — run `cargo doc -p cubecl --no-deps` and/or grep the resolved source under `~/.cargo/registry/src/*/cubecl-*0.10.0*/` for (a) the device-upload entry point (`create_from_slice` vs `create` taking `cubecl::bytes::Bytes`) and (b) the base-2 exp intrinsic on the `Float` trait (`exp2`, else the `exp(x*ln2)`/`powf(2,x)` identity). Record the confirmed names in a short `// cubecl 0.10.0 API: upload=<name>, exp2=<name>` comment at the top of `crates/treelite-cubecl/src/upload.rs` (create the placeholder module file if needed) so Waves 2–3 author against the pinned names, not an assumption. This is name-pinning only — NOT a precision contingency (D-04).
  </action>
  <verify>
    <automated>cargo build -p treelite-cubecl --offline 2>/dev/null || cargo build -p treelite-cubecl</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-cubecl` succeeds (cubecl 0.10.0 + bytemuck resolve and compile).
    - `cargo build --workspace` succeeds (no regression; the new member does not break resolution).
    - `grep -n 'treelite-cubecl' Cargo.toml` shows the member registration.
    - `grep -n 'cubecl = ' Cargo.toml` shows the pinned `0.10.0` with `features = ["cpu"]`.
    - `crates/treelite-cubecl/src/error.rs` defines `pub enum CubeclError` deriving via `thiserror::Error`; `grep -c 'anyhow' crates/treelite-cubecl/src/error.rs` returns 0.
    - The cubecl 0.10.0 upload + base-2-exp method names are confirmed against the installed crate and recorded: `grep -n 'cubecl 0.10.0 API:' crates/treelite-cubecl/src/upload.rs` shows the pinned `upload=` and `exp2=` names (retires RESEARCH A1/A3 before Wave 2).
  </acceptance_criteria>
  <done>The workspace compiles with `treelite-cubecl` registered, cubecl 0.10.0 + bytemuck pinned, a `CubeclError` thiserror enum, and a `predict_cpu` host-launcher stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive TreeBuf::as_bytes() + RED test scaffolds (spike, upload, determinism, matrix sibling)</name>
  <files>crates/treelite-core/src/tree_buf.rs, crates/treelite-cubecl/tests/spike.rs, crates/treelite-cubecl/tests/upload.rs, crates/treelite-cubecl/tests/determinism.rs, crates/treelite-harness/tests/gtil_matrix_cubecl.rs</files>
  <read_first>
    - crates/treelite-core/src/tree_buf.rs:20 (the `T: Copy` enum bound — do NOT widen it) and :57-65 (`as_slice`, the zero-copy analog the new method composes over); module note :11-12 (broad bytemuck::Pod seam is Phase 9 — the new method is a NARROWER second impl, not a widening of the primary API)
    - crates/treelite-harness/tests/gtil_matrix.rs:45 (`use treelite_harness::{RunnerCase, scalar_cpu_case}`), :422-425 (`fn gtil_matrix()` + `let case = scalar_cpu_case()`), :474 (the hard `backend == "scalar-cpu"` assert the sibling must NOT copy)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md "Code Examples" `as_bytes()` + "Wave 0 Gaps"
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-VALIDATION.md "Wave 0 Requirements" + the D-11 smell-guard note
  </read_first>
  <behavior>
    - `TreeBuf::<f32>::from_owned(vec![1.0_f32, 2.0]).as_bytes().len() == 8` (2 × 4 bytes); `bytemuck::cast_slice::<u8, f32>(buf.as_bytes()) == [1.0, 2.0]` (round-trip exact).
    - `TreeBuf::<i32>::from_owned(vec![5_i32]).as_bytes().len() == 4`.
    - Each RED scaffold test is `#[ignore = "MISSING — Wave N must implement <thing>"]` and, when run with `--ignored`, currently fails or is a stub `todo!()` — they exist as Nyquist markers, not yet green.
  </behavior>
  <action>
    In `crates/treelite-core/src/tree_buf.rs`: add a SECOND `impl<T: Copy + bytemuck::Pod> TreeBuf<T>` block (do NOT touch the existing `impl<T: Copy>` or the enum's `T: Copy` bound) exposing `pub fn as_bytes(&self) -> &[u8] { bytemuck::cast_slice(self.as_slice()) }` with a doc comment citing GPU-05/SC3. Add a unit test module asserting the `behavior` round-trips above. Create the four RED test scaffolds: `crates/treelite-cubecl/tests/spike.rs` (a `#[test] #[ignore = "MISSING — Wave 1 spike: break-free descend + f64 in-kernel + exp2/softmax_f64 cast order"]` stub), `tests/upload.rs` (`#[ignore = "MISSING — Wave 2: per-column ragged SoA upload round-trip"]`), `tests/determinism.rs` (`#[ignore = "MISSING — Wave 4: two-run .to_bits() bit-identity (SC2)"]`), and `crates/treelite-harness/tests/gtil_matrix_cubecl.rs` — a SIBLING of `gtil_matrix.rs` whose body is, for now, a single `#[test] #[ignore = "MISSING — Wave 4: register cubecl_cpu_case() and assert the frozen matrix to 1e-5 + provenance"]` stub. Do NOT restructure `gtil_matrix.rs` (D-11 smell guard); the sibling is a thin new file. The sibling MUST eventually assert its OWN backend/provenance, NOT the `"scalar-cpu"` literal at `gtil_matrix.rs:474` — leave a comment recording that.
  </action>
  <verify>
    <automated>cargo test -p treelite-core tree_buf && cargo test --workspace --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-core tree_buf` passes the `as_bytes` round-trip behavior tests.
    - `cargo test --workspace --no-run` compiles ALL test targets including the four new RED scaffolds (they compile; they are `#[ignore]`).
    - `cargo test --workspace` stays green (the ignored scaffolds do not run; no existing test regresses — including the untouched `gtil_matrix.rs`).
    - `grep -c '^#' crates/treelite-core/src/tree_buf.rs >/dev/null; grep -n 'as_bytes' crates/treelite-core/src/tree_buf.rs` shows the new method.
    - `grep -n 'scalar-cpu' crates/treelite-harness/tests/gtil_matrix_cubecl.rs` returns nothing OR only a comment (never an active backend assert copied from the scalar test).
  </acceptance_criteria>
  <done>`TreeBuf::as_bytes()` exists (gated on `T: Pod`, primary `T: Copy` API untouched) with passing round-trip tests; four RED `#[ignore]` scaffolds compile and the workspace suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host Rust → cubecl device buffers | column bytes cross into device handles in later waves; this plan only establishes the byte-view accessor |
| dependency resolution → build | new third-party crates (cubecl, bytemuck) enter the build graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `cubecl`/`cubecl-cpu`/`bytemuck` supply chain | mitigate | RESEARCH Package Legitimacy Audit ran; the slopcheck [SLOP] verdicts are wrong-registry (PyPI) false positives — all three are crates.io packages verified via `cargo search` 0.10.0 and the vendored cubecl manual. Task 1's `cargo build -p treelite-cubecl` (compiles + resolves the real crates.io tree) is the strongest legitimacy proof; pinned exact-version deps in `[workspace.dependencies]`. |
| T-06-02 | Tampering | `TreeBuf::as_bytes` zero-copy cast | mitigate | Use `bytemuck::cast_slice` (validates alignment/size, no UB) gated on `T: Pod` — never a hand-rolled `transmute`. Only numeric POD columns get the impl; bool/enum columns are excluded (materialized to numeric on the host in Wave 2). |
| T-06-SC | Tampering | crate-manager installs (cubecl/bytemuck) | mitigate | Packages APPROVED in the RESEARCH audit (no `[ASSUMED]`/`[SUS]` survive — the [SLOP] verdicts are documented wrong-registry artifacts). No blocking human checkpoint required; the spike compile is the gate. |
</threat_model>

<verification>
- `cargo build --workspace` green (new member + deps resolve).
- `cargo test -p treelite-core tree_buf` green (`as_bytes` round-trips).
- `cargo test --workspace --no-run` compiles the four RED scaffolds.
- `cargo test --workspace` green (ignored scaffolds do not run; no regression).
</verification>

<success_criteria>
- `treelite-cubecl` crate exists, is a workspace member, and builds with cubecl 0.10.0 (cpu) + bytemuck pinned.
- `CubeclError` thiserror enum + `predict_cpu` host-launcher stub present.
- `TreeBuf::as_bytes()` additive accessor exists, gated on `T: Pod`, with passing round-trip tests; primary `T: Copy` API untouched.
- Four RED `#[ignore]` test scaffolds (spike, upload, determinism, gtil_matrix_cubecl sibling) compile; workspace suite green.
</success_criteria>

<output>
Create `.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-01-SUMMARY.md` when done.
</output>
