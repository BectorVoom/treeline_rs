---
phase: 06-cubecl-gtil-kernels-cpu-backend
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - crates/treelite-cubecl/src/upload.rs
  - crates/treelite-cubecl/src/kernels/postproc.rs
  - crates/treelite-cubecl/src/kernels/mod.rs
  - crates/treelite-cubecl/tests/upload.rs
  - crates/treelite-cubecl/tests/postproc.rs
autonomous: true
requirements: [GPU-05, GPU-01]
user_setup: []

must_haves:
  truths:
    - "Each forest column uploads as ONE device handle for the whole forest (no per-tree handle explosion) via TreeBuf::as_bytes + client.create"
    - "A per-tree (offset,len) prefix-sum index addresses tree t's node n in the concatenated buffers"
    - "All 10 postprocessors run as #[cube] helpers reproducing their scalar twins' verbatim cast order within 1e-5"
  artifacts:
    - path: "crates/treelite-cubecl/src/upload.rs"
      provides: "Per-column ragged-SoA concatenation + prefix-sum offset index + zero-copy client.create uploaders"
      contains: "client.create"
    - path: "crates/treelite-cubecl/src/kernels/postproc.rs"
      provides: "10 #[cube] postprocessor ports incl. softmax f32/f64 split, exp2, f64 sigmoid/hinge twins"
      contains: "#[cube]"
    - path: "crates/treelite-cubecl/tests/postproc.rs"
      provides: "Each #[cube] postprocessor vs its scalar twin to 1e-5"
      contains: "assert_abs_diff_eq"
  key_links:
    - from: "crates/treelite-cubecl/src/upload.rs"
      to: "treelite_core::TreeBuf::as_bytes"
      via: "zero-copy per-column byte view"
      pattern: "as_bytes"
    - from: "crates/treelite-cubecl/src/kernels/postproc.rs"
      to: "treelite_gtil::postprocessor"
      via: "verbatim cast-order reproduction asserted to 1e-5"
      pattern: "softmax"
---

<objective>
Build the two faithful-re-expression building blocks the dense kernels assemble in Wave 3 (plan 04): (1) the per-column ragged-SoA host→device upload (SC3/GPU-05) — one handle per column for the whole forest with a prefix-sum offset index, and (2) all ten postprocessors as `#[cube]` helpers with verbatim mixed-precision cast order (D-03), each unit-tested against its `treelite_gtil::postprocessor` scalar twin to 1e-5.

Purpose: Separate the plumbing (upload) and the math (postprocessors) from the traversal-kernel assembly so each is independently verifiable. The cast order IS the 1e-5 contract (CR-01) — these `#[cube]` ports reproduce `postprocessor.rs` line-by-line, never "simplify" to all-f32 or all-f64. The upload reuses the Wave-2-confirmed `client.create`/`bytemuck::cast_slice` path and `TreeBuf::as_bytes()` from plan 01, building the one-handle-per-column layout SC3 mandates (no per-tree handle explosion).
Output: A green `upload` test (forest round-trips, one handle/column) and a green `postproc` test (all 10 postprocessors within 1e-5 of their scalar twins).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-CONTEXT.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md
@crates/treelite-gtil/src/postprocessor.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-column ragged-SoA upload + prefix-sum offset index (SC3/GPU-05)</name>
  <files>crates/treelite-cubecl/src/upload.rs, crates/treelite-cubecl/src/kernels/mod.rs, crates/treelite-cubecl/tests/upload.rs</files>
  <read_first>
    - crates/treelite-core/src/tree.rs:17-81 (the SoA `Tree<T>` columns: `cleft`/`cright`/`split_index` i32, `threshold`/`leaf_value`/`leaf_vector` T, `leaf_vector_begin`/`leaf_vector_end` u64, `default_left` bool, `node_type` enum; `num_nodes` at :72; `has_categorical_split` at :70)
    - crates/treelite-core/src/tree_buf.rs:57-65 (`as_slice`) and the new `as_bytes` (plan 01)
    - crates/treelite-gtil/src/lib.rs:902-926 (`predict`'s up-front `num_feature`/buffer-length validation — mirror this BEFORE any client.create)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md "Pattern 3: Per-column ragged-SoA forest upload" + "Code Examples" upload excerpt + "Pitfall 4" (bool→u32, enum→i32 discriminant) + "Don't Hand-Roll" (cast_slice/Bytes/client.create)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md "upload.rs" entry (columns list + offset index)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/Backend-Agnostic_Buffer_Slicing...md and Batch-Tree_Reorganization_Algorithm.md (offset arithmetic)
    - /home/user/Documents/workspace/optimisor/manual/ZERO_COPY_TRANSMUTATION_CUBECL.md (cast_slice → Bytes → client.create → read-back)
  </read_first>
  <behavior>
    - Concatenating a 3-tree forest's `threshold` column produces one `Vec` of length `sum(num_nodes)` and ONE device handle; `tree_node_offset` is a prefix sum `[0, n0, n0+n1, n0+n1+n2]` so `concat[tree_node_offset[t] + n]` addresses tree t's node n.
    - `default_left` (bool) materializes to a `Vec<u32>` of 0/1; `node_type` materializes to an `i32` discriminant column — both upload as numeric columns (bool is not a cubecl Array element).
    - A round-trip test: upload each column, `client.read` it back via `bytemuck::cast_slice`, and assert it equals the host concatenation element-wise.
    - Host-side validation: a model whose `split_index` exceeds `num_feature`, or a buffer-length mismatch, returns a typed `CubeclError` BEFORE any `client.create` (no OOB device write).
  </behavior>
  <action>
    Promote `crates/treelite-cubecl/src/kernels/mod.rs`'s reserved modules as needed and add `pub mod upload;` to `lib.rs`. In `upload.rs`, for each numeric column (`cleft`/`cright`/`split_index` as `i32`, `threshold`/`leaf_value`/`leaf_vector` as `F`, `leaf_vector_begin`/`leaf_vector_end` as `u32` — narrow from u64 at materialization, mirroring the kernel's u32-index discipline), concatenate the column across all trees into one host `Vec`, then `bytemuck::cast_slice::<_, u8>` → `cubecl::bytes::Bytes::from_bytes_vec` → `client.create` to get ONE handle per column. Materialize `default_left` (bool) as a `Vec<u32>` of 0/1 and `node_type` as an `i32` discriminant column (Pitfall 4) and upload those as numeric columns. Build a `tree_node_offset: Vec<u32>` prefix sum over `num_nodes` and a `tree_leafvec_offset: Vec<u32>` prefix sum for the multiclass leaf-vector value column. Use the new `TreeBuf::as_bytes()` where the column is already the right numeric type; use a materialized `Vec` for bool/enum/u64-narrowed columns. BEFORE any `client.create`, validate `num_feature`/buffer-lengths up front (mirror `predict` lib.rs:902-926) returning a typed `CubeclError` — never an OOB device op. Return a struct bundling the per-column handles + the two offset index vectors. Turn the Wave-0 `tests/upload.rs` scaffold green with the round-trip + validation behaviors.
  </action>
  <verify>
    <automated>cargo test -p treelite-cubecl --test upload</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-cubecl --test upload` passes (forest round-trips; one handle/column; offset index addresses tree t node n; validation rejects bad split_index / length mismatch with a typed `CubeclError`).
    - `grep -c 'transmute' crates/treelite-cubecl/src/upload.rs` returns 0 (cast_slice only, no hand-rolled transmute).
    - The upload struct exposes ONE handle per column (no `Vec<Handle>` keyed per-tree): `grep -c 'per.*tree.*handle\|Vec<Handle>' crates/treelite-cubecl/src/upload.rs` returns 0.
    - `cargo test --workspace` stays green.
  </acceptance_criteria>
  <done>Each forest column uploads as one device handle with a prefix-sum offset index; bool/enum columns materialize to numeric; host-side length validation precedes every device op; the round-trip test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All 10 postprocessors as #[cube] helpers vs scalar twins (verbatim cast order, 1e-5)</name>
  <files>crates/treelite-cubecl/src/kernels/postproc.rs, crates/treelite-cubecl/src/kernels/mod.rs, crates/treelite-cubecl/tests/postproc.rs</files>
  <read_first>
    - crates/treelite-gtil/src/postprocessor.rs (ALL 10 fns + `*_f64` twins; the line anchors per the PATTERNS table: identity :50, identity_multiclass :61, sigmoid :74 / sigmoid_f64 :91, exponential :103 / _f64 :109, exponential_standard_ratio :124 / twin ~:130, logarithm_one_plus_exp :146 / _f64 :152, softmax :175 / softmax_f64 :227, signed_square :267 / _f64 :273, hinge :285, multiclass_ova :301 / _f64 :312)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md "postproc.rs" entry (the sigmoid + softmax_f64 cast-order excerpts that MUST be reproduced verbatim)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md "Pitfall 1" (associated-fn math), "Pitfall 2" (exp2 — use the form the spike resolved in plan 02), "Pitfall 3" (softmax f32-max/f64-norm mixed locals)
    - crates/treelite-cubecl/tests/spike.rs (plan 02 — reuse the resolved `exp2` form and the confirmed softmax_f64 mixed-width pattern)
  </read_first>
  <behavior>
    - `sigmoid`/`sigmoid_f64`, `exponential`/`_f64`, `exponential_standard_ratio` (+twin), `logarithm_one_plus_exp`/`_f64`, `signed_square`/`_f64`, `hinge`, `multiclass_ova`/`_f64`, `softmax`/`softmax_f64`, `identity`/`identity_multiclass` each have a `#[cube]` port whose output matches the scalar twin within 1e-5 over a representative input row (including a large-margin softmax row to exercise the CR-01 cast order).
    - `softmax`/`softmax_f64` keep `max_margin`/`t`/divisor as `f32` and `norm_const` as `f64` exactly where the scalar twin does (the f64 variant keeps cells as f64).
    - `exponential_standard_ratio` uses the base-2 form resolved by the plan-02 spike.
    - `sigmoid_alpha`/`ratio_c` are `f32` model fields cast into `F` at the call site (mirroring `sigmoid_f64`'s `alpha as f64`).
  </behavior>
  <action>
    In `crates/treelite-cubecl/src/kernels/postproc.rs`, port all 10 postprocessors (plus their `*_f64` twins) as `#[cube]` helpers reproducing `postprocessor.rs` cast order line-by-line: `F::exp`/`F::powf`/`F::abs`/`F::copysign` as ASSOCIATED functions (Pitfall 1); softmax keeps explicit `f32`-typed `max_margin`/`t`/divisor locals with f64 cells in the f64 variant (Pitfall 3); `exponential_standard_ratio` uses the spike-resolved `exp2` form; `hinge`/`sigmoid` f64 twins run in f64 (the 05-06 contract). Declare `pub mod postproc;` in `kernels/mod.rs`. Author `tests/postproc.rs` asserting EACH `#[cube]` port within 1e-5 of its scalar twin via `approx::assert_abs_diff_eq!(..., epsilon = 1e-5)` — include a large-margin softmax row and a `multiclass_ova` row. Do NOT collapse any postprocessor to a single-precision rewrite.
  </action>
  <verify>
    <automated>cargo test -p treelite-cubecl --test postproc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-cubecl --test postproc` passes — all 10 postprocessors (+ `_f64` twins) within 1e-5 of their `treelite_gtil::postprocessor` scalar twins.
    - `grep -c '\.exp()\|\.ln()\|\.is_nan()\|\.copysign(\|\.powf(' crates/treelite-cubecl/src/kernels/postproc.rs` returns 0 (associated-fn math only).
    - `softmax`/`softmax_f64` ports contain explicit `f32` `max_margin`/`t`/divisor and `f64` `norm_const` (verify by reading: the mixed-precision split is present, not collapsed).
    - `cargo test --workspace` stays green.
  </acceptance_criteria>
  <done>All 10 postprocessors run as `#[cube]` helpers with verbatim cast order, each asserted within 1e-5 of its scalar twin; no single-precision collapse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host model columns → device handles | per-column concatenation crosses into `client.create` device buffers |
| f32 model fields → f64 kernel locals | `sigmoid_alpha`/`ratio_c` cast into the kernel element width |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering / DoS | malformed model in `upload` (bad `split_index`, child id, leaf-vector offset) | mitigate | Validate `num_feature`/buffer-lengths up front (mirror `predict` lib.rs:902-926) returning typed `CubeclError` (`FeatureIndexOutOfBounds`/`NodeIndexOutOfBounds`) BEFORE any `client.create` — no OOB device write. |
| T-06-07 | Tampering | `bytemuck::cast_slice` column upload | mitigate | `bytemuck::cast_slice` validates alignment/size; bool/enum columns materialized to numeric `Vec` first (not raw-cast). No hand-rolled transmute. |
| T-06-08 | Repudiation | a postprocessor silently collapsed to single precision | mitigate | Each `#[cube]` port asserted against the scalar twin to 1e-5 incl. a large-margin softmax row; a collapsed cast order fails the gate (this is exactly the CR-01 band). |
</threat_model>

<verification>
- `cargo test -p treelite-cubecl --test upload` green (one handle/column, offset index, validation).
- `cargo test -p treelite-cubecl --test postproc` green (10 postprocessors within 1e-5).
- `cargo test --workspace` green (no regression).
</verification>

<success_criteria>
- Each forest column uploads as one device handle (no per-tree explosion) with a prefix-sum offset index; bool/enum columns materialize to numeric; host validation precedes device ops.
- All 10 postprocessors run as `#[cube]` helpers with verbatim cast order, each within 1e-5 of its scalar twin.
</success_criteria>

<output>
Create `.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-03-SUMMARY.md` when done.
</output>
