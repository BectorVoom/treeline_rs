---
phase: 06-cubecl-gtil-kernels-cpu-backend
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - crates/treelite-cubecl/src/kernels/mod.rs
  - crates/treelite-cubecl/src/kernels/traversal.rs
  - crates/treelite-cubecl/tests/spike.rs
autonomous: true
requirements: [GPU-01, GPU-02]
user_setup: []

must_haves:
  truths:
    - "A break-free #[cube] descent kernel descends a 2-tree numerical forest with NO continue and one unit per row"
    - "A 2-tree numerical default-kind spike kernel matches treelite_gtil::predict within 1e-5 on f32 AND f64 input"
    - "A softmax_f64 micro-kernel and an exponential_standard_ratio micro-kernel reproduce their scalar twins' cast order within 1e-5 in-kernel"
  artifacts:
    - path: "crates/treelite-cubecl/src/kernels/traversal.rs"
      provides: "#[cube] break-free descend + next-node helper (numerical, no continue, associated-fn math)"
      contains: "#[cube]"
    - path: "crates/treelite-cubecl/tests/spike.rs"
      provides: "D-04 confirmation: descend + f64 + exp2/softmax_f64 cast order vs scalar twins"
      contains: "assert_abs_diff_eq"
  key_links:
    - from: "crates/treelite-cubecl/tests/spike.rs"
      to: "treelite_gtil::predict"
      via: "1e-5 assertion against the scalar reference"
      pattern: "treelite_gtil::predict"
    - from: "crates/treelite-cubecl/src/kernels/traversal.rs"
      to: "cubecl Float associated fns"
      via: "F::is_nan / no continue / if-statement routing"
      pattern: "F::is_nan"
---

<objective>
The mandatory minimal-kernel spike (D-04 CONFIRMATION, not a go/no-go gate). Build a break-free `#[cube]` descent helper and a tiny 2-tree numerical `default`-kind launch kernel, plus standalone `softmax_f64` and `exponential_standard_ratio` micro-kernels, and assert all of them within 1e-5 of their `treelite_gtil` scalar twins — locking down the four cubecl API-surface assumptions (A1 `exp2`, A2 mixed-width f32/f64 locals, A3 `client.create`, A4 import paths) BEFORE the full port.

Purpose: The user has repeatedly validated cubecl reproduces scalar precision in-kernel including f64 and mixed precision (memory `cubecl-precision-validated`). This spike confirms the CONTROL-FLOW SHAPE (no `continue`, `if`-statement routing, associated-fn math) and one postprocessor's cast order compile and run on `CpuRuntime` — it is NOT expected to fail and MUST NOT carry "fall back to host postprocessors if it misses 1e-5" contingency branches (D-04). Per the registration-not-refactor design (D-11), the descent helper authored here is reused verbatim by the full traversal kernels in Wave 3.
Output: A green `spike` test that turns the RED Wave-0 spike scaffold green and retires assumptions A1–A4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-CONTEXT.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md
@.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md
@crates/treelite-gtil/src/lib.rs
@crates/treelite-gtil/src/postprocessor.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: #[cube] break-free numerical descent helper + next-node routing</name>
  <files>crates/treelite-cubecl/src/kernels/mod.rs, crates/treelite-cubecl/src/kernels/traversal.rs</files>
  <read_first>
    - crates/treelite-gtil/src/lib.rs:432-502 (`evaluate_tree` — the line-by-line descent spec) and :333-355 (`next_node` — XGBoost always kLT: `fvalue < threshold ? left : right`)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md "Pattern 1: Break-free data-dependent tree descent" (the `descend<F: Float>` shape) + "Anti-Patterns to Avoid" + "Pitfall 1" (associated-fn math `F::is_nan`/`F::exp`) + "Pitfall 4" (bool `default_left` as `u32`)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md "traversal.rs" entry (the `let mut next; if … { next = … }` statement-routing rule; base/row_off ragged-SoA indexing)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/Cubecl_loop_control.md (no `continue`; `while`/`break` allowed)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/Cubecl_conditionals.md (if-statement, not if-expression value)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_solution_guide/mismatched types.md (E0308 if-expr, E0599 method-math) and "calling a normal Rust function" (E0433 helpers must be #[cube])
  </read_first>
  <action>
    Create `crates/treelite-cubecl/src/kernels/mod.rs` declaring `pub mod traversal;` (and reserving `pub mod postproc;`/`pub mod default_raw;`/`pub mod leaf_id;`/`pub mod score_per_tree;` for later waves as empty placeholders). In `traversal.rs`, author a `#[cube] fn descend<F: Float>(cleft: &Array<i32>, cright: &Array<i32>, split_index: &Array<i32>, threshold: &Array<F>, default_left: &Array<u32>, base: u32, row_off: u32, input: &Array<F>) -> u32` that ports `evaluate_tree` break-free: a `while cleft[(base+nid) as usize] != -1` loop (NO `continue`), reading `fi = split_index[...]`, `fv = input[(row_off + fi as u32) as usize]`; route via `let mut next: i32 = cright[...]; if F::is_nan(fv) { … default child via default_left[...] == 1u32 } else { if fv < threshold[...] { next = cleft[...]; } }` — `if`-STATEMENTS only, never an `if`-expression value; `F::is_nan` as an ASSOCIATED function. Use `u32`/`i32` for indices, casting to `usize` only at the array-index site. Mark every helper `#[cube]` (a plain Rust fn fails E0433). Add `base`/`row_off` offset arithmetic so the helper addresses the ragged-SoA concatenated columns `concat[tree_node_offset[t] + nid]` (the concatenation itself lands in Wave 2 upload; here the helper just takes the offsets as params). Import cubecl symbols via `use cubecl::prelude::*;`. Confirm A4 import path is `cubecl::cpu::{CpuRuntime, CpuDevice}` by grepping the installed crate if the manual path does not compile.
  </action>
  <verify>
    <automated>cargo build -p treelite-cubecl</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p treelite-cubecl` compiles the `#[cube] descend` helper (proves no-`continue`, `if`-statement routing, and associated-fn math all compile on cubecl 0.10.0).
    - `grep -c 'continue' crates/treelite-cubecl/src/kernels/traversal.rs` returns 0.
    - `grep -n 'F::is_nan' crates/treelite-cubecl/src/kernels/traversal.rs` shows associated-fn math (no `.is_nan()` method call: `grep -c '\.is_nan()' crates/treelite-cubecl/src/kernels/traversal.rs` returns 0).
    - `grep -c '#\[cube\]' crates/treelite-cubecl/src/kernels/traversal.rs` is ≥ 1.
  </acceptance_criteria>
  <done>A compiling `#[cube]` break-free descent helper exists with associated-fn NaN routing, `if`-statement child selection, and ragged-SoA offset indexing — the verbatim shape Wave 3 reuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spike — 2-tree default kernel + softmax_f64/exp2 micro-kernels vs scalar twins (1e-5)</name>
  <files>crates/treelite-cubecl/tests/spike.rs</files>
  <read_first>
    - crates/treelite-gtil/src/lib.rs:643-741 (`predict_preset` — serial tree accumulation the 2-tree default kernel mirrors) and :892 (`predict` host entry the spike asserts against)
    - crates/treelite-gtil/src/postprocessor.rs:124-130 (`exponential_standard_ratio` — `exp2` base-2) and :227-251 (`softmax_f64` — f32 `max_margin`/`t`/divisor + f64 `norm_const`, the CR-01 cast order)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-RESEARCH.md "Pitfall 2" (exp2: try `F::exp2`; else `F::exp(x*LN_2)` or `F::powf(2,x)` in element width) + "Pitfall 3" (softmax mixed f32/f64 locals) + "Open Questions §3 Spike scope" + "Assumptions Log A1/A2/A3/A4"
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-PATTERNS.md "postproc.rs" sigmoid/softmax_f64 cast-order excerpts
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/Cubecl_multi_threading.md (`ABSOLUTE_POS`, `CubeCount::Static((n+255)/256,1,1)`, `CubeDim{x:256}`, bounds check) + Cubecl_generics.md (f64 on CpuRuntime, `client.create`/`read`, `launch::<F,R>`)
    - .planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-VALIDATION.md (spike RED scaffold; `cargo test -p treelite-cubecl` sampling)
  </read_first>
  <behavior>
    - Build a small 2-tree numerical `<f32,f32>` Model in-test (via `treelite_builder` or a hand-built `Model`), run a `#[cube(launch)]` default-kind kernel (descend + serial accumulate + base-score add + identity/sigmoid postproc) over a tiny input matrix, and assert `assert_abs_diff_eq!(kernel_out, treelite_gtil::predict::<f32>(&model, …), epsilon = 1e-5)` element-wise.
    - Repeat the same model+input with `F = f64` input and a `<f64,f64>` preset; assert within 1e-5 of `treelite_gtil::predict::<f64>` (retires A2 — f64 in-kernel).
    - A standalone `softmax_f64` micro-kernel over a 3-class row reproduces `postprocessor::softmax_f64`'s output within 1e-5 (retires A2 mixed-width f32/f64 locals in one f64 kernel).
    - A standalone `exponential_standard_ratio` micro-kernel reproduces `postprocessor::exponential_standard_ratio` within 1e-5 (retires A1 — the chosen `exp2` form: document whether `F::exp2` exists or the `exp(x*ln2)`/`powf(2,x)` identity was used).
  </behavior>
  <action>
    In `crates/treelite-cubecl/tests/spike.rs` (remove the `#[ignore]` Wave-0 marker), author the spike: upload the 2-tree forest's columns via `bytemuck::cast_slice` → `cubecl::bytes::Bytes::from_bytes_vec` → `client.create` (confirming A3 `client.create` is the upload entry; grep the installed cubecl 0.10.0 API if `create_from_slice` is referenced), launch a `predict_default` `#[cube(launch)]` kernel with `CpuRuntime` (one unit per row via `ABSOLUTE_POS`, `if row < num_row` bound, serial `for tree_id` loop calling the Wave-2 Task-1 `descend` helper), read back via `client.read` + `bytemuck::cast_slice`, and assert against `treelite_gtil::predict`. Author the `softmax_f64` and `exponential_standard_ratio` micro-kernels reproducing their scalar twins' EXACT cast order (f32 `max_margin`/`t`/divisor with f64 cells for softmax; the chosen exp2 form for the ratio). Add a brief in-test comment recording which `exp2` form was used and that A1–A4 are retired. Do NOT add any "if it misses 1e-5, fall back to host" branch (D-04) — the spike asserts pass.
  </action>
  <verify>
    <automated>cargo test -p treelite-cubecl --test spike</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-cubecl --test spike` passes (no `#[ignore]` remaining on the spike tests).
    - The spike asserts via `approx::assert_abs_diff_eq!(..., epsilon = 1e-5)` against `treelite_gtil::predict` (f32 AND f64) and the `softmax_f64`/`exponential_standard_ratio` scalar twins.
    - `grep -c 'fall.*back\|contingency\|host.*postproc' crates/treelite-cubecl/tests/spike.rs` returns 0 (no D-04 hedge padding).
    - A comment in `spike.rs` records the resolved `exp2` form and that assumptions A1–A4 are retired.
    - `cargo test --workspace` stays green (no regression).
  </acceptance_criteria>
  <done>The spike is green: break-free descent, f64 in-kernel, and the softmax/exp2 cast orders all reproduce the scalar twins within 1e-5 on `CpuRuntime`; A1–A4 retired; no fallback-contingency padding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host Rust → cubecl device | the 2-tree forest + input matrix cross into device handles (`client.create`) |
| kernel index arithmetic → Array reads | `base+nid`/`row_off+fi` index the device arrays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering / DoS | `descend` array indexing (`split_index`/child ids) | mitigate | The spike uses a hand-built well-formed 2-tree model; full-model host-side length validation lands in Wave 3. The kernel's `if row < num_row` bound is present; `base`/`row_off` offsets come from the prefix-sum index. No OOB write — each row writes its own output cell. |
| T-06-04 | Tampering | `bytemuck::cast_slice` upload in the spike | mitigate | Use `bytemuck::cast_slice` (validates alignment/size), never a hand-rolled transmute (Don't-Hand-Roll). |
| T-06-05 | Repudiation | spike passing on a wrong cast order | mitigate | The spike asserts against the scalar `postprocessor.rs` twins (the 1e-5 measuring stick), so a collapsed all-f32/all-f64 softmax would fail the 1e-5 gate — the cast order is verified, not assumed. |
</threat_model>

<verification>
- `cargo test -p treelite-cubecl --test spike` green (descent + f64 + softmax_f64 + exp2 within 1e-5).
- `cargo build -p treelite-cubecl` green (the `#[cube] descend` helper compiles).
- `cargo test --workspace` green (no regression).
</verification>

<success_criteria>
- A break-free `#[cube]` descent helper compiles (no `continue`, associated-fn math, `if`-statement routing).
- The spike asserts a 2-tree default kernel within 1e-5 of `treelite_gtil::predict` on f32 AND f64 input.
- `softmax_f64` and `exponential_standard_ratio` micro-kernels reproduce their scalar twins within 1e-5; A1–A4 retired; the resolved `exp2` form documented.
- No D-04 fallback-contingency padding present.
</success_criteria>

<output>
Create `.planning/phases/06-cubecl-gtil-kernels-cpu-backend/06-02-SUMMARY.md` when done.
</output>
