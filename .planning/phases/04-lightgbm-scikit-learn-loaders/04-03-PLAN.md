---
phase: 04-lightgbm-scikit-learn-loaders
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/capture_lightgbm.py
  - fixtures/capture_sklearn.py
  - fixtures/lightgbm_numerical.golden.json
  - fixtures/lightgbm_categorical.txt
  - fixtures/lightgbm_categorical.golden.json
  - fixtures/sklearn_rf.golden.json
  - fixtures/sklearn_gb.golden.json
  - fixtures/sklearn_iforest.golden.json
  - fixtures/sklearn_histgb_numerical.golden.json
  - fixtures/sklearn_histgb_categorical.golden.json
autonomous: true
requirements: [LGB-01, LGB-02, SKL-01, SKL-02, SKL-03, SKL-04]
user_setup:
  - service: python-capture-env
    why: "One-time frozen golden capture needs scikit-learn + lightgbm (NOT installed); treelite==4.7.0 + numpy already present"
    env_vars: []
    dashboard_config:
      - task: "uv pip install scikit-learn lightgbm (capture-only; never a CI/runtime dep)"
        location: "main worktree (venv/pyproject untracked, absent from worktrees)"
must_haves:
  truths:
    - "Each estimator family has a frozen golden JSON whose prediction vector is treelite.gtil.predict output (NOT framework predict)"
    - "Each golden carries a manifest with sklearn/lightgbm/treelite/numpy versions + seed"
    - "The IsolationForest golden equals -clf.score_samples(X) (cross-checked at capture time)"
    - "sklearn goldens carry the node arrays in the importer.py dtype contract"
  artifacts:
    - path: "fixtures/capture_sklearn.py"
      provides: "one-time frozen sklearn capture (RF/ET/GB/IF/HistGB array dumps + treelite-GTIL golden + manifest)"
      contains: "treelite.gtil.predict"
    - path: "fixtures/capture_lightgbm.py"
      provides: "one-time frozen LightGBM capture (text model + treelite-GTIL golden + manifest)"
      contains: "treelite.gtil.predict"
    - path: "fixtures/sklearn_iforest.golden.json"
      provides: "IsolationForest golden = -score_samples"
  key_links:
    - from: "fixtures/capture_sklearn.py"
      to: "treelite.gtil.predict"
      via: "golden capture (D-07)"
      pattern: "gtil\\.predict"
---

<objective>
Freeze the per-estimator goldens every Phase-4 slice asserts against (D-06/D-07). A single `uv run python` session fits each sklearn estimator family + builds/loads each LightGBM model, dumps the node arrays (sklearn) + input matrix + a frozen manifest, captures the golden prediction vector from **upstream Treelite GTIL** (`treelite.gtil.predict`, NOT the framework's `predict()`), and commits everything read-only. CI never regenerates.

Purpose: This is the fixtures gate for all estimator-slice golden asserts (Plans 04-08). It runs on the main worktree because the venv/pyproject are untracked. IsolationForest is the canonical "Treelite ≠ framework" case (golden == `-clf.score_samples`).
Output: capture scripts + per-estimator golden JSONs (input matrix + treelite-GTIL output + version-pinned manifest).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-CONTEXT.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frozen sklearn capture — RF/ET, GB, IsolationForest, HistGB (numerical + categorical) goldens</name>
  <files>fixtures/capture_sklearn.py, fixtures/sklearn_rf.golden.json, fixtures/sklearn_gb.golden.json, fixtures/sklearn_iforest.golden.json, fixtures/sklearn_histgb_numerical.golden.json, fixtures/sklearn_histgb_categorical.golden.json</files>
  <read_first>
    - fixtures/capture_golden_v5.py (the run-once-commit-never-regenerate analog — paths-relative-to-script resolution, manifest key set + sha256/version pins, empirical-assertion-in-capture pattern)
    - fixtures/golden_v5.manifest.json (the manifest shape to mirror)
    - treelite-mainline/python/treelite/sklearn/importer.py (porting source — ArrayOfArrays extraction contract: exactly which attributes (children_left/children_right/feature/threshold/value/n_node_samples/weighted_n_node_samples/impurity/node_count) and dtypes; GB leaf-shrink capture-side at 220-223; HistGB _preprocessor/features_map/categories_map capture at 355-478)
    - treelite-mainline/python/treelite/sklearn/isolation_forest.py (expected_depth/calculate_depths/ratio_c derivation)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md (D-06/D-07; Pitfall 4 — HistGB capture-side sklearn version gate; prefer a NON-categorical HistGB fixture first then a categorical one; HistGB packed-struct itemsize 52/56)
  </read_first>
  <action>
    Write `fixtures/capture_sklearn.py` as a one-time frozen capture run via `uv run python` on the main worktree. It must first `uv pip install scikit-learn lightgbm` into the capture env (treelite==4.7.0 + numpy already present) and RECORD the exact installed sklearn/lightgbm/treelite/numpy versions in each manifest (D-06). With a fixed seed, fit and dump, per family: RandomForest + ExtraTrees (classifier + regressor) → node arrays in the `importer.py` dtype contract; GradientBoosting (classifier + regressor) → arrays with leaf-shrink ALREADY applied capture-side (`value*learning_rate`, importer.py:220-223 — the Rust loader must NOT re-shrink); IsolationForest → node arrays + `ratio_c = expected_depth(max_samples_)`; HistGradientBoosting numerical-only (so `features_map = arange`, identity remap, Pitfall 4) AND a separate categorical HistGB (exercising `categories_map`/`features_map`) → the packed `nodes` byte buffer + `expected_sizeof_node_struct` (nodes.itemsize ∈ {52,56}) + features_map + categories_map. For EVERY family, the committed golden prediction vector is `treelite.gtil.predict(model, X)` on the captured input matrix X (D-07), NOT the framework's `predict()`. Print `help(treelite.gtil.predict)` once (as Phase-1 did) and assert the DEFAULT predict kind. For IsolationForest, additionally assert at capture time that the golden == `-clf.score_samples(X)` (D-07 cross-check). Freeze input matrix + golden + manifest into the named `sklearn_*.golden.json` files (mirror `golden.json`/`golden_v5.manifest.json` structure: input, expected output, version pins, sha256, seed). The framework's own `predict()` may be recorded only as a secondary sanity field, never the asserted target.
  </action>
  <verify>
    <automated>test -f fixtures/sklearn_rf.golden.json && test -f fixtures/sklearn_gb.golden.json && test -f fixtures/sklearn_iforest.golden.json && test -f fixtures/sklearn_histgb_numerical.golden.json && test -f fixtures/sklearn_histgb_categorical.golden.json && grep -q "gtil" fixtures/capture_sklearn.py</automated>
  </verify>
  <acceptance_criteria>
    - All five `sklearn_*.golden.json` files exist and are non-empty (the `test -f` verify exits 0)
    - Source assertion: `fixtures/capture_sklearn.py` contains `treelite.gtil.predict` (golden source is Treelite GTIL, D-07)
    - Behavior: `capture_sklearn.py` asserts (at capture time) the IsolationForest golden equals `-clf.score_samples(X)` — the assertion is present in the script
    - Each golden JSON carries a manifest block with sklearn + lightgbm + treelite + numpy version strings and the seed
    - The HistGB numerical golden uses a non-categorical estimator (features_map = identity) and the categorical golden carries categories_map (Pitfall 4 split)
    - GB goldens carry leaf-shrunk values (capture-side); the script comments note the Rust loader must NOT re-shrink
  </acceptance_criteria>
  <done>Five frozen sklearn goldens (RF, GB, IForest, HistGB numerical, HistGB categorical) committed with treelite-GTIL outputs + version-pinned manifests; IForest cross-check present.</done>
</task>

<task type="auto">
  <name>Task 2: Frozen LightGBM capture — numerical + categorical goldens</name>
  <files>fixtures/capture_lightgbm.py, fixtures/lightgbm_numerical.golden.json, fixtures/lightgbm_categorical.txt, fixtures/lightgbm_categorical.golden.json</files>
  <read_first>
    - fixtures/capture_golden_v5.py (run-once-commit-never-regenerate discipline; manifest + sha256 pattern)
    - treelite-mainline/tests/examples/deep_lightgbm/model.txt (the vendored v4 numerical LightGBM fixture, 32 leaves, no categorical — the LGB-01 numerical smoke source per RESEARCH A2)
    - treelite-mainline/src/model_loader/lightgbm.cc (objective/class_id/average_output semantics the golden must reflect)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md (D-06/D-07; A2 — vendored deep_lightgbm/model.txt is the numerical smoke fixture; a fresh categorical LightGBM model must be fit for LGB-02 bitset coverage)
  </read_first>
  <action>
    Write `fixtures/capture_lightgbm.py` (one-time frozen, `uv run python`, main worktree). For the NUMERICAL case: load the vendored `treelite-mainline/tests/examples/deep_lightgbm/model.txt` via `treelite.frontend.load_lightgbm_model` (or the 4.7.0 equivalent — print `help()` once to confirm), capture `treelite.gtil.predict(model, X)` on a seeded input matrix X, and freeze input + golden + manifest into `fixtures/lightgbm_numerical.golden.json` (reference the vendored `model.txt` path; do not copy it). For the CATEGORICAL case (LGB-02 bitset coverage): fit a small LightGBM model WITH categorical features, dump its text form to `fixtures/lightgbm_categorical.txt`, then capture `treelite.gtil.predict` on a seeded X into `fixtures/lightgbm_categorical.golden.json`. Record lightgbm + treelite + numpy versions + seed in both manifests. Golden = Treelite GTIL output (D-07), never the LightGBM framework predict (framework predict may be a secondary sanity field only).
  </action>
  <verify>
    <automated>test -f fixtures/lightgbm_numerical.golden.json && test -f fixtures/lightgbm_categorical.txt && test -f fixtures/lightgbm_categorical.golden.json && grep -q "gtil" fixtures/capture_lightgbm.py</automated>
  </verify>
  <acceptance_criteria>
    - `fixtures/lightgbm_numerical.golden.json`, `fixtures/lightgbm_categorical.txt`, `fixtures/lightgbm_categorical.golden.json` all exist and are non-empty (verify exits 0)
    - Source assertion: `fixtures/capture_lightgbm.py` contains `treelite.gtil.predict` (D-07)
    - The numerical golden references the vendored `deep_lightgbm/model.txt` (does not duplicate it) and carries an input matrix + treelite-GTIL output
    - The categorical golden + its `.txt` model exercise at least one categorical split (so LGB-02 bitset decode has a real target)
    - Both manifests carry lightgbm + treelite + numpy version strings + seed
  </acceptance_criteria>
  <done>LightGBM numerical + categorical goldens frozen with treelite-GTIL outputs + version-pinned manifests; categorical model exercises a bitset split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture-env→repo | One-time Python capture writes read-only fixtures; never executed at Rust runtime/CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | scikit-learn / lightgbm capture-only installs | mitigate | Both are first-party-maintained, ubiquitous PyPI packages used ONLY in the frozen one-time capture (never in the Rust build graph, shipped artifact, or CI runtime); pin exact versions + record in each manifest (D-06). See T-04-SC below. |
| T-04-06 | Repudiation | Golden drift if regenerated with a different sklearn version | mitigate | Manifest records sklearn/lightgbm/treelite/numpy versions + seed; harness `check_manifest` warns on environment drift; fixtures are write-once (D-06) |
| T-04-SC | Tampering | pip installs (scikit-learn, lightgbm) | mitigate | RESEARCH Package Legitimacy Audit tags both `[ASSUMED]` (slopcheck unavailable in sandbox) — both are universally-known first-party scientific packages, capture-only, negligible slopsquat risk; versions pinned + recorded. They never enter the Rust/CI graph, so no blocking Rust-side install gate is required; the capture task pins versions in the manifest. |
</threat_model>

<verification>
- All `sklearn_*` and `lightgbm_*` golden fixtures exist and are non-empty
- Capture scripts source goldens from `treelite.gtil.predict` (grep `gtil`)
- IsolationForest golden == `-score_samples` cross-check present in capture script
- Each golden manifest pins sklearn/lightgbm/treelite/numpy versions + seed
</verification>

<success_criteria>
- Frozen per-estimator goldens captured from upstream Treelite GTIL (D-07) with version-pinned manifests (D-06)
- HistGB split into numerical (identity feature map) and categorical fixtures (Pitfall 4)
- LightGBM numerical (vendored) + categorical (fresh) goldens both present
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- New capture scripts: `fixtures/capture_sklearn.py`, `fixtures/capture_lightgbm.py`
- New frozen golden fixtures: `fixtures/sklearn_rf.golden.json`, `fixtures/sklearn_gb.golden.json`, `fixtures/sklearn_iforest.golden.json`, `fixtures/sklearn_histgb_numerical.golden.json`, `fixtures/sklearn_histgb_categorical.golden.json`, `fixtures/lightgbm_numerical.golden.json`, `fixtures/lightgbm_categorical.golden.json`
- New LightGBM categorical text model: `fixtures/lightgbm_categorical.txt`
- No new crates or CLI flags in this plan
</artifacts_produced>

<output>
Create `.planning/phases/04-lightgbm-scikit-learn-loaders/04-03-SUMMARY.md` when done
</output>
