---
phase: 04-lightgbm-scikit-learn-loaders
plan: 07
type: execute
wave: 3
depends_on: ["04-06"]
files_modified:
  - crates/treelite-sklearn/src/mixin.rs
  - crates/treelite-sklearn/src/lib.rs
  - crates/treelite-harness/tests/sklearn.rs
autonomous: true
requirements: [SKL-03]
must_haves:
  truths:
    - "IsolationForest imports via the MixIn path with exponential_standard_ratio postprocessor + ratio_c"
    - "The IsolationForest golden equals upstream treelite.gtil.predict (== -clf.score_samples), within 1e-5 — NOT the framework's own anomaly score"
  artifacts:
    - path: "crates/treelite-sklearn/src/lib.rs"
      provides: "load_isolation_forest(... ratio_c: f64) (D-01 signature)"
      exports: ["load_isolation_forest"]
    - path: "crates/treelite-sklearn/src/mixin.rs"
      provides: "IsolationForest metadata: kIsolationForest, average_tree_output, ratio_c, exponential_standard_ratio"
  key_links:
    - from: "crates/treelite-sklearn/src/mixin.rs"
      to: "treelite-gtil exponential_standard_ratio postprocessor"
      via: "postprocessor name + ratio_c"
      pattern: "exponential_standard_ratio|ratio_c"
    - from: "crates/treelite-harness/tests/sklearn.rs"
      to: "fixtures/sklearn_iforest.golden.json"
      via: "1e-5 golden assert (golden == -score_samples)"
      pattern: "1e-5|assert_abs_diff"
---

<objective>
Vertical slice: IsolationForest imports (SKL-03) via the MixIn path with the `exponential_standard_ratio` postprocessor and `ratio_c`, verified within 1e-5 of its treelite-GTIL golden. This is the canonical "Treelite ≠ framework" case — the golden is `treelite.gtil.predict` (== `-clf.score_samples`, D-07), deliberately NOT the framework's anomaly score. Reuses the Plan-06 MixIn machinery and the Plan-02 `exponential_standard_ratio` postprocessor.

Purpose: Proves the verify-narrow contract where Treelite's output deliberately differs from the framework, and exercises the `exp2`-based postprocessor + `ratio_c` end-to-end.
Output: `load_isolation_forest` + IsolationForest MixIn metadata + a passing `sklearn_iforest` golden test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-CONTEXT.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IsolationForest MixIn loader (ratio_c, exponential_standard_ratio) + sklearn_iforest 1e-5 golden test</name>
  <files>crates/treelite-sklearn/src/mixin.rs, crates/treelite-sklearn/src/lib.rs, crates/treelite-harness/tests/sklearn.rs</files>
  <read_first>
    - crates/treelite-sklearn/src/mixin.rs (the Plan-06 MixIn machinery to extend — per-node f64 builder emission, BuilderMetadata fill)
    - crates/treelite-sklearn/src/lib.rs (the Plan-06 array-signature surface to add `load_isolation_forest` to)
    - treelite-mainline/src/model_loader/sklearn.cc (porting source — IsolationForest MixIn 33-57, LoadSKLearnModel dispatch 373-383)
    - treelite-mainline/python/treelite/sklearn/isolation_forest.py (expected_depth/calculate_depths: `ratio_c = expected_depth(max_samples_)` — n<=1→0, n==2→1, else 2*(ln(n-1)+euler_gamma) - 2*(n-1)/n; leaf value = pre-computed isolation depth, importer.py:191-205)
    - treelite-mainline/include/treelite/model_loader.h (LoadIsolationForest signature ~161 — `+ ratio_c: f64`)
    - crates/treelite-gtil/src/postprocessor.rs (the Plan-02 `exponential_standard_ratio` — `(-v/ratio_c).exp2()`)
    - crates/treelite-gtil/src/lib.rs (where `ratio_c` is threaded into the postprocessor dispatch — Plan 02)
    - fixtures/sklearn_iforest.golden.json (Plan-03 golden == -score_samples)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md (Code Examples — IsolationForest MixIn metadata; D-07 golden == -score_samples; exponential_standard_ratio exp2 base-2)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-PATTERNS.md (mixin.rs IsolationForest specifics)
  </read_first>
  <behavior>
    - Test: `load_isolation_forest` sets task=kIsolationForest, average_tree_output=true, num_target=1, num_class=[1], leaf_vector_shape={1,1}, postprocessor="exponential_standard_ratio", model.ratio_c = the passed ratio_c, base_scores=[0.0]
    - Test: leaf values are the pre-computed isolation depths consumed AS-IS (no GTIL-side depth recomputation in the loader)
    - Test: the `sklearn_iforest` golden predicts within 1e-5 of `treelite.gtil.predict` (which equals `-clf.score_samples`), NOT the framework's own score
  </behavior>
  <action>
    Add `load_isolation_forest(n_estimators: i32, n_features: i32, node_count: &[i64], children_left: &[&[i64]], children_right: &[&[i64]], feature: &[&[i64]], threshold: &[&[f64]], value: &[&[f64]], n_node_samples: &[&[i64]], ..., ratio_c: f64) -> Result<Model, SklError>` to `lib.rs` (mirror model_loader.h:161, D-01). In `mixin.rs`, route it through the same per-node f64 builder path as GradientBoosting (leaf detection `children_left[i] == -1`), emitting `leaf_scalar_f64(value[tree][node])` (the pre-computed isolation depth — consumed AS-IS, no recomputation). Set metadata per sklearn.cc:33-57: task_type=kIsolationForest, average_tree_output=true, num_target=1, num_class=[1], leaf_vector_shape={1,1}, postprocessor="exponential_standard_ratio", base_scores=[0.0], and post-commit assign `model.ratio_c = ratio_c`. The `ratio_c` value itself is computed capture-side (`expected_depth(max_samples_)`, isolation_forest.py) and passed in — the loader does NOT recompute it. Then add a `sklearn_iforest` test to `crates/treelite-harness/tests/sklearn.rs`: load the array dump from `fixtures/sklearn_iforest.golden.json` via `load_isolation_forest` (passing the captured ratio_c), predict, `assert_abs_diff_eq!(epsilon = 1e-5)` against the golden (which the capture cross-checked == `-score_samples`), track max_dev, call `check_manifest`. No `panic!`/`anyhow`.
  </action>
  <verify>
    <automated>cargo test -p treelite-harness sklearn_iforest</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness sklearn_iforest` exits 0
    - Source assertion: `crates/treelite-sklearn/src/lib.rs` exports `load_isolation_forest` with a `ratio_c: f64` parameter (D-01)
    - Source assertion: the IsolationForest metadata sets postprocessor `"exponential_standard_ratio"` and assigns `model.ratio_c` (grep `exponential_standard_ratio` and `ratio_c`)
    - Behavior: max|delta| between Rust prediction and the treelite-GTIL golden (== -score_samples) < 1e-5 (printed)
    - Behavior: leaf isolation depths consumed as-is (no loader-side recomputation — asserted via a leaf-value test)
    - No regression: `cargo test --workspace` green
  </acceptance_criteria>
  <done>IsolationForest loads with exponential_standard_ratio + ratio_c; sklearn_iforest golden passes within 1e-5 against treelite.gtil.predict (== -score_samples); workspace green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| array dump → loader | IsolationForest node arrays untrusted; child indices and ratio_c must not cause OOB/UB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Denial of Service | OOB child indices in IsolationForest arrays | mitigate | Reuse the Plan-06 MixIn `ModelBuilder` orphan/dangling validation + bounds-check children before indexing per-node arrays; typed `SklError`, never OOB |
| T-04-17 | Tampering | `ratio_c == 0` causing div-by-zero in exponential_standard_ratio | mitigate | The postprocessor computes `(-v/ratio_c).exp2()`; guard `ratio_c != 0` (upstream `expected_depth` returns 0 only for n<=1, an unreachable degenerate fixture) — document and reject/clamp a 0 ratio_c with a typed error rather than producing inf/NaN silently |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — N/A |
</threat_model>

<verification>
- `cargo test -p treelite-harness sklearn_iforest` green (max|delta| < 1e-5 vs treelite.gtil.predict)
- `cargo test --workspace` green
</verification>

<success_criteria>
- IsolationForest imports via MixIn with exponential_standard_ratio + ratio_c (SKL-03)
- Golden asserted against upstream treelite.gtil.predict (== -score_samples), NOT framework anomaly score (D-07)
- Predicts within 1e-5
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07)
- New public function: `load_isolation_forest(... ratio_c: f64) -> Result<Model, SklError>` (crates/treelite-sklearn/src/lib.rs)
- Extended `mixin.rs` with IsolationForest metadata (kIsolationForest + exponential_standard_ratio + ratio_c)
- New harness test: `sklearn_iforest` (in crates/treelite-harness/tests/sklearn.rs)
- Consumes fixtures from Plan 03: `fixtures/sklearn_iforest.golden.json`
</artifacts_produced>

<output>
Create `.planning/phases/04-lightgbm-scikit-learn-loaders/04-07-SUMMARY.md` when done
</output>
