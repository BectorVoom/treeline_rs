---
phase: 04-lightgbm-scikit-learn-loaders
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - crates/treelite-lightgbm/src/bitset.rs
  - crates/treelite-lightgbm/src/parse.rs
  - crates/treelite-lightgbm/src/lib.rs
  - crates/treelite-lightgbm/src/error.rs
  - crates/treelite-gtil/src/lib.rs
  - crates/treelite-harness/tests/lightgbm.rs
autonomous: true
requirements: [LGB-02]
must_haves:
  truths:
    - "LightGBM categorical splits decode from their bitset into category lists via BitsetToList (word/bit order exact)"
    - "Categorical fields parse at upstream per-field precision (cat_threshold = u32, cat_boundaries = u64)"
    - "A categorical LightGBM model loads and predicts within 1e-5 of its treelite-GTIL golden"
  artifacts:
    - path: "crates/treelite-lightgbm/src/bitset.rs"
      provides: "BitsetToList categorical bitset decode (lightgbm.cc:210-221)"
      contains: "BitsetToList"
    - path: "crates/treelite-gtil/src/lib.rs"
      provides: "NextNodeCategorical traversal branch (minimal, for LightGBM categorical)"
      contains: "categor"
  key_links:
    - from: "crates/treelite-lightgbm/src/lib.rs"
      to: "treelite_builder::ModelBuilder::categorical_test"
      via: "categorical node emission"
      pattern: "categorical_test"
    - from: "crates/treelite-harness/tests/lightgbm.rs"
      to: "fixtures/lightgbm_categorical.golden.json"
      via: "1e-5 golden assert"
      pattern: "1e-5|assert_abs_diff"
---

<objective>
Extend the LightGBM slice with categorical-split decoding (LGB-02): port `BitsetToList` verbatim, parse the categorical per-field precision (cat_threshold = u32, cat_boundaries = u64), emit `categorical_test` nodes through the f64 builder, add the minimal `NextNodeCategorical` GTIL traversal branch, and verify a categorical LightGBM model within 1e-5 of its treelite-GTIL golden.

Purpose: Second LightGBM slice. The bitset word/bit order and boundary slicing are exact — any deviation silently mis-decodes categories. Exhaustive categorical PREDICTION parity beyond the captured fixture aligns with Phase-5's categorical GTIL (deferred); this slice proves decode + the captured-fixture path.
Output: `bitset.rs` + categorical parse/emit + minimal `NextNodeCategorical` + a passing `lightgbm_categorical` golden test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-CONTEXT.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port BitsetToList + categorical parse + categorical_test emission</name>
  <files>crates/treelite-lightgbm/src/bitset.rs, crates/treelite-lightgbm/src/parse.rs, crates/treelite-lightgbm/src/lib.rs, crates/treelite-lightgbm/src/error.rs</files>
  <read_first>
    - crates/treelite-lightgbm/src/lib.rs (the Plan-04 converge-then-build path the categorical branch plugs into; numerical_test/leaf emission loop)
    - crates/treelite-lightgbm/src/parse.rs (the Plan-04 LGBTree/LGBModel structs to extend with cat_threshold/cat_boundaries fields)
    - crates/treelite-lightgbm/src/error.rs (the Plan-04 LgbError enum to extend with a bitset/categorical error variant)
    - crates/treelite-builder/src/lib.rs (the `categorical_test(split_index, default_left, categories, right_child, left, right)` builder entry at lib.rs:254-278 — f64 mode from Plan 01)
    - treelite-mainline/src/model_loader/lightgbm.cc (porting source — BitsetToList 210-221, cat_boundaries/cat_threshold slicing 563-573, categorical default direction 569-572, GetMissingType 206-208)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-PATTERNS.md (No-Analog-Found: bitset.rs — port BitsetToList verbatim; do NOT share code with the HistGB `check(bitmap,...)` sibling, DIFFERENT layout)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md (LGB-02 per-field precision; Don't-Hand-Roll BitsetToList; Pitfall 3 categorical missing→right, default_left=false)
  </read_first>
  <behavior>
    - Test: `BitsetToList` decodes a known bitset word array into the exact category list — word = `bits[i/32]`, bit = `i%32`, slot count from `cat_boundaries[k+1]-cat_boundaries[k]`; a reference bitset → reference category list, exact
    - Test: `cat_threshold` parses as `u32` and `cat_boundaries` as `u64` (per-field precision, LGB-02)
    - Test: a categorical node emits `categorical_test` with `default_left=false` and missing-goes-right (lightgbm.cc:569-572)
  </behavior>
  <action>
    Create `crates/treelite-lightgbm/src/bitset.rs` porting `BitsetToList` VERBATIM from lightgbm.cc:210-221 (word = `bits[i/32]`, bit test `(word >> (i%32)) & 1`, slot count = `cat_boundaries[k+1]-cat_boundaries[k]`) — DO NOT reinvent the word/bit order, and DO NOT share code with the HistGB `check(bitmap,...)` sibling (different layout). Extend `parse.rs` LGBTree with `cat_threshold: Vec<u32>` and `cat_boundaries: Vec<u64>` (exact per-field precision), validating `cat_threshold.len() == cat_boundaries.back()` before slicing (Security Domain: reject mismatch with a typed `LgbError`, never an OOB slice). Extend `error.rs` with a `Bitset{detail}` / categorical error variant. In `lib.rs`, when a node's `decision_type` marks it categorical, slice the bitset via `cat_boundaries`, decode the category list via `BitsetToList`, and emit `builder.categorical_test(split_index, /*default_left=*/false, &categories, /*right_child=*/false, left, right)` (categorical missing always goes right, default_left=false per lightgbm.cc:569-572). Bounds-check every index before use. No `panic!`/`anyhow`.
  </action>
  <verify>
    <automated>cargo test -p treelite-lightgbm bitset</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-lightgbm` exits 0 including the `bitset` tests
    - Source assertion: `crates/treelite-lightgbm/src/bitset.rs` contains `BitsetToList` (or a `fn bitset_to_list`) and uses `i % 32` word/bit indexing
    - Source assertion: `parse.rs` parses `cat_threshold` as `u32` and `cat_boundaries` as `u64`
    - Behavior: `BitsetToList` decodes a reference bitset to the exact expected category list (asserted in test)
    - Behavior: a categorical node emits `categorical_test` with `default_left=false`
    - Behavior: `cat_threshold.len() != cat_boundaries.back()` returns a typed `LgbError`, not a panic/OOB
    - No `panic!`/`anyhow` in the library crate
  </acceptance_criteria>
  <done>BitsetToList ported verbatim; categorical fields parsed at exact precision; categorical_test emitted with correct default direction; bounds-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimal NextNodeCategorical GTIL branch + lightgbm_categorical 1e-5 golden test</name>
  <files>crates/treelite-gtil/src/lib.rs, crates/treelite-harness/tests/lightgbm.rs</files>
  <read_first>
    - crates/treelite-gtil/src/lib.rs (the `evaluate_tree` + `PredictScalar` traversal at 28-127; the numerical `NextNode` branch to extend with a categorical branch; the Plan-02 widened output shaping)
    - crates/treelite-core/src/tree.rs (the category_list accessors — `category_list`, `category_list_begin/end`, `category_list_right_child` columns the categorical_test emission filled)
    - treelite-mainline/src/gtil/predict.cc (porting source — NextNodeCategorical: test membership in the node's category list, route by `category_list_right_child` polarity)
    - crates/treelite-harness/tests/lightgbm.rs (the Plan-04 lightgbm test file to add `lightgbm_categorical` to)
    - fixtures/lightgbm_categorical.golden.json (the categorical golden from Plan 03)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md (D-03 minimal pull-forward; categorical evaluation parity beyond the captured fixture is deferred to Phase 5)
  </read_first>
  <behavior>
    - Test: a single categorical node with a known category list routes a row whose feature value IS in the list one way and a row NOT in the list the other way, matching the `category_list_right_child` polarity
    - Test: the `lightgbm_categorical` golden predicts within 1e-5 of its treelite-GTIL golden
  </behavior>
  <action>
    Add a minimal `NextNodeCategorical` branch to `evaluate_tree` in `crates/treelite-gtil/src/lib.rs`: for a categorical node, read the node's category list (via the `category_list_begin/end` slice of `category_list`), test whether the row's feature value (as an integer category, with the float-representability guard deferred to Phase 5 — note this in a comment) is a member, and route to the left/right child per the `category_list_right_child` polarity (port the upstream NextNodeCategorical logic). Keep it MINIMAL (D-03) — only what the captured `lightgbm_categorical` fixture exercises; the exhaustive categorical matrix + float-representability guard (GTIL-06) is Phase 5. Then add a `lightgbm_categorical` test to `crates/treelite-harness/tests/lightgbm.rs` mirroring the `lightgbm_numerical` test: load `fixtures/lightgbm_categorical.txt` via `load_lightgbm`, predict, `assert_abs_diff_eq!(epsilon = 1e-5)`, track max_dev. No `panic!`/`anyhow`.
  </action>
  <verify>
    <automated>cargo test -p treelite-harness lightgbm_categorical</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness lightgbm_categorical` exits 0
    - Source assertion: `crates/treelite-gtil/src/lib.rs` has a categorical traversal branch (grep `categor`)
    - Behavior: category membership routes rows by `category_list_right_child` polarity (asserted in a GTIL unit test)
    - Behavior: max|delta| on the categorical golden < 1e-5 (printed)
    - No regression: `cargo test --workspace` green (numerical lightgbm + XGBoost gates intact)
  </acceptance_criteria>
  <done>Minimal NextNodeCategorical branch added; lightgbm_categorical golden passes within 1e-5; workspace green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted .txt → bitset decode | Categorical bitset words/boundaries are untrusted; OOB on crafted boundaries must be impossible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Denial of Service | `cat_boundaries` slicing into `cat_threshold` (OOB on crafted boundaries) | mitigate | Validate `cat_threshold.len() == cat_boundaries.back()` and each `cat_boundaries[k]` monotone/in-range BEFORE slicing; mismatch → typed `LgbError`, never OOB (ASVS V5; Security Domain threat table) |
| T-04-11 | Denial of Service | `BitsetToList` indexing `bits[i/32]` beyond the bitset word array | mitigate | Bound the category scan to the bitset word length; never index past `bits.len()` |
| T-04-12 | Denial of Service | GTIL `category_list_begin/end` slice OOB | mitigate | Bounds-check the begin/end slice against `category_list.len()` before membership test |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — N/A |
</threat_model>

<verification>
- `cargo test -p treelite-lightgbm bitset` green
- `cargo test -p treelite-harness lightgbm_categorical` green (max|delta| < 1e-5)
- `cargo test --workspace` green
</verification>

<success_criteria>
- LightGBM categorical splits decode via BitsetToList with exact word/bit order + per-field precision (LGB-02)
- A categorical LightGBM model predicts within 1e-5 of its treelite-GTIL golden
- All bitset/category slicing bounds-checked (no OOB on hostile input)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)
- New file `crates/treelite-lightgbm/src/bitset.rs` with `BitsetToList` (categorical bitset decode)
- Extended `LGBTree` parse struct fields: `cat_threshold: Vec<u32>`, `cat_boundaries: Vec<u64>`
- New `LgbError` variant for bitset/categorical errors
- New GTIL categorical traversal branch (`NextNodeCategorical`) in `crates/treelite-gtil/src/lib.rs`
- New harness test: `lightgbm_categorical` (in crates/treelite-harness/tests/lightgbm.rs)
- Consumes fixtures from Plan 03: `fixtures/lightgbm_categorical.txt`, `fixtures/lightgbm_categorical.golden.json`
</artifacts_produced>

<output>
Create `.planning/phases/04-lightgbm-scikit-learn-loaders/04-05-SUMMARY.md` when done
</output>
