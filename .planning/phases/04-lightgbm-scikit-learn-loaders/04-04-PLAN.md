---
phase: 04-lightgbm-scikit-learn-loaders
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - crates/treelite-lightgbm/Cargo.toml
  - crates/treelite-lightgbm/src/lib.rs
  - crates/treelite-lightgbm/src/parse.rs
  - crates/treelite-lightgbm/src/objective.rs
  - crates/treelite-lightgbm/src/error.rs
  - crates/treelite-harness/tests/lightgbm.rs
autonomous: true
requirements: [LGB-01, LGB-03]
must_haves:
  truths:
    - "A LightGBM text-format model loads into a ModelVariant::F64 Model and predicts within 1e-5 of its treelite-GTIL golden"
    - "LightGBM negative-index leaf encoding is re-numbered into a clean depth-wise node sequence (no orphan/dangling errors)"
    - "objective maps to the correct postprocessor with parsed sigmoid_alpha, class_id[i]=i%num_class round-robin, and average_output honored"
  artifacts:
    - path: "crates/treelite-lightgbm/src/lib.rs"
      provides: "load_lightgbm(&str) -> Result<Model, LgbError>"
      exports: ["load_lightgbm"]
    - path: "crates/treelite-lightgbm/src/parse.rs"
      provides: "line-based key=value parse → typed LGBTree/LGBModel structs (per-field precision)"
    - path: "crates/treelite-lightgbm/src/objective.rs"
      provides: "CanonicalObjective alias collapse + objective→postprocessor map + sigmoid_alpha parse"
  key_links:
    - from: "crates/treelite-lightgbm/src/lib.rs"
      to: "treelite_builder::ModelBuilder (f64 mode)"
      via: "converge-then-build emission"
      pattern: "leaf_scalar_f64|numerical_test"
    - from: "crates/treelite-harness/tests/lightgbm.rs"
      to: "fixtures/lightgbm_numerical.golden.json"
      via: "1e-5 golden assert"
      pattern: "1e-5|assert_abs_diff"
---

<objective>
Vertical slice: a LightGBM text-format model loads → predicts → verifies within 1e-5 of its frozen treelite-GTIL golden (LGB-01, LGB-03). Creates the new `treelite-lightgbm` crate mirroring `treelite-xgboost`, with the line-based parser, the objective→postprocessor map (CanonicalObjective + sigmoid_alpha + class_id round-robin + average_output), the negative-index leaf re-numbering, and emission through the f64 ModelBuilder (D-02/D-05). Categorical bitset decode (LGB-02) is the next slice (Plan 05).

Purpose: First of the ascending-risk estimator slices; proves LightGBM loading is real, not structural, against the vendored `deep_lightgbm/model.txt` numerical fixture.
Output: `treelite-lightgbm` crate + `load_lightgbm` + a passing `lightgbm_numerical` golden test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-CONTEXT.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md
@.planning/phases/04-lightgbm-scikit-learn-loaders/04-PATTERNS.md
@crates/treelite-xgboost/src/lib.rs
@crates/treelite-xgboost/src/objective.rs
@crates/treelite-xgboost/src/error.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create treelite-lightgbm crate — parser, objective map, error enum, converge-then-build</name>
  <files>crates/treelite-lightgbm/Cargo.toml, crates/treelite-lightgbm/src/parse.rs, crates/treelite-lightgbm/src/objective.rs, crates/treelite-lightgbm/src/error.rs, crates/treelite-lightgbm/src/lib.rs</files>
  <read_first>
    - crates/treelite-xgboost/src/lib.rs (converge-then-build template — validators `require_non_negative`/`check_dim` at 63-96, per-tree `build_tree`/per-node loop at 142-167, `build_model_from_parsed`, `BuilderMetadata` fill + commit at 248-274)
    - crates/treelite-xgboost/src/objective.rs (objective→postprocessor match structure to mirror)
    - crates/treelite-xgboost/src/error.rs (thiserror enum shape + the `#[error(transparent)]` Core/Builder bridges; the `Legacy { pos, detail }` parse-error variant at 93-121 to mirror for a LightGBM parse error)
    - crates/treelite-xgboost/Cargo.toml (crate manifest shape) and the root Cargo.toml (workspace members list)
    - crates/treelite-builder/src/lib.rs (the f64 builder entry points from Plan 01: `leaf_scalar_f64`, `numerical_test`, `BuilderMetadata`, f64 `commit_model`)
    - treelite-mainline/src/model_loader/lightgbm.cc (porting source — line parser 157-414, objective→postprocessor switch 442-515, class_id round-robin 427-429, average_output 289-291, node-id reassignment 533-601, missing-type default override 579-584)
    - treelite-mainline/src/model_loader/detail/lightgbm.h (CanonicalObjective alias table 26-57)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-PATTERNS.md (lib.rs/objective.rs/parse.rs/error.rs analog sections)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-RESEARCH.md (Pitfall 2 node-id reassignment; Pitfall 3 missing-value default override; LGB-03 objective map)
  </read_first>
  <behavior>
    - Test: `objective.rs` maps canonicalized names to postprocessors — `multiclass`→`softmax`, `binary`→`sigmoid` (with sigmoid_alpha parsed from `sigmoid:<a>`, must be > 0), `cross_entropy`→`sigmoid` (alpha=1.0), `poisson`/`gamma`/`tweedie`→`exponential`, `regression`→`identity` (or `signed_square` if `sqrt` in params); an unknown objective returns a typed `LgbError`, not a panic
    - Test: CanonicalObjective collapses an alias (e.g. `l2_root`→`regression`) before the map (a missing alias would route to the wrong postprocessor)
    - Test: parsing `deep_lightgbm/model.txt` yields the expected num_leaves/num_class and per-field precision (leaf_value/threshold parsed as f64, split_gain as f32)
    - Test: the negative-index leaf re-numbering produces a tree with no orphan/dangling-child errors when emitted through the builder
  </behavior>
  <action>
    The crate is already registered in the root `Cargo.toml` `members` list by Plan 01 — do NOT edit the root manifest here. Create `crates/treelite-lightgbm/Cargo.toml` mirroring `treelite-xgboost/Cargo.toml` (deps: `treelite-core`, `treelite-builder`, `thiserror` workspace; dev-deps: `treelite-gtil` path + `approx` workspace — avoid the harness dep cycle). Implement: `parse.rs` — a line-based `key=value` tokenizer (std `lines()`/`split('=')`, space-delimited arrays) producing typed `LGBModel`/`LGBTree` structs with EXACT per-field precision (leaf_value/threshold = `f64`, split_gain = `f32`, decision_type = `i8`, num_class/num_leaves = ints); every malformed count returns `LgbError` not an OOB slice. `objective.rs` — port `CanonicalObjective` (detail/lightgbm.h:26-57) alias-collapse FIRST, then the objective→postprocessor `match` (lightgbm.cc:442-515) returning a `&'static str` postprocessor name + parsing `sigmoid:<a>` for `sigmoid_alpha` (>0 check). `error.rs` — copy the XGBoost enum shape: `DimensionMismatch{tree,field,expected,got}`, `UnrecognizedObjective(String)`, transparent `Core`/`Builder` bridges, plus a `Parse{line, detail}` variant. `lib.rs` — the converge-then-build path: `load_lightgbm(&str) -> Result<Model, LgbError>` → parse → validators (`require_non_negative`/`check_dim` copied verbatim) → per-tree emission through the **f64** `ModelBuilder` (Plan 01: `start_tree`/`start_node`/`numerical_test`/`leaf_scalar_f64`/`gain`/`end_node`/`end_tree`), with the negative-index leaf re-numbering BFS deque ported from lightgbm.cc:533-601 (seed `(0,1)` normal / `(-1,1)` single-leaf; dfs_index starts at 1, +2 per internal node; `old_node_id < 0` ⇒ leaf value `leaf_value[!old_node_id as usize]`), and the Pitfall-3 missing-type `default_left` override (`default_left = (0.0 <= threshold)` when `missing_type != kNaN`, lightgbm.cc:579-584). Fill `BuilderMetadata` routing through the f64 builder, set `class_id[i] = i % num_class`, `average_tree_output` from the `average_output` key presence, and post-commit assign `model.sigmoid_alpha`. No `panic!`/`anyhow`.
  </action>
  <verify>
    <automated>cargo test -p treelite-lightgbm</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-lightgbm` exits 0 (objective + parse + node-id tests)
    - Source assertion: `crates/treelite-lightgbm/src/lib.rs` exports `pub fn load_lightgbm` and emits through f64 builder calls (`leaf_scalar_f64`)
    - Source assertion: `crates/treelite-lightgbm/src/parse.rs` parses `leaf_value`/`threshold` as `f64` and `split_gain` as `f32` (per-field precision, LGB-02 precursor)
    - Behavior: `objective.rs` maps `multiclass`→softmax, `binary`→sigmoid, `poisson`/`gamma`/`tweedie`→exponential; unknown objective → `LgbError::UnrecognizedObjective`
    - Behavior: loading `deep_lightgbm/model.txt` produces a `ModelVariant::F64` model with no builder orphan/dangling errors
    - No `panic!`/`anyhow` in the library crate (grep `-v '^//'` clean)
  </acceptance_criteria>
  <done>treelite-lightgbm crate loads a LightGBM text model into a ModelVariant::F64 Model with correct objective map, class_id round-robin, average_output, and node-id re-numbering.</done>
</task>

<task type="auto">
  <name>Task 2: LightGBM numerical 1e-5 golden harness test</name>
  <files>crates/treelite-harness/tests/lightgbm.rs</files>
  <read_first>
    - crates/treelite-harness/src/lib.rs (the `Golden`/`Manifest` structs at 76-108, `load_golden` at 116, `run_equivalence` flatten+predict+assert at 189-233, hard `1e-5` epsilon at 229, `check_manifest` at 242, `fixture_path` resolver)
    - crates/treelite-harness/tests/golden_v5.rs (the per-golden test pattern: fixture_path resolver, load, flatten, predict, assert_abs_diff_eq with max_dev tracking)
    - fixtures/lightgbm_numerical.golden.json (the golden produced by Plan 03 — input matrix + treelite-GTIL output + manifest)
    - crates/treelite-lightgbm/src/lib.rs (the `load_lightgbm` entry point under test)
    - .planning/phases/04-lightgbm-scikit-learn-loaders/04-PATTERNS.md (harness test analog section)
  </read_first>
  <action>
    Add `crates/treelite-harness/tests/lightgbm.rs` with a `lightgbm_numerical` test mirroring `tests/golden_v5.rs`: resolve `fixtures/lightgbm_numerical.golden.json` + the vendored `deep_lightgbm/model.txt` via the `fixture_path` resolver, `load_lightgbm` the text model, flatten the golden's input matrix, call `treelite_gtil::predict`, and `approx::assert_abs_diff_eq!(got, expected, epsilon = 1e-5)` while tracking and printing `max_dev` (EQV-04 spirit). Reuse the existing `Golden`/`Manifest`/`check_manifest` helpers; call `check_manifest` to surface environment drift. The `1e-5` epsilon is a HARD gate — never loosened. Add `treelite-lightgbm` as a dev-dependency of `treelite-harness`.
  </action>
  <verify>
    <automated>cargo test -p treelite-harness lightgbm_numerical</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p treelite-harness lightgbm_numerical` exits 0
    - Behavior: max|delta| between Rust prediction and the treelite-GTIL golden is < 1e-5 (printed by the test)
    - Source assertion: `crates/treelite-harness/tests/lightgbm.rs` uses `epsilon = 1e-5` (hard gate, not loosened)
    - No regression: `cargo test --workspace` green
  </acceptance_criteria>
  <done>The lightgbm_numerical golden test passes within 1e-5; max deviation reported; workspace green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted .txt → parser | LightGBM text file is untrusted input; malformed counts/indices must not panic or read OOB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Denial of Service | Malformed `num_leaves`/array lengths in the text parser → OOB slice | mitigate | Validate counts before slicing; `require_non_negative`/`check_dim` (copied from XGBoost) run BEFORE builder emission; malformed input returns `LgbError::Parse`/`DimensionMismatch`, never a panic or OOB (ASVS V5) |
| T-04-08 | Denial of Service | Negative-index leaf decode `leaf_value[!old_node_id]` out of range | mitigate | Bounds-check the decoded leaf index against `leaf_value.len()` before access; out-of-range → typed `LgbError`, never a panic |
| T-04-09 | Tampering | `sigmoid_alpha` parsed ≤ 0 routing a degenerate sigmoid | mitigate | Port the upstream `sigmoid_alpha > 0` check; reject with a typed error |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new third-party packages; `treelite-lightgbm` is an internal path crate — N/A |
</threat_model>

<verification>
- `cargo test -p treelite-lightgbm` green (objective/parse/node-id)
- `cargo test -p treelite-harness lightgbm_numerical` green (max|delta| < 1e-5)
- `cargo test --workspace` green — no XGBoost regression
</verification>

<success_criteria>
- A LightGBM text model loads into a ModelVariant::F64 Model and predicts within 1e-5 of its treelite-GTIL golden (LGB-01)
- objective→postprocessor map + sigmoid_alpha + class_id round-robin + average_output correct (LGB-03)
- Negative-index leaf re-numbering and missing-type default override ported verbatim
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- New crate `treelite-lightgbm` (added to root Cargo.toml members)
- New public function: `load_lightgbm(&str) -> Result<Model, LgbError>` (crates/treelite-lightgbm/src/lib.rs)
- New types: `LgbError` (error enum), `LGBModel`/`LGBTree` (parse structs), the LightGBM `objective.rs` map + `CanonicalObjective` helper
- New harness test: `crates/treelite-harness/tests/lightgbm.rs` (`lightgbm_numerical`)
- Consumes fixtures from Plan 03: `fixtures/lightgbm_numerical.golden.json`
</artifacts_produced>

<output>
Create `.planning/phases/04-lightgbm-scikit-learn-loaders/04-04-SUMMARY.md` when done
</output>
