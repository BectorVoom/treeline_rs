---
phase: 07-gpu-backend-equivalence-report
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - crates/treelite-harness/src/report.rs
  - crates/treelite-harness/src/lib.rs
  - crates/treelite-harness/tests/gtil_matrix_gpu.rs
  - crates/treelite-harness/tests/gpu_crossover.rs
  - docs/GPU_EQUIVALENCE_REPORT.md
  - docs/GPU_CROSSOVER.md
autonomous: false
requirements: [GPU-04]
user_setup: []
must_haves:
  truths:
    - "The harness emits an OBSERVATIONAL per-model-class deviation report — measured, never a CI gate (D-01)"
    - "Report columns are: model class | postprocessor | ROCm max-|delta| | f64-fallback used? | CUDA | wgpu | predicted band (D-08), one row per frozen-golden model class (D-07)"
    - "The report carries the D-03 predicted-deviation band alongside the measured ROCm column; absent backends render 'not run — no device' (D-05/D-08)"
    - "The report is regenerated by running the ROCm-feature sibling on the developer's AMD hardware — never hand-edited (D-06)"
    - "The GPU sibling NEVER fails on a GPU |delta| > 1e-5 (records only, D-01); the CPU 1e-5 gate stays on the scalar/cubecl-cpu siblings, untouched"
    - "The CPU/GPU crossover is MEASURED on ROCm hardware and DOCUMENTED (not enforced in code, predict() does not auto-route); CPU stays the default backend (SC3, D-09/D-10)"
  artifacts:
    - path: "crates/treelite-harness/src/report.rs"
      provides: "Report-mode max-|delta| accumulation + markdown (+ optional JSON sidecar) emitter from per-cell provenance (D-06/D-08)"
      contains: "max_abs_delta_report_mode"
    - path: "crates/treelite-harness/tests/gtil_matrix_gpu.rs"
      provides: "#[ignore]'d ROCm sibling driving the frozen matrix in report mode (D-06)"
      contains: "rocm_case"
    - path: "crates/treelite-harness/tests/gpu_crossover.rs"
      provides: "#[ignore]'d wall-clock crossover sweep CPU vs ROCm across num_row (D-10)"
      contains: "rocm_case"
    - path: "docs/GPU_EQUIVALENCE_REPORT.md"
      provides: "Committed GPU equivalence report (GPU-04)"
      contains: "max"
    - path: "docs/GPU_CROSSOVER.md"
      provides: "Documented (not enforced) CPU/GPU crossover heuristic + dominant metric (SC3/D-09/D-10)"
      contains: "crossover"
  key_links:
    - from: "crates/treelite-harness/tests/gtil_matrix_gpu.rs"
      to: "treelite_harness::report"
      via: "emits the markdown report from per-cell provenance"
      pattern: "report::"
    - from: "crates/treelite-harness/tests/gtil_matrix_gpu.rs"
      to: "rocm_case() + frozen fixtures/gtil/*.golden.json"
      via: "drives the SAME frozen matrix on the ROCm backend (D-07)"
      pattern: "fixtures/gtil"
    - from: "crates/treelite-harness/src/report.rs"
      to: "treelite_cubecl::model_routes_to_scalar_fallback"
      via: "per-cell provenance (no re-derived parallel copy, WR-04)"
      pattern: "model_routes_to_scalar_fallback"
    - from: "crates/treelite-harness/tests/gpu_crossover.rs"
      to: "docs/GPU_CROSSOVER.md"
      via: "wall-clock sweep writes the documented crossover number"
      pattern: "GPU_CROSSOVER"
---

<objective>
Turn measured GPU runs into the committed Phase-7 artifacts. Add a `report.rs` to `treelite-harness` that accumulates per-cell max-|delta| in REPORT MODE (record, never panic — D-01) from the same per-cell provenance the cubecl matrix uses, a new `#[ignore]`'d `gtil_matrix_gpu.rs` sibling that drives the SAME frozen `fixtures/gtil/*.golden.json` matrix (D-07) through `rocm_case()` and emits `docs/GPU_EQUIVALENCE_REPORT.md` (GPU-04), and a `#[ignore]`'d `gpu_crossover.rs` wall-clock sweep that MEASURES and DOCUMENTS the CPU/GPU crossover point (SC3/D-09/D-10) in `docs/GPU_CROSSOVER.md`. The equivalence report carries one row per frozen-golden model class with the D-03 predicted-deviation band alongside the measured ROCm column; CUDA/wgpu render "not run — no device" (D-05). The crossover is documented-only — `predict()` does NOT auto-route below the threshold (D-09); CPU stays the default backend. The developer regenerates both artifacts by running the ROCm-feature siblings on the AMD hardware (D-06/D-10, the checkpoint below) — the numbers are never hand-edited.

Purpose: The slice that converts the GPU vertical (Plan 03) into the committed, harness-emitted, observational equivalence report (GPU-04) AND the documented crossover heuristic (SC3). The 1e-5 hard gate stays on the CPU spine.
Output: `report.rs` + `gtil_matrix_gpu.rs` + `gpu_crossover.rs` siblings + committed `docs/GPU_EQUIVALENCE_REPORT.md` and `docs/GPU_CROSSOVER.md`, regenerated on ROCm hardware.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gpu-backend-equivalence-report/07-RESEARCH.md
@.planning/phases/07-gpu-backend-equivalence-report/07-PATTERNS.md
@.planning/phases/07-gpu-backend-equivalence-report/07-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See Plan 07-01's "Artifacts this phase produces" section — this plan creates the `treelite_harness::report` module (incl. `max_abs_delta_report_mode`), the `gtil_matrix_gpu.rs` and `gpu_crossover.rs` siblings, and the committed `docs/GPU_EQUIVALENCE_REPORT.md` + `docs/GPU_CROSSOVER.md` (exclude from drift verification).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author report.rs (report-mode delta + markdown/JSON emitter) and wire it into the harness lib</name>
  <files>crates/treelite-harness/src/report.rs, crates/treelite-harness/src/lib.rs</files>
  <read_first>
    - crates/treelite-harness/tests/gtil_matrix_cubecl.rs (READ `assert_within` 296-324 IN FULL — the max-deviation accumulation loop; the NaN/inf-structural skip + finite-|delta| logic is COPIED, the hard `approx::assert_abs_diff_eq!` gate at ~321 is DROPPED for report mode, D-01; also the provenance tagging 44-60 / 405-431 and `model_routes_to_scalar_fallback` delegation 330-340)
    - crates/treelite-harness/src/manifest.rs (the `Manifest` struct fields 36-101: `backend`, `rustc`, `os`, `arch`, device/model — reuse for the report header, D-06)
    - crates/treelite-harness/src/lib.rs (add `pub mod report;`)
    - .planning/phases/07-gpu-backend-equivalence-report/07-RESEARCH.md (Report Emission lines 347-371: markdown layout, emission mechanics, the recommended JSON sidecar; the predicted-deviation table lines 327-334 for the predicted-band column values)
    - .planning/phases/07-gpu-backend-equivalence-report/07-PATTERNS.md (the report.rs section lines 228-249, the predicted-band-per-class source)
  </read_first>
  <action>
    Create `crates/treelite-harness/src/report.rs` and add `pub mod report;` to `lib.rs`. Provide `fn max_abs_delta_report_mode(got: &[f64], want: &[f64]) -> f64` copied from `assert_within`'s loop with the hard-gate assert REMOVED (record max_dev, skip NaN/non-finite cells, NEVER panic — D-01). Provide a markdown emitter that writes the columns `model class | postprocessor | ROCm max-|delta| | f64 fallback used? | CUDA | wgpu | predicted band (D-03)` with a header line carrying the `Manifest` provenance (device name / ROCm version / rustc / date) and a stated reference (f64 scalar GTIL spine) + "Observational — NOT a CI gate (D-01)" + the honest determinism note ("observed run-to-run stable on <device>; bit-identity NOT guaranteed on GPU"). The predicted-band column draws from the D-03 per-postprocessor-family table (RESEARCH lines 327-334). Emit a machine-readable JSON sidecar `gpu_equivalence.json` alongside (RECOMMENDED, D-08 — `[{model_class, postprocessor, backend, max_abs_delta, f64_fallback, predicted_low, predicted_high}]`); keep it observational (not asserted as a gate). Use `treelite_cubecl::model_routes_to_scalar_fallback` for per-cell provenance — never a re-derived parallel copy (WR-04).
  </action>
  <verify>
    <automated>cargo test --workspace && cargo clippy -p treelite-harness -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub mod report' crates/treelite-harness/src/lib.rs` returns 1.
    - `grep -c 'max_abs_delta_report_mode' crates/treelite-harness/src/report.rs` returns >= 1.
    - `grep -c 'assert_abs_diff_eq\|assert_within' crates/treelite-harness/src/report.rs` returns 0 (report mode NEVER hard-gates, D-01).
    - `grep -c 'model_routes_to_scalar_fallback' crates/treelite-harness/src/report.rs` returns >= 1 (provenance reuse, WR-04).
    - `cargo test --workspace` green (the new module compiles; default cpu-only build unaffected).
  </acceptance_criteria>
  <done>`report.rs` provides the report-mode max-|delta| accumulator + markdown/JSON emitter reusing the shared provenance predicate, wired into the harness lib; no hard gate; workspace suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the gtil_matrix_gpu.rs ROCm sibling (report-mode, skip-not-fail) and the gpu_crossover.rs sweep</name>
  <files>crates/treelite-harness/tests/gtil_matrix_gpu.rs, crates/treelite-harness/tests/gpu_crossover.rs</files>
  <read_first>
    - crates/treelite-harness/tests/gtil_matrix_cubecl.rs (READ THE WHOLE FILE — the gpu sibling copies its structure; the deltas table is in 07-PATTERNS.md lines 257-265: swap `cubecl_cpu_case()`→`rocm_case()`, add `#[ignore]` + `#![cfg(feature = "rocm")]`, replace the 1e-5 `assert_within` panic with report-mode RECORD, handle `Err(DeviceUnavailable)`→"not run — no device"+CONTINUE, call `report::` to emit; copy the fixture-load helpers `fixture_path`/`cell_to_f64`/`flatten_output`/`kind_of`/`decode_input_f64`/`frozen_csr`/`run_cell`/`MatrixGolden`/`MatrixManifest` verbatim, lines 62-292)
    - crates/treelite-harness/src/report.rs (the Task-1 emitter the gpu sibling calls)
    - crates/treelite-harness/src/lib.rs (`rocm_case()`/`cuda_case()`/`wgpu_case()` from Plan 03; `scalar_cpu_case()`/`cubecl_cpu_case()` for the crossover CPU baseline)
    - 07-01-SUMMARY.md (the A3 finding: if GPU client FFI-aborts on no device, cuda/wgpu rows render "not run" WITHOUT constructing a client; if catchable, `Err(DeviceUnavailable)` handling suffices)
    - .planning/phases/07-gpu-backend-equivalence-report/07-RESEARCH.md (Validation Architecture skip-not-fail lines 443-445; Pitfall 4 host-vs-device postproc lines 399-403 — postprocessing stays host-side this phase; CPU/GPU Crossover lines 373-377 for the sweep + dominant-metric guidance)
  </read_first>
  <action>
    Create `crates/treelite-harness/tests/gtil_matrix_gpu.rs` as a SIBLING of `gtil_matrix_cubecl.rs`: gate the file with `#![cfg(feature = "rocm")]`, mark the driver `#[test] #[ignore]` (developer runs it on ROCm hardware to regenerate the report, D-06), drive the SAME frozen `fixtures/gtil/*.golden.json` cross-product (D-07) through `rocm_case()`, compute per-cell deviation via `report::max_abs_delta_report_mode` (RECORD, never panic — D-01), on `Err(DeviceUnavailable)` mark "not run — no device" + CONTINUE (skip, D-05), attempt cuda/wgpu per the A3 finding, then call the Task-1 emitter to write `docs/GPU_EQUIVALENCE_REPORT.md` + JSON sidecar with the predicted-band + measured-ROCm + f64-fallback-used (D-02) + CUDA/wgpu cells. NEVER fail on `|delta| > 1e-5`.
    Create `crates/treelite-harness/tests/gpu_crossover.rs` — a `#![cfg(feature = "rocm")] #[test] #[ignore]` wall-clock sweep (D-10): time a fixed model through a CPU baseline case (`cubecl_cpu_case()` or `scalar_cpu_case()`) vs `rocm_case()` across `num_row ∈ {1, 10, 100, 1_000, 10_000, 100_000}` (and a couple of forest sizes), INCLUDING upload + launch + readback in the GPU timing. Record where ROCm wall-clock first beats CPU and which metric dominates (row count vs rows×features — let the data decide, do NOT pre-commit a formula). Write the empirical crossover number + dominant metric to `docs/GPU_CROSSOVER.md`, stating explicitly that this is DOCUMENTED-ONLY: `predict()` does NOT auto-route (D-09), CPU stays the default backend, and the Phase-8 PyO3 caller is the documented consumer.
  </action>
  <verify>
    <automated>cargo test --workspace && cargo build -p treelite-harness --features rocm --tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'rocm_case' crates/treelite-harness/tests/gtil_matrix_gpu.rs` returns >= 1; `grep -c 'rocm_case' crates/treelite-harness/tests/gpu_crossover.rs` returns >= 1.
    - `grep -c '#\[ignore\]' crates/treelite-harness/tests/gtil_matrix_gpu.rs` and `... gpu_crossover.rs` each return >= 1; both files `grep -c 'cfg(feature = "rocm")'` >= 1.
    - `grep -c 'assert_within\|assert_abs_diff_eq' crates/treelite-harness/tests/gtil_matrix_gpu.rs` returns 0 (observational — never hard-gates GPU delta, D-01).
    - `grep -c 'fixtures/gtil' crates/treelite-harness/tests/gtil_matrix_gpu.rs` returns >= 1 (drives the SAME frozen goldens, D-07).
    - `cargo test --workspace` (default) green — both siblings are feature+ignore-gated and do not run in default CI; `cargo build -p treelite-harness --features rocm --tests` compiles them.
  </acceptance_criteria>
  <done>`gtil_matrix_gpu.rs` (report-mode, skip-not-fail) and `gpu_crossover.rs` (wall-clock sweep) exist, are feature+ignore-gated, drive the frozen matrix / sweep through `rocm_case()`, and write the committed report + crossover doc; default suite green and the rocm feature build compiles both.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regenerate and commit the GPU report + crossover doc on ROCm hardware (GPU-04, D-06/D-10)</name>
  <what-built>
    Plans 01-03 added the `R`-generic launcher, `Backend::Rocm` + `rocm_case()`, and the device-absent typed skip. Tasks 1-2 of this plan added `report.rs`, the `#[ignore]`'d `gtil_matrix_gpu.rs` ROCm sibling that drives the frozen golden matrix in report mode and emits `docs/GPU_EQUIVALENCE_REPORT.md` (+ `gpu_equivalence.json`), and the `#[ignore]`'d `gpu_crossover.rs` wall-clock sweep that writes `docs/GPU_CROSSOVER.md`. This step RUNS both on the developer's AMD/ROCm hardware to produce the committed numbers (D-06/D-10 — regenerated by the harness, never hand-edited).
  </what-built>
  <how-to-verify>
    On the AMD/ROCm box (memory `gpu-hardware-rocm-only`; run on the main tree, not a worktree, per memory `worktree-isolation-unsafe-here`; use `uv run` if any python is involved):
    1. Run: `cargo test -p treelite-harness --features rocm --test gtil_matrix_gpu -- --ignored`
       Expected: produces predictions for the frozen golden model set on `Backend::Rocm` (GPU-03 satisfied) and writes `docs/GPU_EQUIVALENCE_REPORT.md` + `gpu_equivalence.json`.
    2. Open `docs/GPU_EQUIVALENCE_REPORT.md` and confirm: one row per frozen-golden model class (D-07); the columns populate (model class, postprocessor, ROCm max-|delta|, f64-fallback used?, CUDA, wgpu, predicted band); CUDA renders "not run — no device" (no NVIDIA); wgpu renders its actual result or "not run" per the AMD/Vulkan capability; the header carries device/ROCm/rustc/date provenance and the "Observational — NOT a CI gate" + determinism-not-guaranteed notes.
    3. Sanity-check the measured ROCm |delta| values fall in (or are flagged outside) their D-03 predicted bands — a value an order of magnitude above its band signals a `native_exp` mapping (Pitfall 2) worth recording.
    4. Run: `cargo test -p treelite-harness --features rocm --test gpu_crossover -- --ignored`
       Expected: writes `docs/GPU_CROSSOVER.md` with the empirical crossover row count + dominant metric + the documented-only note (predict() does not auto-route, CPU stays default).
    5. Confirm the default CI gate is unaffected: `cargo test --workspace` (no features) stays green at 1e-5.
    6. Commit the regenerated `docs/GPU_EQUIVALENCE_REPORT.md` (+ JSON sidecar) and `docs/GPU_CROSSOVER.md`.
  </how-to-verify>
  <resume-signal>Type "approved" once both artifacts are regenerated on ROCm hardware and committed, or describe any row that is missing / outside its predicted band / failed to run.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROCm device → readback buffers → report numbers | GPU output crosses back to the host for the observational deviation measurement and the crossover timing. No network/auth/untrusted-input surface — the inputs are the frozen, already-validated golden fixtures (D-07). |
| committed report/doc files ← harness emission | The report + crossover doc are repo files written by the harness, never hand-edited (D-06/D-10); their integrity is "regenerated from the executed path." |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Repudiation / Tampering | Hand-edited report/crossover numbers drifting from reality | mitigate | Both artifacts are harness-emitted from the executed path and regenerated on ROCm hardware (D-06/D-10); the JSON sidecar lets a future check assert no silent drift. The checkpoint requires live regeneration, not manual edits. |
| T-07-08 | Information Disclosure (honesty) | A green-looking report masking real GPU divergence | mitigate | Observational, never a CI gate (D-01); records |delta| even above 1e-5; states determinism is observed-not-guaranteed; carries the predicted band so an out-of-band measured value is a visible finding (Pitfall 2). |
| T-07-09 | Denial of Service | cuda/wgpu rows aborting the report run on absent hardware | mitigate | Skip-not-fail: `Err(DeviceUnavailable)`→"not run — no device"+CONTINUE; honors the Plan-01 A3 finding (pre-probe if the driver hard-aborts). |
| T-07-SC | Tampering | cargo installs | mitigate | No new crates; the report/crossover tooling reuses existing harness deps. RESEARCH legitimacy posture holds. |

No high-severity threats: observational reporting + wall-clock timing over frozen trusted fixtures with no external-input surface. The realistic surface is artifact-honesty (T-07-07/08), mitigated by harness-emission + the observational/predicted-band design.
</threat_model>

<verification>
- `cargo test --workspace` (default cpu-only) green — both GPU siblings are feature+ignore-gated and never run in default CI; the 1e-5 hard gate is untouched.
- `cargo build -p treelite-harness --features rocm --tests` compiles the siblings.
- On ROCm hardware (checkpoint): the matrix sibling produces predictions + emits the committed report; the crossover sweep emits the documented heuristic; CUDA renders "not run — no device"; the report is observational (records |delta| > 1e-5 without failing).
</verification>

<success_criteria>
- An observational, harness-emitted, committed `docs/GPU_EQUIVALENCE_REPORT.md` with one row per frozen-golden model class, the columns, and the D-03 predicted band alongside the measured ROCm column (GPU-04).
- A committed `docs/GPU_CROSSOVER.md` with the measured crossover number + dominant metric, documented-only (predict() does not auto-route; CPU stays default — SC3/D-09/D-10).
- Both artifacts regenerated on ROCm hardware (D-06/D-10), never hand-edited; absent backends render "not run — no device" (D-05).
- The GPU siblings never hard-gate on GPU |delta|; the CPU 1e-5 gate stays on the scalar/cubecl-cpu siblings.
</success_criteria>

<output>
Create `.planning/phases/07-gpu-backend-equivalence-report/07-04-SUMMARY.md` when done, recording the measured ROCm per-class deviations vs their predicted bands, whether any class needed f64 promotion (D-02), the wgpu row's actual capability on the AMD/Vulkan path, and the measured crossover number + dominant metric.
</output>
