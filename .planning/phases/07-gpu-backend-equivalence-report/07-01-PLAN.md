---
phase: 07-gpu-backend-equivalence-report
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/treelite-cubecl/Cargo.toml
  - crates/treelite-cubecl/src/error.rs
  - crates/treelite-cubecl/src/lib.rs
  - crates/treelite-cubecl/src/device.rs
  - crates/treelite-cubecl/tests/device_absent.rs
autonomous: true
requirements: [GPU-03]
must_haves:
  truths:
    - "Default `cargo build` and `cargo test --workspace` stay green with NO ROCm/CUDA libs present (D-04)"
    - "`treelite-cubecl` exposes additive `rocm`/`cuda`/`wgpu` cargo features that forward to the cubecl umbrella (D-04)"
    - "Selecting a compiled-in backend with no device yields a typed `CubeclError::DeviceUnavailable`, never a process abort (D-05)"
    - "The Wave-0 spike records whether cubecl HIP/CUDA `client()` returns a catchable error or FFI-aborts on a missing device (RESEARCH Open Q A3)"
  artifacts:
    - path: "crates/treelite-cubecl/src/device.rs"
      provides: "Per-backend ComputeClient<R> constructor + device-availability path (D-05)"
      contains: "DeviceUnavailable"
    - path: "crates/treelite-cubecl/src/error.rs"
      provides: "DeviceUnavailable error variant"
      contains: "DeviceUnavailable"
    - path: "crates/treelite-cubecl/tests/device_absent.rs"
      provides: "Device-absence spike + typed-skip test (Pitfall 1 / A3)"
      contains: "DeviceUnavailable"
    - path: "crates/treelite-cubecl/Cargo.toml"
      provides: "rocm/cuda/wgpu features forwarding to cubecl"
      contains: "rocm = [\"cubecl/rocm\"]"
  key_links:
    - from: "crates/treelite-cubecl/src/device.rs"
      to: "crates/treelite-cubecl/src/error.rs"
      via: "returns CubeclError::DeviceUnavailable"
      pattern: "DeviceUnavailable"
    - from: "crates/treelite-cubecl/Cargo.toml"
      to: "cubecl umbrella features"
      via: "feature forwarding"
      pattern: "cubecl/rocm"
---

<objective>
Stand up the Wave-0 infrastructure that gates every later Phase-7 backend registration: the additive `rocm`/`cuda`/`wgpu` cargo features (default = cpu only, D-04), the `CubeclError::DeviceUnavailable` typed variant (D-05), the `device.rs` per-backend `ComputeClient<R>` constructor, and the MANDATORY device-absence spike that answers RESEARCH Open Question A3 — does cubecl HIP/CUDA `client()` return a catchable error or hard-FFI-abort on a missing device? That answer determines whether `DeviceUnavailable` is reachable via `catch_unwind` or needs a pre-construction probe, and it MUST be settled before any `*_case()` registration (Plan 03) attempts to construct a GPU client.

Purpose: De-risk the single HIGH-risk unknown (A3) and lay the typed-error + feature seam before generalizing the launcher. This is the thinnest "the seam is provable" slice — after it, a missing device is a typed skip, not a crash.
Output: New `device.rs` + `DeviceUnavailable` variant + `[features]` table in `treelite-cubecl/Cargo.toml` + `tests/device_absent.rs` spike, with the default cpu-only build untouched and green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gpu-backend-equivalence-report/07-CONTEXT.md
@.planning/phases/07-gpu-backend-equivalence-report/07-RESEARCH.md
@.planning/phases/07-gpu-backend-equivalence-report/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This phase (across all four plans) creates these NEW symbols — exclude them from source-drift verification:
- `Backend::Rocm`, `Backend::Cuda`, `Backend::Wgpu` (harness enum variants — Plan 03)
- `rocm_case()`, `cuda_case()`, `wgpu_case()` (harness RunnerCase constructors — Plan 03)
- `CubeclError::DeviceUnavailable { backend }` (error variant — THIS plan)
- `treelite_cubecl::predict::<R, F>` (generalized launcher — Plan 02) + the `predict_cpu` shim over it
- `treelite_cubecl::device::client::<R>()` and per-backend `rocm_client`/`cuda_client`/`wgpu_client` (THIS plan + Plan 02)
- cargo features `rocm`, `cuda`, `wgpu` on `treelite-cubecl` (THIS plan)
- `treelite_harness::report` emitter symbol(s) + `max_abs_delta_report_mode` (Plan 04)
- committed report file `docs/GPU_EQUIVALENCE_REPORT.md` (Plan 04)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the rocm/cuda/wgpu cargo features and the DeviceUnavailable error variant</name>
  <files>crates/treelite-cubecl/Cargo.toml, crates/treelite-cubecl/src/error.rs</files>
  <read_first>
    - crates/treelite-cubecl/Cargo.toml (the [dependencies] block; no [features] table exists today — the analog is the `cubecl = { workspace = true }` line)
    - crates/treelite-cubecl/src/error.rs (the whole `CubeclError` enum; copy the `#[error(...)]` + doc-comment discipline from the `Unsupported(String)` variant at lines 86-87; note the enum derives `Debug, Error, PartialEq, Eq`)
    - Cargo.toml (root, line 25 `cubecl = { version = "0.10.0", features = ["cpu"] }` — this line MUST stay cpu-only per Pitfall 5; do NOT add GPU features at the workspace level)
    - .planning/phases/07-gpu-backend-equivalence-report/07-PATTERNS.md (the error.rs and Cargo.toml pattern sections, lines 118-165)
  </read_first>
  <action>
    Add a `[features]` table to `crates/treelite-cubecl/Cargo.toml`: `default = []`, `rocm = ["cubecl/rocm"]` (implies `cubecl/hip`), `cuda = ["cubecl/cuda"]`, `wgpu = ["cubecl/wgpu"]`. These forward to the cubecl 0.10.0 umbrella crate's runtime features per RESEARCH Standard Stack — do NOT add separate `cubecl-hip`/`cubecl-cuda`/`cubecl-wgpu` deps, and explicitly do NOT reference `zenforks-cubecl-hip` (the removed typosquat fork). Leave root `Cargo.toml` line 25 cpu-only.
    Add a `DeviceUnavailable { backend: &'static str }` struct-variant to `CubeclError` with `#[error("no device available for the {backend} backend (skip, not a failure)")]` and a doc-comment stating it is a typed SKIP (D-05) the caller branches on — NEVER a silent CPU fallback. The `&'static str` field keeps the existing `PartialEq, Eq` derives intact.
  </action>
  <verify>
    <automated>cargo build -p treelite-cubecl && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' crates/treelite-cubecl/Cargo.toml | grep -c 'rocm = \["cubecl/rocm"\]'` returns 1; same for the `cuda`/`wgpu` lines.
    - `grep -c DeviceUnavailable crates/treelite-cubecl/src/error.rs` returns >= 1.
    - `grep -c 'zenforks' crates/treelite-cubecl/Cargo.toml Cargo.toml` returns 0 (the typosquat is never referenced).
    - `grep -c 'cubecl/rocm\|cubecl/cuda\|cubecl/wgpu' Cargo.toml` (ROOT) returns 0 (workspace stays cpu-only, Pitfall 5).
    - `cargo build -p treelite-cubecl` (default, no features) succeeds; `cargo test --workspace` stays green (no ROCm libs required).
  </acceptance_criteria>
  <done>The crate compiles with the three additive features declared and the `DeviceUnavailable` variant present; the default cpu-only build and full workspace test suite are green with no GPU system libs present.</done>
</task>

<task type="auto">
  <name>Task 2: Author device.rs client constructor + the A3 device-absence spike test</name>
  <files>crates/treelite-cubecl/src/device.rs, crates/treelite-cubecl/src/lib.rs, crates/treelite-cubecl/tests/device_absent.rs</files>
  <read_first>
    - crates/treelite-cubecl/src/lib.rs (line 312 `CpuRuntime::client(&Default::default())` — the analog for the generic client constructor; the `use cubecl::...` imports at lines 31-36; add `mod device;` near the other module declarations)
    - crates/treelite-cubecl/src/error.rs (the `DeviceUnavailable` variant added in Task 1)
    - .planning/phases/07-gpu-backend-equivalence-report/07-RESEARCH.md (Pattern 2 lines 229-251, the ROCm client pattern lines 491-508, Pitfall 1 lines 381-385, Open Question 1 lines 533-536)
    - .planning/phases/07-gpu-backend-equivalence-report/07-PATTERNS.md (the device.rs section lines 97-114)
  </read_first>
  <action>
    Create `crates/treelite-cubecl/src/device.rs` and declare `mod device;` in `lib.rs`. Provide a generic `pub fn client<R: Runtime>() -> Result<ComputeClient<R>, CubeclError>` plus `#[cfg(feature = "...")]`-gated per-backend helpers: `rocm_client()` returning `ComputeClient<cubecl::hip::HipRuntime>`, `cuda_client()` returning `ComputeClient<cubecl::cuda::CudaRuntime>`, `wgpu_client()` returning `ComputeClient<cubecl::wgpu::WgpuRuntime>`. Each constructs `R::client(&<Device>::default())` (the uniform cubecl pattern; wgpu may use `WgpuDevice::DefaultDevice` — confirm the exact 0.10 symbol/init surface via `cargo doc` / the `find-docs` ctx7 CLI before committing, A1). The cpu path mirrors `lib.rs:312` exactly. As the FIRST attempt, wrap GPU construction in `std::panic::catch_unwind` mapping any unwind to `CubeclError::DeviceUnavailable { backend: "rocm" }` etc.
    Create `crates/treelite-cubecl/tests/device_absent.rs` — the A3 spike. Behind `#[cfg(feature = "cuda")]` (CUDA is the absent-device case on this NVIDIA-less box), invoke `cuda_client()` and assert it returns `Err(CubeclError::DeviceUnavailable { .. })` — NOT a crash. CRITICAL spike finding to RECORD in the SUMMARY: whether `catch_unwind` traps the failure or the process hard-aborts (the C driver `abort()` below catch_unwind's reach). If it aborts, the SUMMARY MUST flag that Plan 03 needs a pre-construction device probe (cubecl enumeration API, confirmed via `cargo doc`) so the harness never calls `client()` for an absent backend. The default (no-feature) test body is a no-op so `cargo test --workspace` stays green without CUDA.
  </action>
  <verify>
    <automated>cargo build -p treelite-cubecl && cargo test --workspace && cargo clippy -p treelite-cubecl -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mod device' crates/treelite-cubecl/src/lib.rs` returns >= 1.
    - `grep -c 'fn client<R\|rocm_client\|cuda_client\|wgpu_client' crates/treelite-cubecl/src/device.rs` returns >= 1 for each helper name.
    - `grep -c DeviceUnavailable crates/treelite-cubecl/tests/device_absent.rs` returns >= 1.
    - `cargo build -p treelite-cubecl` (no features) and `cargo test --workspace` are green WITHOUT any GPU libs (the cfg-gated GPU code does not compile in the default build).
    - SUMMARY records the A3 finding (catchable error vs FFI-abort) and, if abort, the explicit instruction that Plan 03 must pre-probe device availability.
  </acceptance_criteria>
  <done>`device.rs` exists with the generic + per-backend constructors returning typed `DeviceUnavailable` on no device; the spike test compiles under each feature and is a no-op by default; the A3 behavior (catchable vs abort) is recorded in the SUMMARY for Plan 03 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPU driver FFI (HIP/CUDA) ← cubecl client construction | A missing-device driver-init failure crosses from the C driver into the Rust process; this is the only untrusted-control-flow surface this plan touches. There is no network, auth, or untrusted-input surface (numerical compute over already-trusted in-process fixtures). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | `device::*_client()` on a missing GPU device | mitigate | Wrap construction in `catch_unwind` → typed `DeviceUnavailable`; the A3 spike (Task 2) confirms whether the driver hard-aborts; if so, the SUMMARY mandates a pre-construction probe in Plan 03 so an absent backend is never constructed (never crashes the host process). |
| T-07-02 | Tampering | Workspace default build accidentally pulling a GPU runtime (Pitfall 5) | mitigate | Features forward via the crate-local `[features]` table only; root `Cargo.toml` stays cpu-only; acceptance grep asserts no `cubecl/rocm` at the workspace level. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH Package Legitimacy Audit verified the four official `cubecl` family crates (registry + repo + downloads); `zenforks-cubecl-hip` is a typosquat fork — acceptance grep asserts it is never referenced. No new top-level crates added (cubecl is already a dep); features only. |

No high-severity threats exist for this plan: it is numerical GPU compute over trusted in-process model fixtures with no network/auth/untrusted-input surface. The realistic surface is the driver-FFI DoS (T-07-01), mitigated by the typed skip + the mandatory A3 spike.
</threat_model>

<verification>
- `cargo build -p treelite-cubecl` (default) and `cargo test --workspace` green with no GPU system libs (D-04 / Pitfall 5 regression).
- `DeviceUnavailable` variant present and reachable; `device.rs` constructors return it on no device.
- The A3 device-absence behavior is recorded (catchable vs FFI-abort) for Plan 03.
</verification>

<success_criteria>
- Additive `rocm`/`cuda`/`wgpu` features declared, forwarding to the cubecl umbrella; default = cpu only.
- `CubeclError::DeviceUnavailable` typed variant exists (D-05).
- `device.rs` provides generic + per-backend client constructors that map no-device to the typed skip.
- The A3 spike answers whether a missing device is a catchable error or an FFI abort, and the SUMMARY carries that finding forward.
- The 1e-5 CPU spine and default build are untouched and green.
</success_criteria>

<output>
Create `.planning/phases/07-gpu-backend-equivalence-report/07-01-SUMMARY.md` when done. The SUMMARY MUST explicitly record the A3 spike finding (does cubecl HIP/CUDA `client()` return a catchable `Err` or hard-abort on a missing device) and, if it aborts, the instruction that Plan 03 must pre-probe device availability before construction.
</output>
