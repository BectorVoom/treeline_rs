//! cubecl `#[cube(launch)]` traversal + postprocessor kernels (Wave 3).
//!
//! Placeholder module. The kernel surface (`descend`/`next_node` `#[cube]`
//! helpers, the 10 postprocessor ports, and the per-predict-kind launch
//! kernels `predict_default`/`predict_raw`, `predict_leaf_id`,
//! `predict_score_per_tree`) lands in plan 06-04 once the Wave 1 spike and the
//! Wave 2 upload contracts are green. Authored against the cubecl 0.10.0 API
//! names pinned in `upload.rs`.
